# REAL_02：銜尾蛇發現協定 - 傳輸層與基礎發現 (OODP L1-L3)

> [!NOTE]: [Standard / 混合規範性]  
> 本規範定義 **OODP (Ouroboros Discovery Protocol)** 的 **L1-L3 層**實作：物理傳輸、節點定址與基礎收斂協議。

> [!IMPORTANT]
> - **[Core Requirement]**：為了確保跨實作的互操作性，實作者**必須**遵循的規範。
> - **[Reference Recommendation]**：**建議**實作者採用的最佳實踐。

本文件是 **[SPEC_13: 銜尾蛇發現協定](./SPEC_13_Ouroboros_Discovery_Protocol.md)** 的 **L1-L3 層**工程實作配套。

關於 OODP 五層架構的完整定義，請參閱 SPEC_13 §0。

---

## 本文定位

| OODP 層級 | 名稱 | 對應文件 | 職責 |
| :--- | :--- | :--- | :--- |
| **L1 物理層** | 傳輸載體 | **本文 §3** | TCP/UDP、libp2p、QUIC 封包傳輸 |
| **L2 定址層** | 節點定位 | **REAL_03** | CAID 格式、Kademlia XOR 路由、節點發現 |
| **L3 收斂層** | 基礎收斂 | **本文 §7** | 分散式 `&` 運算、衝突判定、基礎路由 |
| L4-L5 優化 | 譜幾何 | **APP_05** | LADD 引力路由、熱帶剪枝、視角管理 |

**關鍵區分**：
- 本文 (REAL_02) 處理「傳統」分散式系統問題：封包怎麼傳、節點怎麼找、基礎合併如何判定
- APP_05 (LADD) 處理「譜幾何」優化：如何用譜特徵加速路由、如何用引力場優化選擇

---

## 1. 創世自舉流程 (Genesis Bootstrapping) **[Core Requirement]**

為了解決語言初期的「冷啟動」問題，引擎必須實作嚴格的自舉流程。

1.  **初次啟動檢測**：當引擎啟動且本地不存在 `.oo/` 目錄時，觸發自舉。
2.  **種子解壓縮**：引擎將內建的官方標準庫（如 `~%List`, `~%Math`）寫入本地存儲。
3.  **創世 Commit 產生**：
    *   計算種子原始碼的 CAID。
    *   驗證 CAID 是否與引擎硬編碼的「官方種子 CAID」一致。
    *   產生宇宙的第一個 Commit $C_0$（創世 Commit）。
4.  **建立根宇宙 `_`**：將 `_` 指向 $C_0$，完成工作區初始化。

---

## 2. OODP 協定棧總覽

```
┌──────────────────────────────────────────────┐
│  L5 應用層: ~%Discovery./find (SPEC_13 §6)    │
│  L4 視角層: 權威格論、譜消融 (APP_05)          │
├──────────────────────────────────────────────┤
│  L3 收斂層: 分散式 & 運算、衝突判定 (本文 §7) │
├──────────────────────────────────────────────┤
│  L2 定址層: CAID、Kademlia (REAL_03)          │
├──────────────────────────────────────────────┤
│  L1 物理層: TCP/UDP/libp2p (本文 §3)          │
└──────────────────────────────────────────────┘
```

**本文涵蓋**：L1 傳輸、L3 基礎收斂，以及 L2-L3 的協定介面。

---

## 3. L1 物理層：傳輸載體 (The Carrier Layer)

`~%Discovery./fetch` 在語言層是抽象態射，實際的網路行為由引擎實作。

### 3.1 傳輸優先順序

```
1. 本地快取 (.oo/objects/)     ← 最快，無網路請求
2. 工作區已知節點              ← 團隊內部共享
3. 設定的 registry 節點        ← 類似 npm registry，但非強制中央
4. IPFS / Git HTTP             ← 公開後備來源
```

引擎按順序嘗試，第一個回應有效內容的來源獲勝。
來源不影響收斂結果——相同的 hash 無論從哪裡取得，內容必然相同。

### 3.2 OODP 基礎封包格式

**角色，不是指令名**：任何以 OODP 暴露投影與取物能力的程序都是發現／取物
節點。CLI 拼法是 `oo node serve`（REAL_01 §1.2 宇宙節點）；`serve-discovery`
不是獨立子命令。同一封包承載三種 `%op`；本引擎目前**實作** `#fetch` 與 `#advertise`（§4.2），
對 `#discover` 回傳明確的「本節點未實作」狀態（不得以靜默代答）。

**Request 結構**（n/ Cocoon 或等價 JSON；線上鍵名帶 `%`）：
```nlang
{{
    %op:   #discover | #fetch | #advertise | #find_node
    %hash:   "hash:sha256:v1:9f86d081..."  ;; 目標 CAID（#fetch 必填）
    %from:   @caid                         ;; 請求節點 ID（節點身分弧起攜帶）
    %ad:     @ServiceAdvertisement         ;; #advertise 必填（§4.2）
    %target: @caid | @str                  ;; #discover 為 CAID（§4.3）；
                                           ;; #find_node 為 40 hex 之節點 ID（§4.4）
}}
```

**`%from`【2026-07-27 更新,規範性】**:請求**必須**攜帶請求方之 `node_id`(§4.1)。**但它是主張,不是認證**——本版不簽名,任何對等點皆可填入任意值。因此**不得有任何判定依賴它**:服務、驗證與四向結果,無論 `%from` 為何皆須完全相同(與本節「對等點的 `%status` 是主張,不是驗證」同一律)。其用途為可觀測性,以及為 `#advertise`(宣告者)與 `#discover`(DHT 路由)鋪路。`#fetch` 本身不依賴「誰在問」:物件自驗證(REAL_03 §6.6)。

**【2026-07-28 補】** 上述「屆時另訂」於 `#advertise` 已定:該 op 的 `%from` **必須**與 `%ad.node_id` 相符,且身分由廣告自身的簽章認證(§4.2.1–§4.2.2)。**這不使 `%from` 在 `#fetch` 上變成認證**——`#fetch` 的服務與四向結果仍必須與 `%from` 無關。同一個欄位在一個 op 上被簽署、在另一個 op 上仍是主張,並無矛盾:承擔認證的從來不是欄位,是簽章。

**【2026-07-28 再補,`#discover` 定案】** 上文「`#discover` 仍待定」現已裁定:**`#discover` 上的 `%from` 與 `#fetch` 同,是主張而非認證**,**不得有任何判定依賴它**(§4.3.6)。理由:一次發現回答只是把**已簽署之公開記錄**再廣播一次,使答案取決於誰在問,買不到任何安全性,卻造出一個分區面(同一個問題對不同提問者給不同答案)。

**Response 結構**（JSON 物件；鍵名字面為 `%status` 等）：
```nlang
{
    %status: #success | #not_found | #conflict | #not_implemented | #rejected
    %result: _         ;; 僅 #success 時承載值；缺席不得視為成功
    %reason: #tag      ;; 每一個非 #success 皆必須攜帶（§3.2.1）
    %source: @str      ;; 回應節點的識別碼
    %hops:   @int      ;; 本記錄在抵達你之前被轉發過幾次；直答為 0（§4.3.4）
    %peers:  [@RelayedAdvert]   ;; 僅 #discover；可為空列表（§4.3.3）
}
```

**`#rejected` 與 `%reason`【2026-07-28 新設;2026-07-29 擴及全部非成功狀態,規範性】**:`#rejected` 意為「請求可理解,但本節點拒絕受理」,與 `#conflict`(格式錯或完整性裁決)、`#not_implemented`(不辦此 op)皆不同。其**判別理由必須**由 `%reason` 攜帶。

> **原條文寫「`%reason` 當且僅當 `%status` 為 `#rejected` 時出現」,已刪除。** 該限制與同段下一句自相矛盾:下一句主張「狀態集合維持小,**可分性住在 `%reason`**」,而「當且僅當 `#rejected`」正好使可分性**只能**住在狀態碼裡。本節在同一段裡主張一個修法又禁止它。**現行規則見 §3.2.1。**

理由不得收攏成單一標籤:一個以「四件事必須可分」為論旨的協定,若把四種拒絕收成一個 `#conflict`,即是 §1.2 `#timeout` 那個錯誤降一層重演。狀態集合維持小而跨 op 穩定,可分性住在 `%reason`——新增 op 不得為此膨脹共用狀態集。

#### 3.2.1 每一個非成功回應都必須說出為什麼 **[Core Requirement,2026-07-29 新設]**

> **軸的定義已移出本節(2026-08-09)**:「狀態／原因」之分與「狀態集不得增長、原因集得增長」的通則,規範性定義在 **[TAG_REGISTRY](./TAG_REGISTRY.md) §0**。本節保留的是該通則在**線上面**的具體形(`%status` 五者與 `%reason` 開放集)。此前本節與 **[SPEC_08](./SPEC_08_Meta_and_Runtime.md) §3.2.1** 各自獨立地定義了同一個二分。

*   **`%reason` 於 `%status` 非 `#success` 時必須出現(MUST)**,於 `#success` 時**不得**出現。
*   **`%status` 集合不得為此增長(MUST NOT)**:仍為 `#success` / `#not_found` / `#conflict` / `#not_implemented` / `#rejected` 五者。新增 op 或新增可分情況一律以 `%reason` 承載。
*   **本節點不服務之 op 回 `#not_implemented`(MUST)**,而非 `#conflict`——不論該 op 名是「未知」或「已知而未實作」。二者對提問者是同一件事:**換一台問**。而 `#conflict` 說的是另一件事:**修你自己的封包**。
*   **未知的 `%reason` 標籤不得使回應失效(MUST NOT)**:收方遇到不認得的理由,**必須**依 `%status` 行事並將該理由視為不透明字串。理由集得增長;狀態集不得。

**跨 op 之共用理由集**(此表為規範性;`#rejected` 另有 §4.2.2 之集合與 §4.3.5.1 之 `#entropy_unavailable`):

| `%status` | `%reason` | 意義 |
| :-- | :--- | :--- |
| `#not_implemented` | `#unknown_op` | 本節點不服務此 op |
| `#not_found` | `#not_held` | 明確的缺席 |
| `#not_found` | **`#standard_root_unavailable`** | **持有那些位元組,而接不回它所指名的標準根**(REAL_03 §6.8;2026-08-16 新增)。**狀態不變**:補救確實是換一台問,而本節上方逐字規定狀態集不得增長。**不得改回 `#not_held`**——那是一句關於自己持有什麼的假話 |
| `#conflict` | **`#caid_mismatch`** | **持有,而位址重算不符——本表上唯一的完整性裁決** |
| `#conflict` | `#malformed` | 封包不成形(含無 `%op`、空行、`%target` 形狀錯) |
| `#conflict` | `#missing_field` | op 可辨識,而必填欄缺席 |
| `#conflict` | `#unparseable_caid` | `%hash` / `%target` 存在但不是 CAID |
| `#rejected` | `#entropy_unavailable` | **收方取不到隨機性,故不作答**——說的是**收方做不到**,而非請求有問題(§4.3.5.1)。〔2026-08-16 更正〕原文作「本表上唯一一列」,新增 `#standard_root_unavailable` 後不再為真;兩者的分野見下方附註 |

> **本表現有兩列說的是「收方做不到」**,而它們刻意落在不同的 `%status` 上:
> `#entropy_unavailable` 是 `#rejected`(**我不作答**),`#standard_root_unavailable` 是
> `#not_found`(**我交不出來,去問別台**)。其餘各列說的都是「你的請求有問題」或「我沒有」。
>
> **`#standard_root_unavailable` 未登記於 [TAG_REGISTRY](./TAG_REGISTRY.md),這是刻意的**:
> 〔量 2026-08-16〕該檔今日只收了本表理由的一個**不一致子集**(`#request_too_large`、
> `#entropy_unavailable` 在,而 `#unparseable_caid`／`#not_held`／`#missing_field`／`#unknown_op`
> 不在)。逐筆補會讓下一個新理由再問一次同樣的問題;「登記簿到底收不收 OODP 的
> `%reason`」已進 `meta/WORK_QUEUE.md` Inbox 待裁。**在那之前,本表是這些理由唯一的家。**
>
> > **〔2026-08-23 更正，O71〕** 上段兩句都不再為真。`#standard_root_unavailable`
> > **已登記**於 `TAG_REGISTRY`，標為「原因·**線上／⊥**」（比照 `#caid_mismatch`）。
> > 判準：收不收一個 `%reason`，看它**在不在兩層都成立**，不看它從哪裡來。
> > 僅線上成立的理由（`#malformed` `#missing_field` `#not_held` `#unknown_op`
> > `#unparseable_caid`）家仍在本表；操作名與狀態不入簿（`TAG_REGISTRY` §0.4）。

> **`#caid_mismatch` 是這張表的重點,不是其中一列。** `#conflict` 此前同時表示「你的請求有問題」與「我這裡的位元組壞了」,而只有後者是關於**內容**的裁決。收方據此決定要不要在對方名下記一筆完整性事件——見 §3.2.2。**一個無法被佐證的指控不該寫進聲譽紀錄。**

#### 3.2.2 協定層的回答不是完整性裁決 **[Core Requirement,2026-07-29 新設]**

收方**必須**區分「對端關於**內容**的裁決」與「對端關於**這次交談**的回答」,且**只有前者得記入完整性紀錄**(REAL_03 §6.6)。

| 對端所言 | 客戶端 `%cause` | 記入完整性紀錄? |
| :-- | :--- | :--- |
| `#conflict` + `%reason: #caid_mismatch` | `#caid_mismatch` | **是** |
| `#conflict` + 其他理由,或**無**理由 | `#peer_refused` | 否 |
| `#not_implemented` | `#peer_not_implemented` | 否 |
| `#rejected` | `#peer_refused` | 否 |
| **本引擎不認得的 `%status`** | `#peer_unknown_status` | **否** |
| `#not_found` | `#missing_key` | 否 |

*   **未知狀態碼不得被當作損壞(MUST NOT)**。**一個比你新的節點不是一個被弄壞的節點。** 若違反此條,協定的**每一次**未來擴充都會使舊實作指控新實作損壞——而完整性紀錄正是操作者用來判斷誰在供應壞資料的東西,污染它即使其失去用途。此為 REAL_03 §6.6「裁決必須為真」在網路面的形。
*   **無理由之 `#conflict` 判為拒絕(MUST)**:舊實作不攜帶 `%reason`,而**無法佐證的指控不得寫入**。其代價明列於此並被接受:舊對等點**自身偵測到**的完整性問題,於收方讀作拒絕。代價有界——收方本就對其接受的每一個位元組重算位址(REAL_03 §6.6),失去的是對方的**報告**,不是自己的**檢查**。

| 對等點狀態 | `%status` | 客戶端可見 |
| :-- | :--- | :--- |
| 持有且位址驗證通過 | `#success` | 值（客戶端仍須重算 CAID） |
| 未持有 | `#not_found` | 缺席（`⊥ #missing_key`），**不是** conflict |
| 持有但本地位址驗證失敗 | `#conflict` | 具名完整性裁決（客戶端側為 `#caid_mismatch` 等） |
| 接受連線後無回應 / 逾時 | （無回應） | `⊥ #peer_timeout`，與上三者可分 |

**對等點的 `%status` 是主張，不是驗證**。即使對端宣稱 `#success`，客戶端仍
必須對 `%result` 重算內容位址；不符則為 `#caid_mismatch`（REAL_03 §6.6），
與對端說法無關。

**禁止以靜默代替狀態**：缺物、腐敗、未知 op、未實作 op 皆須有可區分的
`%status`（或明確逾時）；不得以 0-byte 回應讓客戶端把四種情況收成同一結果。

> **【2026-07-29 已結案】** 下述缺口由 §3.2.1 補上:未知 op 改回 `#not_implemented`(修法 a)**且**全部非成功狀態攜帶 `%reason`(修法 b)——**兩解皆採,因其各答一半**。實測七格全可分(值/損壞/缺席/未知 op/缺欄/格式錯/CAID 不可解)。本條自此**得**被引用。以下為原始記錄,保留其量測。
>
> **原記錄:本條目前未被滿足,且是本規格自身的缺口【2026-07-28,實測】**:上列四者只有三個碼——
> **腐敗與未知 op 同回 `#conflict`**(實測 v0.2.51:`%op: #find_node`〔當時未知〕、
> `%op: #unify`〔未實作〕與一行格式錯誤的請求,三者回應完全相同)。於是新客戶端對舊節點
> 發一個新 op,分不出「這台不辦這件事」與「我把封包寫壞了」——前者該換一台問,後者該修自己。
>
> 此為 v0.2.48 `#timeout` 判例同型而寬一個 op 集:**一條要求四者可分的 MUST,自己只給了三個碼**。
> 修法有二(**2026-07-29 裁定:兩者皆採**):(a) 未知 op 改回 `#not_implemented`,使該碼意為「本節點不辦此 op」
> 而不細分知不知道這個名字;(b) 於 `#conflict` 上比照 §4.2.2 引入 `%reason`。
> ~~**在裁定之前,本條不得被引用為已達成的保證。**~~(裁定已作出,見 §3.2.1)

**傳輸協定**：OODP 封包可跑在任何傳輸層上：Unix socket、TCP、HTTPS、或作為 libp2p 的擴展。預設讀取逾時由引擎約定（現行實作 10s）。

**舊式裸 CAID 請求:已移除,且它從未真正相容【2026-07-27 更正,規範性】**:信封之前的「裸 CAID 行」請求形式**已移除**;節點對其回以 `#conflict`,與任何格式錯誤之請求相同。

> **更正**:前一版本節將其列為「已申報且有期限的相容面」,並要求接受者**必須以本節之回應信封作答**。該要求使這個相容面**在構造上不可能相容**——舊客戶端送出裸 CAID 之後期待的是裸值,收到信封則解不開。實測(v0.2.47 客戶端 → v0.2.48 節點)得 `⊥ #caid_mismatch`,**與移除之後完全相同**。**一個以新格式作答的過渡通融,不是過渡通融。** 故其移除不使任何原本可行者失效。

**逾時之標籤【2026-07-27 補】**：對等點逾時收斂為 `⊥ #peer_timeout`，**不得**沿用 §1.2 之 `#timeout`。後者是**本地運算**超出 `%timeout`，其修復建議（優化性能、減少嵌套、放寬時限）對「對端不回話」而言指向錯誤的地方。四向可分之要求若在第五種情況上失守，本節即自我拆台。見 **[TAG_REGISTRY](./TAG_REGISTRY.md)** §1.3。

#### 3.2.3 收方必須在處理之前先設界 **[Core Requirement，2026-08-11 新設]**

一個服務中的節點在**未認證的**輸入上所做的第一件事，就是它對任何人開放的第一件事。
§3.4 的五道檢查、§4.2.6 的同意閘、乃至 `%ad` 本體的巢深上限，**全都跑在剖析之後**；
若剖析本身無界，那些檢查所保護的行程可能在抵達它們之前就已死亡或停擺。
**門內的鎖不能代替門本身的鎖。**

*   **請求必須有位元組上限（MUST）**：收方**必須**在**讀滿**單一請求之前對其設界，
    超限者以 `%status: #rejected` 與 `%reason: #request_too_large` 拒絕
    （TAG_REGISTRY §1.3）。**依大小拒絕，不依內容**——此時尚未剖析，
    故**不得**記入完整性紀錄（§3.2.2）。
*   **請求必須有形狀上限，且先於剖析（MUST）**：收方**必須**在剖析器遞迴**之前**
    拒絕巢狀過深的請求，理由為 `#stack_overflow`（TAG_REGISTRY §2.7.4）。
    「先跑再說」在此**不可行**：原生堆疊耗盡是行程中止，不是可攔截的失敗。
*   **兩者可分（MUST）**：`#request_too_large` 治**太大**，`#stack_overflow`
    治**太深**。一個 69 位元組的請求可以極深，一個 128 KiB 的請求可以極淺；
    以其中一條代替另一條，會使被擋下的那類看起來像另一類。
*   **拒絕不得以沉默表達（MUST NOT）**：超限請求**必須**得到具名回應，
    而非連線被靜默切斷——否則發送方無法分辨「我被拒絕了」與「這台掛了」，
    此為 §3.2「禁止以靜默代替狀態」在入口處的形。
*   **收方不得以請求的性質命名自己的無能為力（MUST NOT，2026-09-09 新增；裁定 D62）**：
    收方因**自身**狀態（資源被宿主拒絕、實作出錯）而答不出來時，
    `%reason` **必須**是 `#host_resource_denied` 或 `#internal_error`
    （**[TAG_REGISTRY](./TAG_REGISTRY.md)** §2.7.5），
    **不得**沿用任何描述**請求**的理由——`#stack_overflow`（太深）與
    `#request_too_large`（太大）皆為關於請求的斷言。
    > **這一款守的是上一款的理由。** 上一款禁止沉默，逐字是為了讓
    > 「**我被拒絕了**」與「**這台掛了**」可以分辨。**一個冒名的具名回應
    > 不會被讀成沉默，它會被讀成前者**——於是那台掛了的機器不但沒有被看見，
    > 還讓每一個跟它說話的人以為是自己的請求有問題。
    > **具名了，而名字把它放進錯的那一桶，比沉默更遠離這一款的目的。**
    > 且依本節，此類拒絕**不入完整性紀錄** ⟹ **一台不再服務的節點不留痕跡。**
    >
    > **論證性量測（2026-09-09，參考實作 `v0.45.0`）**：同一個 1 行 `#fetch`
    > 請求，只改**伺服器行程**的位址空間上限——1200／700 MB 得
    > `%status: #not_found`、`%reason: #not_held`；**500／300 MB 得
    > `%status: #rejected`、`%reason: #stack_overflow`**。
    > 請求一個位元組都沒有變。

> **論證性量測（2026-08-11，參考實作）**：在設界之前，一個 **69 位元組**、
> `%ad` 內含 2000 層括號的 `#advertise` 請求，使節點停擺逾 **90 秒**，
> 期間另一個合法客戶端**完全得不到服務**；同一入口上一個 8000 個 `!` 的請求
> 使**服務行程直接中止**。兩者皆為**形狀**攻擊而非大小攻擊——它們遠小於任何
> 合理的位元組上限。
> ⟹ 拿掉這個量測，「先於剖析」只是實作偏好；有了它，那是**單一封包即可
> 使節點停止服務**與否的分界。

### 3.3 效能優化建議 [Reference Recommendation]

*   **本地優先**：本地快取（`.oo/objects/`）的優先級必須極高，以確保離線觀測的流暢。
*   **預取機制 (Prefetching)**：針對 Staged 區或歷史鏈中的未知 CAID 實作非同步預取。

---

## 4. L2-L3 介面：節點發現與路由

### 4.1 Kademlia DHT 基礎路由 **[Core Requirement]**

L2 使用 **Kademlia XOR 路由**尋找物理節點。這是工程妥協——純粹的譜路由在離散 DHT 中難以實作。

**路由表結構【2026-07-28 落地並精確化】**：

*   **節點 ID** = `node_id` CAID 之**內容摘要前 160 bit**,即**前 20 個位元組**(原始位元組,非十六進位字元)。該 CAID 定址何物見 §4.1.1。
    > **欄位名更正**:原文作 `content_digest`,而該欄位名為 `digest`。此非文字瑕疵——宇宙決定性弧曾有一次比對讀了不存在的鍵而**兩側恆得 None**,於是空洞通過。**規格寫錯欄位名,會製造出綠色的空洞量測。**
*   **距離度量** = `XOR(a, b)`,以大端序整數比較。
*   **桶索引** *i* = `XOR(self, peer)` 的**前導零位元數**;等價地,與自身共享恰好 *i* 個前導位元者入桶 *i*。`i == 160` 意為該對等點**即是自己**,**不得**存入。
*   **每桶容量 k = 20**。
*   **160 個固定桶**。桶分裂(標準 Kademlia 二叉樹)為後續優化,本版不做:在可觸及的深度上它不改變任何可量測行為。

#### 4.1.1 節點身分 **[Core Requirement,2026-07-27 新設]**

前條說了如何由一個 CAID 導出節點 ID,卻從未說**那是什麼東西的 CAID**。本節補上。

*   **材質(MUST)**:節點身分是一組**金鑰對**;`node_id` 為其**公鑰**之 CAID,DHT 位址取其前 160 bit。**不得**由宇宙內容導出——**[SPEC_13](./SPEC_13_Ouroboros_Discovery_Protocol.md)** §4.1.2 義務 #1 要求同源宇宙在任何行程、任何工作區得到相同的根 CAID(實測:兩個全新工作區、同一份原始碼 ⟹ 同一個根 digest),而那正是節點位址**不能**具備的性質:所有服務同一宇宙的節點將佔據同一個槽位。**讓宇宙可聯邦的性質,恰好取消它作為節點位址的資格**——內容定址回答「是什麼」,節點位址回答「眾多持有者中的哪一個」。且 §4.2 之 `signature` 本就需要私鑰。
*   **與操作者身分之別(MUST NOT)**:節點金鑰**不得**與 **[REAL_01](./REAL_01_Ouroboros_Engineering.md)** §7.5 之操作者金鑰為同一把。二者回答不同問題:操作者金鑰回答**誰授權**(治理、`#refine` 簽署,屬人),節點金鑰回答**哪一台在服務**(屬工作區)。以操作者金鑰為節點金鑰,將使複製工作區等同複製操作者的簽署能力——正是 §7.5 存在的理由。
*   **住所與作用域(MUST)**:節點金鑰**不得**置於工作區儲存(`.oo/`)之內,理由同 §7.5.1(該樹是設計來被取用與複製的成品)。其**工作區絕對路徑為身分的一部分**:引擎**無法**區分「工作區被搬移」與「工作區被複製」——它只觀察到路徑改變——故令路徑參與身分,使**複製在結構上即為另一個節點**,無需偵測、無需啟發式、無需詢問操作者。代價是搬移工作區即更換節點身分,此為明知之取捨。

    **該路徑必須先正規化(MUST,2026-07-28 補)**:符號連結、`.`/`..` 與重複分隔符**必須**於導出身分前解析完畢。原文只寫「絕對路徑」,而**絕對不蘊涵正規**:未正規化的實作會使**同一個目錄的兩個名字成為兩個節點**——例如工作區搬家後在原處留下一條指向新址的符號連結,兩邊皆可用,卻各自取得身分並各自簽章。這恰好違反本條自己的理由:本條令路徑參與身分,是為了讓**複製**在結構上成為另一個節點;**兩個名字不是一份複本**,把它判成複本,與判不出複本是同一類錯誤的兩面。
*   **保管(MUST)**:適用 §7.5.3 全部條款(僅存 PKCS#8、自建立即擁有者專屬、既有目錄不得改權限、不得靜默取代、併發產生須恰有一個結果且各行程回報者即為被持久化者、**必須在語言層路徑邊界之內且該保護不得依賴路徑恰好含儲存目錄名之元件**)。
*   **惰性(SHOULD)**:於首次網路使用時產生即可;從未接觸網路的工作區不必先有節點身分。

#### 4.1.2 填充、驅逐與其誠實界限 **[Core Requirement,2026-07-28 新設]**

*   **只由已認證的廣告填充(MUST)**:僅在 §4.2.2 五道檢查**全數通過**之後插入。**不得**由 `%from` 或任何未簽署欄位學習節點。
    > 標準 Kademlia 從它收到的**每一則訊息**學習節點,因為它沒有更好的東西。本協定有簽署的廣告,故可以免費地更嚴格;而若照抄那個做法,`%from` 是未簽的主張(§3.2),等於把任意插入白送給任何人。
*   **重複出現即更新(MUST)**:已在表中者再次廣告只更新其記錄,**不佔用新槽位、不驅逐任何人、不改變其在既有者中的位置**。
*   **桶滿:偵守優先(MUST)**:桶已達 k 時,**新對等點不入表**,並記錄該次丟棄。**不得**於插入路徑進行網路 I/O——ping-and-replace 會使「受理一則廣告」變成可在陌生人指定時機向外撥號,那是為了一項本版不需要的精緻化而新開的放大向量。
*   **自己不入表(MUST)**。

**偵守優先買到什麼,以及沒買到什麼**:

`node_id = sha256(公鑰)`,而磨鑰匙是可平行、可預算的(成本模型見 **[SPEC_15](./SPEC_15_Anti_Patterns.md)** §7.1)。偵守優先使**事後**磨鑰匙一文不值:對一個已滿的桶,再多的算力買到零。

**但它同時是「事前磨」的攻擊面,且本規格必須照實說**:先到者填滿某桶之後,其後所有誠實節點於該桶**一律被拒**(實測:攻擊者先填滿桶 0,隨後 20 個誠實節點全數被拒)。標準 Kademlia 以**活性驅逐**(ping 最舊者,不回應才替換)緩解此事,而本版明文不做。

⟹ **偵守優先是一個競速條件,不是一項防禦**。它使攻擊者必須**早到**,而非僅僅有錢。任何把它讀成女巫防禦的敘述都是錯的:ORDER_00 §1.1 已論證框架內部供不出女巫抗性。

*   **本版不持久化(MUST NOT,2026-07-28)**:路由表與對等點目錄皆為**行程記憶體**。**不得**因本版之路由功能而於工作區產生任何耐久檔案。
    > **理由不是保守,是分界**。耐久的 OODP 狀態會一併帶來:回收(何時忘記一個對等點)、遷移(跨版本讀舊檔)、§5.1 的新檔條款,以及**一項安全性質的改變**——記憶體內的表在重啟時清空,故上述「先到者鎖死某桶」目前**止於重啟**;一旦落磁碟,那個意外的緩解就消失。**那是一次應當被裁定的變更,不是某次實作的順手產物。**

**與譜幾何的分層**：
- **L2 (Kademlia)**：解決「這台機器在哪裡」
- **L3+ (譜感知)**：解決「這個子空間由誰服務」

### 4.2 服務廣告基礎格式 **[Core Requirement,2026-07-28 落地]**

節點向網路宣告其承載的服務（L3 基礎版）：

```nlang
@ServiceAdvertisement: {{
    node_id:     @caid     ;; 公鑰之 CAID（§4.1.1）
    public_key:  @str      ;; 公鑰本體,hex【新設】
    services:    [@caid]   ;; 該節點宣稱可服務的子空間 CAID 列表
    listen_port: @int      ;; 宣告之監聽埠【新設】
    capacity:    @int      ;; 當前負載容量（相對值）
    ts:          @int      ;; Unix 秒【新設】
    ttl:         @int & ..15
    affiliation: ?@Affiliation  ;; 可選。操作者簽署之歸屬聲明（§4.2.8）
    signature:   @str      ;; 對本體之簽章,hex（§4.2.1）
}}
```

`affiliation` 為**可選**:其缺席合法,且不改變本節其餘一切。它位於簽章所涵蓋的本體之內,故節點亦簽署之。

#### 4.2.1 公鑰隨廣告同行,與簽章之所簽 **[Core Requirement]**

*   **公鑰必須內嵌(MUST)**:`node_id` 是**雜湊**,Ed25519 無從對雜湊驗簽,故廣告**必須**攜帶公鑰本體,收方重算其 CAID 與 `node_id` 比對。**不得**改以取物(`#fetch node_id`)代之:驗證廣告的目的正是為了得知對等點是誰、在哪裡,一條預設「對端已可達」的路線是自舉迴圈。

    > 此前 §7.2 義務 #1 要求「驗證 `signature` 與 `node_id` 對應的公鑰匹配」,而本節格式從未攜帶公鑰,亦無取得路徑(實測:節點對自身 `node_id` 之 `#fetch` 回 `#not_found`)。**該義務自 2026-05-14 寫下起即無法被滿足**;無法被滿足的檢查不是更嚴格的檢查,是沒有檢查。**[REAL_01](./REAL_01_Ouroboros_Engineering.md)** §7 的操作者面第一天就是這樣做的(`%authority` 攜帶公鑰本體而非其 CAID),線上面未跟上。

*   **簽章之所簽(MUST)**:令**本體**為本廣告**未加入 `signature` 欄位前**之值,則

    ```
    payload   = "oodp-advert:v1:" ++ CAID(本體)
    signature = Ed25519(節點私鑰, payload)
    ```

    收方**必須**移除且僅移除 `signature` 欄位後重算 `CAID(本體)`。域分隔前綴為**必須**,使廣告簽章不得被挪用為其他以值為對象之簽章(同 §7 操作者面 `refine:` 之分隔)。

*   **簽章承諾的是值,不是編碼**:因所簽為內容位址,對等點得重新編碼(JSON ↔ cocoon、任意欄位順序)而不破壞簽章。多數協定須靠正準序列化才有此性質,內容定址免費給出。惟**列表順序有義**(改變順序即改變 CAID),故收方**不得**在計算位址前對 `services` 排序、去重或正規化。

#### 4.2.2 收方驗證階梯 **[Core Requirement]**

順序具規範性:

| # | 檢查 | 失敗 |
| :-- | :--- | :--- |
| 1 | `%ad` 存在、為 combo、必填欄位齊備(含 `public_key` 與 `signature`) | `#rejected` `%reason: #malformed` |
| 2 | `CAID(public_key) == node_id` | `#rejected` `%reason: #identity_mismatch` |
| 3 | `%from == %ad.node_id` | `#rejected` `%reason: #identity_mismatch` |
| 4 | 簽章以 `public_key` 驗證 §4.2.1 之 payload | `#rejected` `%reason: #bad_signature` |
| 5 | `|now − ts| ≤ 60s` | `#rejected` `%reason: #stale` |
| — | 以上皆過 | `#success` |

第 2 步**必須**先於第 4 步,且其失敗**必須**具名為 `#identity_mismatch`:只問「簽章對不對得上封包裡的公鑰」的引擎會接受每一份偽造,因為偽造者兩樣都供給。第 3 步同理——一個對「誰在說話」給出兩種說法的封包,不得由收方靜默擇一。

#### 4.2.3 廣告本體是數據,不是程式 **[Core Requirement]**

引擎**不得**對廣告本體求值。本體是數據:僅字面量原子、列表與字面量所組成之 combo;其餘一切(態射、應用、路徑、算術、插值……)**必須**判為 `#malformed`,且該判定**必須**先於任何求值。判別**應以白名單為之**,使日後新增之語法形在有人明確決定它得上線之前預設被拒;遞迴**必須**有深度上限。

> **承重量測(2026-07-27)**:實作曾以求值本體為受理廣告的**第一個動作**,早於本節五道檢查。一個未帶簽章、未帶金鑰、未帶身分的封包令節點在攻擊者指定路徑寫下攻擊者指定內容,而後才回以 `#rejected #malformed`——**裁決是對的,效果已經發生了**。此為 **[SPEC_08](./SPEC_08_Meta_and_Runtime.md)** §6.1.2「授權須於施加特權效果的當下出示」在線上的對應面:此處根本沒有授權,效果照樣施加。

#### 4.2.4 未被驗證者 **[Core Requirement]**

`services` 與 `capacity` 是**主張**。收方無從知道對端持有什麼,故廣告一個宣告者並不持有的 CAID **必須**受理(`#success`);拒絕它等於宣稱自己知道一件無從知道的事。謊報的代價由後續 `#fetch` 支付——那裡取回的物件自我認證(REAL_03 §6.6),或取不回。簽章不使清單為真,它使說謊者可歸責。

此即 §6.1「Hash 保證完整性,不保證可信度」在廣告上的分工:**公鑰是自我認證的物件**(誰遞給你的不相干,雜湊得到 `node_id` 它就是那把鑰匙),**服務清單是斷言**(可認證其出處,無從驗證其內容)。

#### 4.2.5 位址:主機觀察,埠宣告 **[Core Requirement]**

廣告**不攜帶主機**。收方**必須**以承載該廣告之連線的來源主機為對等點主機,並以廣告內**經簽署**的 `listen_port` 為其埠——監聽埠無從觀察(來源埠是臨時埠,不是它),主機則無從偽造成另一台機器。

於是**一份廣告只能描述你已經在跟他講話的那一台**:一個有效簽章永遠無法指名第三方,反射與放大向量**在結構上**關閉,不靠檢查。代價是 NAT 之後的節點宣告不出可達位址——**那是事實,不得粉飾**。

直接 `#advertise` 受理所記錄的來源性為 **`direct`**。這是收方本地的觀測資料,不是廣告本體、簽章或宇宙值的一部分;其唯一依據是主機來自**承載該廣告之連線**。收方**不得**由 `%hops`、非空的主機或位址、`received_at`、或「曾與中繼者建立 TCP 連線」推導 `direct`。

#### 4.2.6 對等點目錄之地位 **[Core Requirement,2026-07-28 改寫]**

受理之廣告存於引擎本地的**對等點目錄**。其地位逐條如下:

*   **得**由 `#discover` 搜尋並轉發(§4.3)。
*   **與取物來源集合分離(MUST)**:對等點目錄與引擎實際用來取物的來源集合是兩個不同的集合。持有某對等點之廣告,**不使** `~%Discovery./fetch`/`./find` 去連它。**理由**:讓本地取物去撥號給一台只是「有人跟你提過」的主機,是一個同意權問題,不是一個路由優化;它應當被單獨裁定,而非作為發現的副作用發生。

    > **【2026-07-31 修訂】** 本條原作「取物的來源集合**只**由操作者顯式建立(`./connect`)」。該裁定被 §4.2.6.2 收窄:來源集合現有**兩個類別**——操作者逐次建立的**手動**類別,與依 §4.2.6.2 之資格自動成立的類別。**分離本身不變**,改變的是「什麼算操作者的同意」:自動類別的同意由**歸屬信任根**表達,而根是操作者帶外寫下的。上句的「只」若不改,規格會在它自己新設的機制上自相矛盾。
*   **來源性不是准入(MUST NOT)**:`direct`、`relayed` 與 `unknown` 只描述收方如何見到某一筆廣告;它們不是信任、同意或自動准入的裁決,也不得單獨把目錄記錄變成取物來源。

    > **該句自 2026-07-28 寫下起,直到 2026-07-30 都只是一個意圖 [誠實記帳]**:`./connect` 當時是一個僅帶 `#io` 的語言層 builtin,**任何程式**——包含一支自對等點取來並求值者——皆得加入遠端來源,而引擎隨後就會去撥號(實測:無 `--grant`、無 `--privileged`,`⊥ #peer_timeout` 於 5.05 秒後返回,該秒數即撥號發生之證據)。當時唯一的圍堵是**來源集只活在一個行程裡**,而那是**意外的**,不是任何人的裁定。§4.2.6.1 使該句成為機制。

#### 4.2.6.1 撥號需要同意 **[Core Requirement,2026-07-30 新設]**

*   **遠端形式須出示能力(MUST)**:以遠端位址(`tcp://…`)建立取物來源,**必須**出示 **[SPEC_08](./SPEC_08_Meta_and_Runtime.md)** §6.1.4 能力格之對應能力(軸一第三類);未出示者判 `#privileged_required`,且診斷**必須指名所缺之能力**(§6.1.4「出示某個能力不等於出示該能力」)。
*   **閘先於效果(MUST)**:拒絕**必須**發生在任何連線嘗試**之前**。回傳 `⊥` 而封包已經送出,等於把對方想要的東西(一個封包、一個查詢)照給不誤——此為 SPEC_08 §6.1.2「授權須於施加特權效果的當下出示」在本層之對應面。**判定法**:對一個丟棄封包的位址,被拒絕之呼叫其耗時**必須**與完全沒有來源時同量級,而非與一次連線逾時同量級。
*   **本地形式不設閘(MUST NOT)**:以本地物件庫路徑建立之來源**不**需要該能力。**理由不是便利**:本地庫**不撥號給任何人,也不告訴任何人你在找什麼**,故本節所治的兩項成本皆不適用;其路徑之越界另由 SPEC_08 §6.3 無條件治理。
*   **不留存(MUST NOT)**:該能力逐次呼叫出示,**不得**被記入 `.oo/` 或任何處以供下次沿用(§6.1.2 無後門;REAL_01 §7.0.1「逐次出示強於長期令牌」)。

> **本節所治者不是完整性。** **[SPEC_13](./SPEC_13_Ouroboros_Discovery_Protocol.md)** §6.1.1 與 **[REAL_03](./REAL_03_CAID_Protocol.md)** §6.6 已使每個來源皆為讀路徑、位元組取回後重算位址比對、驗證不過者跳過而掃描繼續——**一個惡意來源交不出錯的答案**。一個來源真正取得的是兩件:**花你時間的權利**(實測:每個丟包成員每次取物 **5.05 秒**,循序,且 §6.1.1 禁止提早放棄,3 個成員 15.09 秒;連線被拒者僅 0.06 秒故不顯現此成本)與**知道你在找什麼**。前者可設上限且沉默可偵測,**後者無上限亦不可撤回**。
>
> 故本節的軸線是**誰得以花你的時間、誰得以知道你在找什麼**——這正是上一條把它叫作**同意權**而非信任的理由:你不需要信任來源的正確性(驗證會處理),你需要同意的是**付錢**與**告知**。
*   **不得**進入宇宙(**[SPEC_13](./SPEC_13_Ouroboros_Discovery_Protocol.md)** §4.1.2 義務 #3)。判定法:兩個工作區演化同一份原始碼,一個從未接觸網路、一個受理廣告並服務發現,其**根 CAID 必須相等**。
*   **不得**因收到未經請求之封包而寫入物件儲存。

#### 4.2.6.2 自動准入與其上限 **[Core Requirement,2026-07-31 新設]**

§4.2.6.1 使「撥號需要同意」成為機制,但它把同意的粒度定在**每一台機器一次**。一位操作者的十台機器,於是要十次人類決策——而 §4.2.8 的歸屬聲明恰好回答了「這幾台是同一個人的」。本節把兩者接起來:**歸屬不是偏好,是同意的聚合單位**。人類決策從「每台一次」變成「每個操作者一次」,而那正是 §4.2.8 存在的理由。

*   **資格(MUST,全部成立方得准入)**:一筆目錄記錄得成為**自動**取物來源,當且僅當

    1. 該廣告仍通過 §4.2.2 之完整階梯(簽章、身分、字面、`ttl` 範圍、新鮮度);
    2. 該**確切廣告**之觀測來源性為 `direct`(§4.2.5 之記錄規則、§4.3.4 之三值定義);
    3. 其**直接觀測到的主機**與**簽章內之 `listen_port`** 構成來源位址;
    4. 其 §4.2.8 之歸屬聲明**於此刻**驗得過且未過期;
    5. 其導出之操作者金鑰位於本工作區已載入之歸屬信任根之中;
    6. 同一 `node_id` 之較新或不同廣告**各依其自身**的來源性與聲明重新裁定——**一次舊的直接觀測不得為新廣告背書**。

    **必須綁定確切廣告,不得只看 `node_id` 或只看最後一行耐久記錄。** 否則「這台我見過」會沿著一個 `node_id` 無限期繼承下去,而那正是第 6 條要擋的。

*   **准入不撥號(MUST)**:受理或載入一筆合格記錄**不得**觸發任何連線。第一次撥號仍是既有的取物掃描。**理由**:若准入自己撥號,則「同意」與「撥號」之間再無時間差,§4.2.6.1 的閘先於效果在本層即失效。

*   **位址的界限(MUST)**:主機由**收方觀測**,埠由**廣告者簽署**。廣告者因此**只能**把你指向**他自己**的某個埠,不能指向第三方。**實測(2026-07-31)**:`observed_host` 由受理路徑寫入,廣告本體對它沒有輸入。此界限是本節唯一阻止「廣告即轉址」的東西,故它是規範性的,不是實作細節。

*   **上限(MUST)**:自動來源之數量**必須**有一個明確上限,且**必須**與手動 `./connect` 來源、本地物件庫分開計數。溢位時**保留在席者**,**不得**以容量或任何被廣告的數字驅逐——`capacity` 是不可驗證的主張(§4.2.4),讓它決定驅逐等於把說謊變成有利可圖。

    > **上限之導出**:§4.2.6.1 實測每個沉默來源每次取物耗 **5.05 秒**且循序,而 SPEC_13 §6.1.1 禁止提早放棄。上限 $n$ 因此把最壞情況定在 $n \times 5.05$ 秒。本版取 **3**(約 15 秒),理由是它**來自那個量測**,而不是沿用發現的 8 或 Kademlia 的 $K=20$——**三個不同的問題不該共用一個數字**。

*   **`ttl: 0` 不禁止取物(MUST NOT 誤讀)**:§4.2.7 之 `ttl: 0` 意為「不要**轉發**我」。它**不**意味「不要向我**取物**」:轉發是把別人的記錄交給第三方,取物是收方自己去拿他被告知的東西——**兩個不同的問題,不得由一個欄位同時回答**。故 `ttl: 0` 之直接、有根廣告**仍得**被自動准入。〔2026-07-31 裁定。此前引擎已如此行事而規格未言,本條使其成為裁定而非巧合。〕

*   **失格即除名(MUST)**:聲明過期、根被移除、記錄被較新的 `relayed` 或 `unknown` 廣告取代、或工作區被複製而觀測者半邊被清除——任一發生時,對應之自動來源**必須**消失,**不得**留下一個過期的席位。判定法:一份於受理時有效、於下一次取物時已過期的聲明,其來源**不得**再被撥號。

*   **自動類別不持久(SHOULD)**:自動來源集為行程本地,於開啟工作區時由合格之耐久記錄重建。**本版不規範重建的順序**,而在上限會被觸及的部署中,「保留在席者」跨重啟即**不是**一個定義好的性質——見 §4.2.6.3 之待決。

#### 4.2.6.3 跨重啟的「在席」 **[Core Requirement,2026-07-31 新設;同日結案]**

> **本節經歷兩次更正,兩次都留在文內 [誠實記帳]。** 初稿寫作「重啟後哪三筆先被走訪取決於實作的目錄容器,故同一份磁碟狀態兩次啟動**可以**得到不同的三個」——**實測推翻**:重建是決定性的,五次重啟得到完全相同的三個。誤述來自讀碼未量測。第二稿把缺陷改記為「確定成了錯的東西」並列為待決;本稿結案。

上限之溢位規則是「保留在席者」,而在席順序在單一行程內由准入次序給出。**重建不是准入**——重啟後自動來源由耐久記錄重排而成,故「在席」跨重啟的意義,**完全**由 §5.1.2 之**重建序**給出。

*   **不另立順序(MUST)**:§4.2.6.2 之自動來源席位**必須**依 §5.1.2 之收受順序判定,**不得**自訂第二個順序。一份耐久目錄只有一個「誰先到」。
*   **不採抽樣(MUST NOT)**:§4.3.5.1 對「一個上限說了至多幾個卻沒說是哪幾個」的裁定是**宣告抽樣**。**該解不移轉至此**,因為代價不同量級:發現的答案不是裁決(§4.3.2),重抽只花延遲;而自動來源花的是操作者的**時間**與**「你在找什麼」**(§4.2.6.1)。每次重啟重抽會使誠實來源無故失席,並把攻擊者的機會由**零次**變成**每次開機一次**。

    > 兩節同形而異解,正是「同一個形狀不蘊涵同一個答案」的實例。判別問的仍是那句:**若這件事是假的,誰付出代價、付出什麼**(REAL_01 §7.6)。

**與 LADD 的區分**：
- 本文 (L3 基礎)：簡單的服務列表廣告
- APP_05 (L4 優化)：譜幾何廣告 (GBB)，包含質量、譜指紋、引力場

#### 4.2.7 `ttl` 的語義,與它為何沒有單位 **[Core Requirement,2026-07-28 新設]**

`ttl` **不是時長**。它是一個**格論量**:**[APP_05](./APP_05_LADD_Global_Logic_Lattice.md)** §4.1 的第一級路由過濾是
$MASA_{overlap} = MASA_Q \sqcap MASA_{N_i}$——**每一跳都是一個 meet**,而 meet 沿格序下降
($A \sqcap B \sqsubseteq A$);APP_05 §3.1 的質量 $m(C)=\mathrm{Tr}(P_C)$ 是整數(投影的秩)。
一個**整數、隨 meet 下降、自然有上界**的量,正是 `@int & ..15` 的形狀。

> **它沒有單位,是因為格論的秩本來就沒有秒或跳這種單位。而且沒有任何節點遞減它——數學在遞減它。**

此設計的關鍵性質是:因為每個節點各自從**內容定址資料重算**它,原始的 `ttl` 是**自我認證的**——
**[SPEC_13](./SPEC_13_Ouroboros_Discovery_Protocol.md)** §6.1.1 意義下的**度數 0** 量,不需要信任任何中繼者。

**但本層算不出它**,理由見 §4.3.7。故本版對 `ttl` 的規範限於它撐得住的部分:

*   **範圍(MUST)**:`0..=15`。逾範圍者於 §4.2.2 之階梯判為 `#malformed`。
*   **`ttl: 0` 意為「不要轉發我」**(MUST)。此值在兩種讀法下皆有定義(秩預算已無可 meet;轉發深度為零),故可安全實作。
*   **不得修改(MUST NOT)**:`ttl` 在簽章之內,任何節點遞減它都會毀掉簽章。**跳數計數器不能住在簽章裡;跳數預算可以。**
*   **不得與 `%hops` 相比(MUST NOT)**:見 §4.3.4。

#### 4.2.8 歸屬聲明 **[Core Requirement,2026-07-30 新設]**

§4.1.1 給每個工作區一把自己的節點金鑰,§4.1.1 之附註又**禁止**該金鑰與操作者金鑰為同一把。二者合起來的後果是:**一位操作者的數台機器彼此是陌生人**,而操作者既無從說「這幾台是我的」,也無從收回。

**歸屬聲明**是操作者對「某節點屬於我」所簽的一句話。它住在廣告本體之內,故節點自己也簽了它。

```nlang
affiliation: {{              ;; 可選欄位,位於 @ServiceAdvertisement 之內
    operator_key: @str       ;; 操作者公鑰,hex
    signature:    @str       ;; hex
    expires:      @int       ;; Unix 秒
}}
```

*   **所簽之 payload(MUST)**:

    ```
    payload = "oodp-affiliation:v1:" ++ node_id ++ ":" ++ expires
    ```

    此為本規格第三個簽章域(另二為 §4.2.1 之 `oodp-advert:v1:` 與 §7 操作者面之 `refine:`)。**兩個被綁進 payload 的東西都是承重的**:少了 `node_id`,一份真實的聲明可被任何節點穿戴;少了 `expires`,持有者自行續期。域前綴帶 `:v1:`,因為 `refine:` 未帶版本是不重犯的缺陷。

*   **最長效期(MUST)**:`expires - now` **不得**逾 **30 日**;逾者不予採信。**沒有上限則本節的撤銷模型什麼也沒買到**——操作者大可簽一份百年聲明並稱之為短命。

*   **撤銷即到期(MUST NOT 另設撤銷清單)**:OODP 除廣告自身的 `ts`/`ttl` 外沒有任何分發通道,而憑空生一個撤銷通道所引入的失效模式比它治的還多。撤銷因此**由短效期加續簽承擔**。此非將就:公開網路上**有**撤銷通道的一方也在往這裡走——CA/Browser Forum 逐年壓低憑證上限(825 → 398 日且續降),理由正是撤銷在實務上不可靠(CRL/OCSP 常態 fail-soft)。ORDER_01 §7.4 對委任所取的「除非顯式標註否則自動屆期」是同一個直覺,低一層。

*   **只能加,不能減(MUST)**:歸屬聲明是**斷言**(SPEC_13 §6.1.1 意義下的度數 ≥1),它**不得**使任何本會被拒者被接受,亦**不得**使任何本會被受理者被拒。**故它不在 §4.2.2 的驗證階梯之內,也不新增任何 `%reason`**:一份驗不過的聲明只是「歸屬未經證實」,而不是廣告有問題——節點自己的簽章仍然有效,**它是誰從未有疑問**。與 §4.2.4「未被驗證者」同一條紀律,施於一個新欄位。

*   **三條路徑,全有或全無(MUST)**:一份聲明有三種抵達方式,而**只驗其中一部分,等於沒驗**:

    | 抵達方式 | 何時判 |
    | :-- | :--- |
    | 直接 `#advertise` | 受理當下 |
    | 隨 `#discover` 逐字轉發(§4.3.3) | 收方自行重判,**不採信中繼者的裁決**(§3.2.2) |
    | 自耐久對等目錄載入(§5.1.2) | 每次載入重判 |

    第三條**不是形式**:`expires` 是對**當下**的判斷,而一份受理時有效的聲明可以在檔案躺在磁碟上的期間死去。**裁決不得被持久化**;它是衍生物,依 §5.1.2 之原則於載入時重算。實作若把裁決存下再讀回,便是在重播一個過期的答案。

*   **鑄造需要操作者私鑰,服務不需要(MUST)**:聲明是一次鑄成、隨廣告出貨的靜態物件。**這是本設計唯一的安全支點**:若服務時需要操作者私鑰,則一台有歸屬的機器就是一台握有操作者簽署能力的機器,而複製工作區即複製該能力——正是 §4.1.1 附註存在的理由。私鑰之保管與位置依 **[REAL_01](./REAL_01_Ouroboros_Engineering.md)** §7.5;聲明本身**只含公開材料**,但仍**不得**置於 `.oo/` 之內,理由同 §7.5.1:那棵樹是為了被複製而存在的。

*   **它買不到女巫抗性(說明,但規範性地不得被讀成別的)**:攻擊者**不簽就是了**。本機制只約束**選擇加入**的一方,故它買到的是誠實操作者自願揭露所帶來的**女巫可辨識性**,不是抗性。**[REAL_01](./REAL_01_Ouroboros_Engineering.md)** §7.6 第 5 列(女巫抗性須外部物理錨點)**不因本節而被回答**,第 6 列才是本節。

#### 4.2.8.1 歸屬信任根 **[Core Requirement,2026-07-31 新設]**

**歸屬信任根(affiliation root)** 是收方的一句帶外斷言:「由這把操作者公鑰簽出的有效歸屬聲明,**得**在另經裁定的准入規則中被讀成我的同意。」它不使聲明變真——簽章與效期仍依 §4.2.8 重判——而是說**若它為真,誰的話對這一個節點算數**。

*   **作用域為工作區(MUST)**:信任根置於 `<workspace>/.oo/discovery.n`,不得置於 `~/.oo/`。一個工作區就是一個節點(§4.1.1);若信任根住在 HOME,於 A 節點作的一次同意會靜默授權同一 Unix 帳號下的 B、C、D 節點,那是**同意作用域外溢**。

*   **唯一閉合形狀(MUST)**:

    ```nlang
    affiliation_roots: [
        "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
    ]
    ```

    檔案**恰有**此一頂層欄位,其值**恰為**字串列表;每一字串恰為 **64 個小寫十六進位字元**之 Ed25519 公鑰。集合語義去重;正準寫回按字典序、每行一把。實作得借用 n/ parser,但**只能讀 AST 資料,不得求值**:態射、路徑展開、插值、呼叫、tuple、引號欄名及未知欄位皆不是另一種合法拼法。

*   **缺席、空集合、讀不懂三者有別(MUST)**:`discovery.n` 缺席與 `affiliation_roots: []` 均為有效空集合;檔案一旦存在,空白、不可讀、不可解析、非正準金鑰、未知或重複欄位、解析後非一般檔案皆**必須**使開啟工作區的操作具名失敗。錯誤須同時指出 `discovery.n` 與原因,不得沿用 `.oo/architects.json` 將所有錯誤吞成空集合之行為。列出一個缺席的集合不得順手造檔。

*   **管理面為帶外面(MUST)**:新增、移除、列出信任根由工作區操作者直接管理斷言層;它不是 n/ 程式可自授的能力,故不得新增語言態射,亦不需要 **[REAL_01](./REAL_01_Ouroboros_Engineering.md)** §7.0.1 的程式能力授予。管理動作不得鑄造操作者或節點身分、不得寫物件層、不得改 `.oo/format`、不得建立對等目錄。參考實作之面為 `oo node trust list|add|remove`。

*   **根不是准入本身(MUST)**:只有根、而沒有另行驗證為真且未過期的 §4.2.8 聲明時,不得新增來源、撥號、建立對等點、改變路由或排序。即使二者兼具,自動准入仍須另有**總量上限與准入/驅逐裁定**;不得從本節單獨推導。§4.2.8「只能加不能減」亦不變:根的有無不得改變廣告本身是否有效。

*   **它只回答歸屬問題(MUST NOT 借名)**:公鑰長得相同不使權威問題相同。下列四份斷言不得合併,亦不得再以「第幾份名單」計數——**問題才是不變量**:

    | 斷言 | 回答的問題 |
    | :-- | :-- |
    | REAL_01 §7.2 `~/.oo/authorized_keys` | 誰可簽發尚未實作之服務面特權令牌? |
    | `.oo/architects.json` | 誰可為此宇宙簽署 `#refine`? |
    | §6.2 套件根信任清單 | 誰可授權套件黑名單更新? |
    | `.oo/discovery.n` `affiliation_roots` | 誰的歸屬聲明得構成本節點之准入同意? |

    歸屬信任根尤其**不授予**治理權、套件權、服務令牌發放權、語言能力或度數 0 正確性偏好。其代價是 REAL_01 §7.6 所稱**錯置的同意**,只由作出該帶外選擇的收方承擔。

### 4.3 `#discover`:服務索引 **[Core Requirement,2026-07-28 新設]**

`#advertise`(§4.2)使節點說得出「我在哪、我承載什麼」。`#discover` 使**別的節點問得出**「誰承載這個」。

#### 4.3.1 請求

```nlang
{{ %op: #discover, %from: @caid, %target: @caid }}
```

`%target` **必填**;缺席或不可解析 ⟹ `#conflict`。`%from` 為主張(§3.2)。

#### 4.3.2 回應方搜尋什麼

**僅搜尋對等點目錄**(§4.2.6),取其**經簽署**之 `services` 列表中與 `%target` **完全字串相符**者。

*   **不得**查詢自身物件儲存。持有該物件卻從未向你廣告過的節點,在此問不出來——那是 `#fetch` 的問題,直接問它即可。此限制**必須**照實記錄,不得以查儲存粉飾。
*   **不得**在比對前對 `services` 排序、去重或正規化(§4.2.1:列表順序在位址之內)。
*   排除**必須**先於封頂(§4.3.5):`ttl == 0` 者、以及超出收方陳舊上限(§4.3.6)者。

命中之意義**僅為**「有人聲稱服務此物」(§4.2.4)。不多於此。

#### 4.3.3 回應與**逐字轉發**

```nlang
{{ %status: #success, %source: @caid, %hops: 1,
   %peers: [ {{ %ad: @str, %observed_host: @str }} ] }}
```

*   **`%ad` 是字串,承載廣告之 n/ 原始碼,逐位元組如其抵達時**(MUST)。回應信封為 JSON,n/ combo 無法以結構嵌入其中;改攜原始文本,使「逐字」**字面可查**,而非某個重新編碼器的性質。§4.2.1 已言簽章承諾的是值而非編碼,故重新編碼**仍會**驗過——但那也意味著**沒有人分辨得出忠實的轉發與有損的轉發**。故:**不得**重新編碼,不得增刪、重排或改寫任何欄位,`ttl` 尤然。
*   **`%observed_host` 是 `%ad` 的兄弟,不是它的欄位**(MUST);`%hops` 在信封中,在每個 `%ad` 之外。**簽章不覆蓋的東西,不得坐在簽章覆蓋的東西裡面**——信任邊界必須看得見於封包的形狀,而非藏在散文裡。
*   **經 `#discover` 得到的廣告,其收方來源性一律為 `relayed`**(MUST),包括信封宣稱 `%hops: 0`、`%observed_host` 非空、以及收方雖然與中繼者建立了 TCP 連線的情形。該連線承載的是中繼者的回應,不是原廣告所宣稱的主機觀察;`%hops` 只能描述信封,不能把轉發改說成直接看見。
*   **空結果為 `#success` 配 `%peers: []`,不得為 `#not_found`**(MUST)。「我不知道有誰」是一個答案;`#not_found` 會把它與「我沒有索引」壓成同一個值,而客戶端據此無法決定該改問別人還是別再問這台。

#### 4.3.4 一筆被轉發的記錄,其信任沿記錄內部裂開 **[Core Requirement]**

§4.2.5 以**結構**買到反射與放大的關閉:「一個有效簽章永遠無法指名第三方」。
而 **`#discover` 就是指名第三方那個操作**。它按構造重新打開 §4.2.5 關上的向量,
且此協定的任何安排都無法再以結構關上它。存活下來的,是**每一筆記錄內部的裂縫**:

| 欄位 | 地位 |
| :--- | :--- |
| `node_id`, `public_key`, `services`, `listen_port`, `ts`, `ttl` | **自我認證**——在簽章之內。誰遞給你的不相干。 |
| `%observed_host` | **斷言**——中繼者的**觀察**。永遠無法被簽:§4.2.5 的要旨正是主機出於觀察而非宣告,而**觀察無法旅行**。 |
| `%hops` | **斷言**——中繼者自己的**計數**。 |

⟹ 一筆被轉發的記錄是**一個自我認證的物件,裹在斷言的指標裡**。

**收方來源性的三值**:來源性是接收端的觀測 metadata,綁在**同一筆逐字 `%ad` 所表的簽章廣告記錄**上,而不是只綁在 `node_id` 上:

*   **`direct`**:收方直接受理有效 `#advertise`,主機來自承載該廣告的連線(§4.2.5)。
*   **`relayed`**:收方透過 `#discover` 得到該廣告,無論 `%hops` 宣稱為 `0` 或其他值。
*   **`unknown`**:來源性缺失、無法辨識,或由不攜帶觀測半邊的舊記錄載入;它**不得**被提升為 `direct`。

同一筆 exact signed advertisement 的來源性合併優先序為 **`direct > relayed > unknown`**:較弱的後來觀測不得覆蓋較強的既有觀測。**不同簽章廣告即使有同一 `node_id`,也各自以自己的來源性開始;不得從舊廣告繼承 `direct`。**這個 metadata 不改廣告本體、簽章、CAID 或 `%discover` 線上回應,也不構成信任、同意或准入裁決。

**`%hops` 的規範**:直答為 `0`,經索引轉發一次為 `1`(§3.2)。它是**可觀測性,不是閘**:

*   **不得**以 `%hops` 與 `ttl` 相比作為檢查(MUST NOT)。兩者是**不同的量**——一個是轉發計數,一個是秩預算——恰好共用一個範圍;以其一閘另一,**看起來像防禦,實為範疇錯誤**。
*   且 `%hops` **不可驗證**:說謊的中繼者永遠報 `0`。任何以它為據的界只約束誠實的一方。收方的保護是**它自己的**預算(§4.3.5)與陳舊上限(§4.3.6),二者皆為本地,且**不得**被宣稱為多於此。

#### 4.3.5 放大:由結構關閉改為由預算關閉 **[Core Requirement]**

§4.2.5 以構造關上了放大器。一個單行請求換回一個長列表就是放大器,故此處**必須**改以明確的界關上:

*   `%peers` 每則回應至多 **8** 筆(排除之後施加);
*   回應本體至多 **64 KiB**;若封頂後之筆數仍超出,**必須**再減。

**且此界必須由雙方各自持守**(MUST,2026-07-28 驗收所加)。只做在回應方的界保護的是**別的節點不被你放大**,對**你的節點被別人放大**毫無作用:實測一個從不停頓的對端送出 67.1 MB,收方全數緩衝、峰值 143 MB、解析出 8156 筆——而讀取逾時**從未觸發**,因為逾時針對的是**停頓**,不是**總量**。

故收方**必須**對 `#discover` 回應施加同一個 64 KiB 上限,且逾限**必須**具名(建議 `#oversize`)而**不得**把截斷的前綴當作一個短答案處理。

> 此非新政策,是同一條界由**真正會受傷的那一側**持守。(`#fetch` 的回應不在此列:物件無規定之大小上限,對其封頂需要另行裁定。)

#### 4.3.5.1 哪八個:溢出時的選擇 **[Core Requirement,2026-07-30 新設]**

§4.3.5 定了**至多 8 筆**,卻從未說是**哪** 8 筆。這一節補上。

*   **未溢出時,全數回傳(MUST)**:存活候選不多於上限時,**全部**回傳,且每次皆同。選擇只發生在溢出處。
*   **溢出時,每次查詢**獨立**抽一次均勻不放回樣本(MUST)**。
*   **不得加權(MUST NOT)**:特別是 `capacity`——§4.2.4 已言它是**無從驗證的主張**,以它加權等於把說謊的誘因從排序搬進抽樣。
*   **不得依提問者(MUST NOT)**:§3.2 之 2026-07-28 裁定已定 `#discover` 的 `%from` 為主張且**不得有任何判定依賴它**。此處重申,因為「依提問者鍵控」正是實作抽樣時最容易滑進去的形。
*   **排除先於選擇(MUST)**:§4.3.2 之 `ttl == 0` 與陳舊排除在抽樣**之前**施行,不在之後。
*   **不得靜默退回固定序(MUST NOT)**:實作若因任何理由取不到隨機性,**不得**默默回退為某個內部迭代序的前綴——那正是本節所修的缺陷,而靜默的回退使它不可察覺。
*   **取不到隨機性時具名拒答(MUST;2026-08-16 新增)**:實作**必須**回 `#rejected` `%reason: #entropy_unavailable` 且**不得**附任何樣本,亦**不得**回一個空的 `#success`——後者與「目錄真的是空的」無法區分。
    **不得**使用 `#conflict`:依 §3.2 該狀態意為「**修你自己的封包**」,而此處請求毫無問題,問題在收方的宿主;且依 §3.2.2 之表,`#conflict` 配非 `#caid_mismatch` 之理由會使提問者記下 `#peer_refused`——**一句關於對方意願的陳述,而沒有人確立過它**。
    `#rejected` 是對的那一格:§3.2 定義它為「理解了但拒絕受理,區辨在 `%reason`」,而**新增可分情況一律以 `%reason` 承載**正是該節所規定的擴充方式。
    〔量,2026-08-16〕某實作於此路徑回 `#conflict`,提問者因而記為 `#peer_refused`——**修「說錯話的退回值」的那次修改自己說錯了話**。

##### 為何是抽樣而不是排序

**任何決定性、與提問者無關的排序 $f(\text{peer}, \text{target})$ 都可被磨。** 要把 8 個身分擠到 $N$ 個誠實節點之上約需 $O(N)$ 次嘗試,而 **[SPEC_15](./SPEC_15_Anti_Patterns.md)** §7.1 實測鑄鑰速率為 3,500 把/秒——$N=1000$ 時不到一秒;且依 §5.1.2 之耐久目錄,買到的席位是**永久**的。

> **決定性的規則,是攻擊者也能離線算的規則。** 防守方每次查詢算一次;攻擊者每個目標算一次,然後把贏的鑰匙永久留著。不對稱在此。

##### 這不是本規格放棄決定性

`n/` 是決定性的(CAID、觀測、權威),而此處要的是相反的東西。張力在注意到下面這件事時解消:**§4.3.5 的封頂本來就已經是一次截斷**,答案從來不是整份目錄。把**某一次任意截斷**擺出穩定得像正準答案的樣子,那才是偽造;誠實地說「這是一次抽樣」不是說謊的那一邊。

**且原本的不確定性是意外的。** 修訂前的實作以雜湊表迭代序取前 8,故答案是逐行程隨機種子的函數——**同一行程內問幾次都是同樣的 8 個**(實測),重啟才換一組。它既非決定性亦非抽樣,而**沒有人選擇過它**。意外的性質不是性質:第一個來修這個不穩定性的人會順手拆掉它,而且不知道自己拆了什麼。

##### 誠實的代價:目錄因此可被遠端列舉

**「重試得以觸及任何人」與「反覆詢問終將觸及所有人」是同一件事。** 實測:20 筆目錄於 **8 次查詢、0.03 秒**內被完整列出;修訂前則需等待對方重啟。

此代價**被接受**,理由是它不洩漏任何秘密:目錄所存者為**已簽署的公開廣告**,而節點轉發它們正是本節的用途(§4.2.4、§4.3.3)。**且退出機制早已存在且為規範性的**——`ttl: 0` 意為「不要轉發我」(§4.2.7),該廣告自此不進入任何回答。修訂前的不可列舉性是**模糊性**,不是政策,而模糊性同樣屬於「意外的性質」。

##### 與 `#find_node` 的不對稱,及其理由

`#find_node` **必須維持決定性**(§4.4:XOR 序,Kademlia 之查找收斂倚賴它)。二者的差別不是慣例:

> **`#find_node` 的答案,提問者自己算得出來因而可查證**——他握有目標與各節點 ID,可自行驗算那些節點確實最近。**`#discover` 的答案不能**:命中之意義僅為「有人聲稱服務此物」(§4.3.2),任何 8 個與任何另外 8 個一樣可信。
>
> **決定性屬於答案可被查證之處;在不可查證之處,它是偽造的。**

兩個 op 共用同一個回應編碼(§4.3.3),故實作**不得**因其形狀相同而統一其選擇規則。

##### 本節不宣稱女巫抗性

以上皆為**執行軌的成本整形**。**[ORDER_00](./ORDER_00_Interim_Constitution.md)** §1.1 與 SPEC_15 §7.1 之結論不變:內部機制供不出女巫抗性,錨在外部。本節所買到的,**僅為使重試有意義**——修訂前,一個拿到 8 個女巫節點的客戶端再問幾次都是同樣那 8 個。

#### 4.3.6 收方驗證階梯 **[Core Requirement]**

對 `%peers` 之**每一則**獨立施行;失敗**丟棄該則,不丟棄整份回應**:

| # | 檢查 | 失敗 |
| :-- | :--- | :--- |
| 1 | `%ad` 可解析、為 combo、§4.2 必填欄位齊備 | 丟棄該則 |
| 2 | `CAID(public_key) == node_id` | 丟棄該則 |
| 3 | 簽章以 `public_key` 驗證 §4.2.1 之 payload | 丟棄該則 |
| 4 | `0 <= ttl <= 15` | 丟棄該則 |
| 5 | `ts` 在收方陳舊上限之內 | 丟棄該則 |
| — | 皆過 | 可用之候選 |

*   **丟則不丟份**之理由同 **[SPEC_13](./SPEC_13_Ouroboros_Discovery_Protocol.md)** §6.1.1:來源在度數 0 上對等,跳過說謊者並繼續。一則不可驗證的記錄**不得**使同一份回應中可驗證者一併失效。
*   **此處無 `%from` 檢查**:§4.2.2 步驟 3 問的是 `#advertise` 的**說話者**;在一筆被轉發的記錄上,說話者是中繼者,而記錄不是關於他的。
*   步驟 2 **必須**先於步驟 3,理由同 §4.2.2:只問「簽章對不對得上封包裡的公鑰」的引擎會接受每一份偽造,因為偽造者兩樣都供給。
*   **廣告本體是數據不是程式**(§4.2.3)**逐字適用於每一則 `%ad`,且白名單判定必須先於任何求值**。發現回應是**你自己發起的請求所換回的遠端輸入**——正是最容易被忘記要不信任的那一類。

**陳舊上限(收方本地政策)**:`ttl` 既已用於傳播界,廣告**不宣告任何壽命**;`ts` 是記錄中唯一的時間。故收方**必須**自行施加陳舊上限,此屬 SPEC_13 §6.1.1 允許的**可用性政策**(得組態;**不得**藉此關閉驗證),且**必須**可為操作者所見。建議值 **15 分鐘**。**引擎不得自行於廣告格式增設壽命欄位**——那是規格改動。

#### 4.3.7 為何本層算不出 `ttl` 的原設計 **[說明]**

$d_L$ 與 GBB 住在 L3+ 的譜幾何側,`ttl` 住在 L2/線上的廣告格式裡,而**兩者從未相交**。
本層剩下的只能是**跳數**——即算不出距離時的退化影子:一個**斷言的指標**(度數 ≥1),
落在 §4.3.4 那道裂縫的**錯誤一側**。**欄位留下了機制的名字,失去了機制。**

此非新的妥協,而是 **[SPEC_13](./SPEC_13_Ouroboros_Discovery_Protocol.md)** §0 開宗明義所宣告之混合設計的後果——§0 列出「純粹譜路由的三項困難」,本節與 §4.1.1、§4.2.5 恰為其三:
無離散可驗證的預算(困難一)、$d_L$ 難建確定性路由表(困難二)、物理節點需穩定標識而譜指紋隨內容變動(困難三)。

**但須分辨**:§0 宣告的妥協畫在**層界**上(L2 走 Kademlia、L3+ 走譜感知,各司其職)。
一個 L3+ 的量出現在 L2 的線上格式裡,是那條線**沒有被守住**,不是妥協本身。
補救是說清楚它屬於哪一側、另一側不在時它是什麼意思(§4.2.7),不是重新設計協定。

多跳閘門屬後續路由弧——真正的機制(自內容定址資料重算距離)在那裡才拿得到。

### 4.4 `#find_node`:路由表查詢 **[Core Requirement,2026-07-28 新設]**

`#discover`(§4.3)問「誰**服務**這個 CAID」,答案來自服務索引。
`#find_node` 問「誰在這個**節點 ID** 附近」,答案來自路由表(§4.1)。
**兩個不同的問題、兩張不同的表,故兩個 op**——共用一個 op 正是本規格連續三弧在拆解的病。

#### 4.4.1 請求

```nlang
{{ %op: #find_node, %from: @caid, %target: @str }}
```

*   **`%target` 是 160-bit 節點 ID,不是 CAID(MUST)**:恰好 **40 個小寫十六進位字元**。長度不符、含大寫、為 CAID、缺席——皆 `#conflict`。
    > 把 CAID 也收下,就是讓「附近是誰」與「誰服務這個」重新變回同一個問題。由 CAID 導出目標(取其摘要前 20 位元組)是**呼叫方**的事。
*   `%from` 為主張(§3.2),**不得**有任何判定依賴它,且**不得**因此請求而被填入路由表(§4.1.2)。

#### 4.4.2 回應

與 §4.3.3 **同形**,刻意如此——收方以同一道階梯(§4.3.6)驗證每一則:

```nlang
{{ %status: #success, %source: @caid, %hops: 1,
   %peers: [ {{ %ad: @str, %observed_host: @str }} ] }}
```

*   內容為**已知對等點中距 `%target` 最近的 k 個**,依 XOR 距離**遞增**排序,同距者以 ID 自身決勝,使順序為全序且可重現。
*   **必須搜尋整張表,不得只搜目標所落之桶(MUST)**:對任意目標,最近的 k 個可能散在數個桶中。只回「目標那個桶」的實作在多數輸入上看起來是對的,而它是錯的。
*   空表 ⟹ `#success` 配 `%peers: []`。
*   **上限**:每則回應至多 **k = 20** 筆,且本體至多 **64 KiB**;**二者皆須由雙方各自持守**(§4.3.5 同律——只有誠實一方在守的預算不是預算)。

**放大比之記錄**:滿載回應實測 20 筆 / 10,347 位元組,對 87 位元組之請求為 **118.9×**——在位元組預算之內,但**約為 `#discover` 之四倍**(k=20 對上限 8)。OODP 走 TCP,來源位址無法偽造,故此為**已連線對等點的頻寬乘數,不是反射向量**;真正的界是位元組預算而非筆數。

#### 4.4.3 本版不做

疊代查找與多跳(α 平行、逐輪逼近)、活性 ping 與其驅逐、桶分裂、持久化(§4.1.2)。
本 op 一跳作答一次;**發現到的對等點仍不成為取物來源**(§4.2.6)。

---

## 5. 本地儲存結構

### 5.1 建議的 `.oo/` 內部結構

```
.oo/
├── format                      ;; 佈局格式版本（單行,現為 `1`）
├── objects/                    ;; L2: 內容定址儲存
│   ├── sha256/
│   │   └── 9f/
│   │       └── 86d081884c7d659a...
│   └── blake3/
├── refs/                       ;; L3: 別名與重定向
│   ├── HEAD
│   ├── deps/                   ;; 已發現套件的 hash 記錄
│   └── redirects/              ;; CAID_blur -> CAID_exact 映射
├── peers/                      ;; L2: 耐久對等目錄（§5.1.2）
│   └── directory
└── discovery.n                 ;; 歸屬信任根設定（§4.2.8.1）
```

#### 5.1.1 儲存的兩份宣告 **[Core Requirement,2026-07-29 新設;2026-08-15 拆為兩軸]**

物件是自描述的——CAID 字串自帶 `v1`/`v2`。**沒有版本的是佈局**:`HEAD`、`staged`、`abandoned` 這些檔案的存在與意義,此前無任何標記可供辨識。於是一次日後的佈局變更**無從宣告自己**,而讀到它的舊引擎會繼續操作一個它其實看不懂的儲存。

**但佈局不是唯一沒有號碼的東西。** CAS 物件的**編碼**——它們被寫成什麼形——同樣沒有。本節設立時兩者恰好同步,於是一個計數器被當成兩個用,而**這種便宜只在兩軸不分岔的期間成立**。

*   **兩份宣告,各治一軸(MUST)**:
    *   `.oo/format` 宣告**佈局**:`.oo/` 底下有哪些檔、各是什麼意思。
    *   `.oo/objects.format` 宣告 CAS 物件的**編碼**。
    *   兩者皆**自描述**——內容須說出自己在量哪一軸,不得為裸數字。
*   **CAS 物件不得自陳其編碼(MUST NOT)**:編碼宣告**必須**由容器給出。理由見 [REAL_03](./REAL_03_CAID_Protocol.md) §6.8.1:位址由值決定而非由檔案位元組決定,故編碼與身分正交;一個寫在物件內的編碼標記若進入雜湊,就把與位址無關之物塞進了身分,若不進入雜湊則直接違反 §6.7。**物件得指名它所依賴之物(那是內容),不得宣告它自己如何被編碼(那是容器的後設資料)。**

    > **〔2026-08-26 界線澄清〕本款禁的是「哪一版編碼」,不是「我是哪一種文件」。**
    > **版本仍然只住在容器**:`.oo/objects.format` 是唯一說出 `encoding=N` 的地方,
    > 而物件裡沒有、也不得有那個號碼。
    > 但物件**必須**說得出**自己是哪一種文件**(值／commit／暫存區),
    > 而那件事決定它怎麼被解碼——見 [REAL_03](./REAL_03_CAID_Protocol.md) **§6.8.3**
    > (耐久形必須自報身分,理由是 O35「線上就是 store」:物件會離開它的倉,
    > 而容器的宣告留在原地) 與 **§6.8.1 的 2026-08-26 更正**
    > (「不進雜湊 ⟹ 未被覆蓋」那一步不成立:能不能在**不改變解碼結果**的前提下改動它,
    > 才是判準;一個決定解碼的自報必然落入 §6.6 的重算)。
    > 〔量,引擎 Q-012〕把一個 commit 物件的自報換成值物件的自報 ⟹ `#caid_mismatch`
    > **並報出重算的 digest**;刪除或竄改自報 ⟹ `#object_undecodable`,與完整性失效分開報。
*   **缺席即拒絕開啟(MUST)**,且**引擎不得在讀取路徑上寫下任何宣告(MUST NOT)**:沒有宣告的儲存**不是**版本 1 的儲存,它是一個**引擎無法判斷的**儲存。
*   **既有的裸數字依明文規則讀取(MUST)**:本節拆分之前的儲存以單一裸數字 `N` 宣告兩軸,**必須**讀作「佈局 1、編碼 N」。此為**明文規則,不是推論**,且**讀取不得改寫它**。〔**2026-08-25 更正**〕本款原文的結尾為「——遷移只發生在寫入路徑上」。**該句已刪**,並由下一款取代:遷移不發生在寫入路徑上,它發生在一個**被明示要求的遷移動作**上。
*   **寫入不得改動它所找到的宣告(MUST NOT,2026-08-25 新增)**:一次寫入(提交、演化、回收)**不得**改寫、新增或移除該儲存既有的任何宣告。建立一個尚無宣告的儲存時當然要寫下宣告——**那不是改動**。
*   **宣告的推進必須被明示要求(MUST,2026-08-25 新增)**:任一軸的前進**必須**是一個獨立、由使用者明示要求的動作,且該動作**必須**在動手前說出代價——特別是**哪些引擎此後將打不開這個儲存**。推進**只動容器**:**不得**移動 `HEAD`、**不得**改寫任何物件、**不得**改變任何根的形式或位址。

*   **宣告未推進者不得收到它宣告不了的東西(MUST NOT,2026-09-03 新增)**:一次寫入
    進入宣告較舊佈局的儲存時,寫出的位元組**必須**是該宣告所對應的形式。若某項功能
    只能以較新佈局才表達得出的形式寫下,該功能**必須在寫下任何東西之前**拒絕,
    並指名那個被明示要求的遷移動作。

    > **上一款還不夠,而差在哪裡要說清楚。** 上一款擋住的是**宣告被改壞**;
    > 本款擋住的是**內容被寫成宣告說不出的樣子**。兩者都成立,那行宣告才**仍然為真**
    > ——否則儲存宣告著一個舊佈局,而裡面躺著造它的引擎驗證不了的東西,
    > 這比改壞宣告更難察覺:**沒有任何一次操作報過錯。**
    >
    > **判例(實測,引擎 Q-016a 與真 v0.42.0 二進位,2026-09-03)**:`layout=3` 的儲存
    > 收到 `oo evolve --pin`(其注入形式只有 `layout=4` 表達得出)⟹ `exit=1`、
    > **注入目錄仍為空**,訊息逐字指名 `oo migrate --grant migrate`;
    > 而同一個儲存收到**普通** `evolve` ⟹ 寫出舊形式的框,
    > **造它的 `v0.42.0` 二進位 `status` `exit=0` 讀得到、`commit` `exit=0` 提交得了。**
    > ⟹ **能力以佈局為閘,而不是以「寫了再說」為閘。**

    > **判例(實測,引擎 v0.33.0 與真 v0.20.0 二進位,2026-08-25)**:**這是本節第二次,一條寫下時為真的條款變成假的。**
    >
    > 前一次是「缺席即視為版本 1 並補寫」(見下方判例)。這一次是「遷移只發生在寫入路徑上」——它在**兩軸尚未分岔、且沒有任何儲存會因宣告改寫而被鎖住**的年代為真。
    > 〔量〕對一個 `v0.20.0` 所造的儲存(宣告裸 `2`)跑一次 `commit`:該次提交**回報成功**,並把 `.oo/format` 改寫為 `layout=2`、新增 `.oo/objects.format`。**造這個儲存的引擎從此拒絕開啟它**(`store format version layout=2 is not supported`)。
    > 〔量,把兩個檔切開來測〕**鎖住舊引擎的只有 `format` 那一行**:留著 `objects.format`、只把 `format` 改回 `2` ⟹ **舊引擎照讀,而且讀得到新引擎剛寫的那個提交**。
    > ⟹ **新引擎寫的位元組本來就是舊引擎讀得懂的;鎖住它的是一行宣告,不是內容。** 一次沒有人要求的遷移,替使用者做了一個不可逆的決定,而它甚至不需要為了寫入而發生。
    > **判例(實測,引擎 Q-012,2026-08-26)——編碼軸第一次由一個被明示要求的動作前進。**
    > 〔量〕`oo migrate --grant migrate` 對一個 `encoding=4` 的儲存把宣告推進到 `5`,
    > **`HEAD` 不動、任何物件不被改寫、任何位址不動**;而一次寫入(`evolve` ＋ `commit`)
    > 進入同一個 `encoding=4` 儲存時,**寫出的仍是 encoding-4 的物件、宣告一字未動**,
    > 且**造那個儲存的真 `oo v0.35.0` 讀得到新引擎剛寫的提交**。
    > ⟹ 上面兩款(寫入不得改動宣告／推進必須被明示要求)在**兩軸真的分岔之後**
    > 第一次被完整地行使,而不只是被寫下來。

*   **版本只因裁定而移動(MUST)**:任一軸的號碼**不得**因某次實作的順手變更而前進;每一次前進都必須有一則說出「舊引擎照舊讀法會得到什麼」的裁定,並記入 `spec/CHANGELOG.md`。**權威的版本對照住 [SPEC_00](./SPEC_00_Introduction.md) §4.4,不住本節。**
*   **讀不出來不得等於沒有(MUST NOT,2026-09-06 新增)**:一個持久化的值,
    若**無法被讀出**、或**只能被本引擎部分表示**,實作**不得**把它降級成一個
    較小的、它讀得懂的值,尤其**不得**降成空集合或預設值。**必須拒絕整個操作**,
    並說出「本引擎讀不完整」——**不是**完整性失效(**[REAL_03](./REAL_03_CAID_Protocol.md)** §6.8),
    **也不是**「那裡本來就沒有東西」。

    > **上一款治的是宣告,本款治的是宣告底下的一個欄位。** 兩者是同一句話的兩個尺度:
    > **不認得的東西必須具名地拒絕,而不是被讀成一個剛好認得的東西。**
    >
    > **⚠ 這一款特別容易在「遮蔽」的形式下被違反,而且理由聽起來是對的。**
    > 一個把未知位元遮掉的解碼器,在問句是「**這能不能授予什麼**」時是**正確的**
    > ——未知位元不得成為能力。**但同一個遮蔽,在問句是「**有沒有東西發生過**」時
    > 把一組未知位元變成空集合,而空集合讀起來就是「沒有發生」。**
    > **同一個函式,兩個問句,一對一錯。**
    >
    > **判例(實測,引擎 Q-040,2026-09-06)**:效應解除之標籤集持久化為位元。
    > 〔量,離開碼不經管線〕`1`(已知)⟹ 提交因未出示能力而 `exit=1`;
    > `not-a-number` ⟹ `exit=1`;而 **`8`(只由未知位元構成)⟹ `exit=0`,提交落地,
    > 且該 Commit 連特權標記都沒有**——**只改成員裡的一個數字,值一個位元組未動,
    > 授權要求即蒸發**。修法是**在解碼路徑上驗證**,而**不是**改動那個遮蔽函式
    > ——它對它自己的問句仍然是對的。

*   **不認得的宣告必須拒絕整個操作(MUST)**,錯誤須同時指名**儲存所宣告的**與**引擎所知的**,且**不得讀取、寫入或回收**該儲存。

    > 拒絕不是建議。一個引擎若打開了它讀不懂的儲存而**繼續操作**,結果不是損壞就是靜默誤讀;**具名地拒絕是唯一誠實的選項**。

> **判例(實測,引擎 v0.21.0 與真 v0.20.0 二進位)**:「缺席即視為版本 1 並補寫」是本節的原條款,而它**在寫下時是真的**——2026-07-28 當下每一個既存儲存的佈局都是 1。它變成假的,是因為該計數器開始承載另一軸而沒有人回頭看這個假設。代價是一條六步的鏈,**每一步的元件都照設計運作、無一步報錯**:
>
> 1. 一個編碼為 3 的儲存缺 `.oo/format`;
> 2. **唯讀**的 `oo log` 在它上面寫下 `1`——假的,而同一次執行成功讀出了編碼 3 的物件;
> 3. 舊引擎因 `1 ≤ 2` **打得開**(誠實宣告時它正確拒絕);
> 4. 舊引擎把一個健康的根報成 `#caid_mismatch … corrupt`——假的;
> 5. 舊引擎**寫得進去**,HEAD 移動;
> 6. 一次**合法的**回收清掉了原根與原 commit。**歷史消失。**
>
> 引擎沒有刪掉歷史;它造出了一個狀態,使一次合法的回收刪掉歷史。**要治的是第 1–5 步。**

> **〔2026-08-14 更正,2026-08-15 併入本次拆分〕** 本節原文另寫死「現為 `1`」。v0.20.0 已將其推進至 `2` 而未同步本行,使該 MUST 由「過期」變成「錯」——照字面實作的引擎會寫出 `1`,而它的物件是 2 的。**釘一個會演進的量時要釘關係,不要釘字面值。**

> **【2026-07-28 附註,2026-07-29 已兌現】** 上表原畫的是 `routing/buckets.dat`,並附註「目前不存在……目錄一旦落磁碟即成為需要 GC 與遷移的持久狀態,故其出現**應當**是一個被裁定的事件,而非某次實作的順手產物」。該裁定即 §5.1.2,而它**同時更正了藍圖的名字**:落磁碟的不是桶。

#### 5.1.2 耐久對等目錄 **[Core Requirement,2026-07-29 新設]**

`#advertise`(§4.2)受理的記錄**得**跨行程留存。留存的是**目錄**,不是 §4.1 的桶索引。

*   **索引為衍生物,不得持久化(MUST NOT)**:桶索引是目錄上的第二層索引,其內容為 `leading_zeros(XOR(self, peer))` 之函數,而 `self` 為本節點身分(§4.1.1)。持久化它會產生**第二個真相來源**,而兩者一旦分歧,**沒有任何東西能說哪一個對**。索引**必須**於載入時由目錄重建。

*   **重建序(MUST;2026-07-31 補足)**:按本節點觀測到的收受順序遞增重放;無該順序者(見下)退回簽章中的 `ts`。§4.1 的**佔位者優先**是插入序的函數而非別的,故重放即複現之。

    > **本條原文只說「收受時刻」,而那不足以複現插入序 [誠實記帳]。** 時刻若以秒記錄,一次突發之內全部平手,「重放即複現之」在該處**為假**。實測(2026-07-31):五則廣告於同一秒受理,重放序退化為 `node_id` 升冪,與到達次序無關。本條因此補上下列兩點——它們不是新政策,是讓上面那句話真的成立。

    *   **記錄必須使收受順序成為全序(MUST)**:耐久記錄**必須**攜帶足以將同一秒內之到達完全排序者(更細的時刻、單調遞增的受理序號,或其他)。**本規格不指定拼法**,只要求該順序在重建、壓實與載入三處為**同一個**順序。
    *   **該資料屬斷言半邊,且不得取自線上(MUST NOT)**:它是收方對「我先聽見誰」的記錄,**不在簽章之內**,廣告**不得**攜帶它;攜帶者依 §4.2.3 之字面本體閘處理。
    *   **必須為加性(MUST)**:新增欄位而非改寫既有欄位之單位。把 `received_at` 由秒改為毫秒不是加性——舊引擎會把毫秒讀成秒,而 §5.1.1 的佈局版本並未因此提升。
    *   **缺席與不可解析不是同一件事(MUST)**:**缺席**表示該記錄由較舊的引擎寫下,其到達早於本機制,故**得**排在前;**存在但不可解析**則是一筆說不出自己何時到達的記錄,**不得**因此繼承前者的優先序。實作**不得**以同一個退回值涵蓋兩者。

        > **量測(2026-07-31,驗收)**:某實作對兩者皆退回 `0`,而 `0` 排在一切真實序號之前 ⟹ 一筆內容為字串、負數、浮點或 `null` 的排序欄位,**取得了最優先的席位**。此路徑需要能寫 `.oo/` 的非語言層行程(語言層由 SPEC_08 §6.3 的路徑閘擋住),故屬 §6.3.3 已排除之對手;但**退回值本身選錯了方向**——讀不懂的排序資料應當失去優先,而不是取得它。

    *   **平手不得單以對方身分打破(MUST NOT)**:若實作仍有殘餘平手,**不得**以對方的 `node_id`(或任何純由對方決定之值)排序。**理由**:`node_id = sha256(公鑰)`,而純由對方決定的順序**與受害者無關**——最小者對每一個節點都最小,故磨一次即對全網有效。此與 §4.1 的桶指標形成對照:桶指標對**本節點自身 id** 取 XOR,故磨的成本是**每個受害者一次**,SPEC_15 §7.1 的成本表正是據此逐目標定價。殘餘平手**應**混入本節點身分,使成本回到逐受害者。
    *   **同一順序治理自動來源席位(MUST)**:§4.2.6.2 之自動來源上限,其「保留在席者」由本條之順序給出。**一份耐久目錄只有一個到達順序**;桶索引與自動來源集若各用一個,兩者會在同一份檔案上給出兩種「誰先到」。

*   **一則記錄有兩半,且它們分屬不同層(MUST)**。此即 §4.2.5 之切分落在儲存上:

    | 半邊 | 內容 | 隨複本旅行? |
    |---|---|---|
    | **自我認證** | 逐字 `%ad` 原文(含 `node_id`/`public_key`/`services`/`listen_port`/`capacity`/`ts`/`ttl`/簽章) | **是** |
    | **斷言** | 觀測到的主機、跳數、收受時刻、由前者導出的位址,及收方來源性 | **否** |

    「旅行」指**被複製到另一個節點**,不指撐過一次重啟。重啟原地是同一個節點在記得自己看過什麼——那不是旅行,是記憶。故耐久檔**必須**記載它所屬的節點身分;身分相符時斷言半邊一併還原,不符時**只還原自我認證的半邊**,該節點**必須**重新觀測後方能撥號,但**得**轉發(轉發送出的是逐字簽章本體,§4.2 已言簽章承諾的是值不是編碼)。

    來源性屬斷言半邊,不在簽章之內;同一節點重啟時應還原它,複製或 owner 不相符時必須清除它並載入為 `unknown`。舊格式缺少 `provenance` 欄位、或欄位值無法辨識時,也必須保守載入為 `unknown`,不得默認 `direct`。複本在新的 `#discover` 觀測後得成為 `relayed`,但複製本身永遠不能產生 `direct`。

    > **一個觀測不能旅行,一份簽章可以。** 複本得**複述**它被告知的事,不得**聲稱**它看見過。

*   **儲存記錄必須於載入時重新驗證(MUST)**:每則自檔案讀入的記錄**必須**通過與轉發記錄相同的階梯(§4.3.3:字面本體、欄位完備、`ttl` 範圍、`CAID(公鑰) == node_id`、簽章),未通過者**必須**丟棄並計數。

    > **理由**:上表「自我認證」一欄之所以能寫「是」,靠的不是記錄帶著簽章,而是**有人去檢查那個簽章**。一份沒人檢查的簽章記錄不是自我認證的事實,是一份**穿著簽章的斷言**——恰是本節費力分開的兩層又合回去。且 SPEC_08 §6.3 已裁 `.oo/` 的斷言層無從驗證而只能信任;耐久目錄**不屬於**該層,因為它**驗得了**。能驗而不驗,是把度數 0 的東西當度數 ≥1 對待。
    >
    > 代價由 SPEC_15 §7.1 計價:該節把路由表席位標價為**鑄造身分**,而一行未經檢查的檔案內容一個都不用花。

*   **寫入量必須為記錄數之線性(MUST)**:每次受理重寫全檔是 O(N²)。實測對照:150 則廣告下,全檔重寫式累計寫入 **14,380,000 位元組**,而追加式為 **131,568 位元組**(877 B/筆),**109×**。

*   **自動移除的界線(MUST)**:被**取代**的記錄(同一 `node_id` 之較新簽章廣告)**得**自動移除,無須出示任何能力。SPEC_08 §6.2「遺忘不得自動發生」治的是**使已記錄事實的內容不可達**;一則廣告從未被提交、無任何已記錄事實指向它、移除它不使任何 `#rollback` 變為不可逆。**該紅線不覆蓋整個 `.oo/`**,此處即其界線。

### 5.2 物件儲存結構 **[Core Requirement]**

本地物件儲存必須將演算法納入路徑：

```
.oo/objects/
├── sha256/
│   └── 9f/
│       └── 86d081884c7d659a...
└── blake3/
    └── 12/
        └── 34abcd...
```

### 5.3 CAID 重定向快取 [Reference Recommendation]

為了落實 SPEC_13 §5.2 的「自動重定向」，建議實作以下策略：

1.  **重定向查表 (RLT)**：記錄 `CAID_blur -> CAID_exact` 的 O(1) 跳轉。
2.  **鏈式扁平化**：多級精煉自動壓縮為直接指向最終 CAID。
3.  **背景查驗**：對 `#blur` 節點啟動 Worker 詢問是否有更新的精確版本。

---

## 6. 信任模型與安全

### 6.1 Hash 保證完整性，不保證可信度

`hash:sha256:xxx` 只保證「你拿到的內容和發布者當初的內容一致」。
它**不保證**：發布者善意、套件無漏洞、符合使用場景。

### 6.2 幾何信任鏈 (Recursive Trust Geometry)

信任在 `n/` 中被轉化為對 **CAID 的偏好選擇**。

**1. 顯式別名優先**
若多個權威對同一個別名提供不同 CAID,其裁決屬 **[SPEC_13](./SPEC_13_Ouroboros_Discovery_Protocol.md)** §7 的別名信任格,不是位元來源排序。`.oo/discovery.n` 只承載 §4.2.8.1 之歸屬信任根,**不得**被借作來源順序:同意向誰撥號不等於授予誰替別名選 CAID 的權威。

**2. 套件根信任清單 (Package Root of Trust)**
引擎應允許設定「套件根信任清單」。只有在此清單授權範圍內的黑名單更新才會被合併;此清單不回答節點歸屬,不得與 §4.2.8.1 合一。

**3. 簽章驗證 (建議實作)**
```nlang
{
    %content: { ... }
    %sig:     b"..."     ;; Ed25519 簽章
    %pubkey:  b"..."     ;; 發布者公鑰
}
```

### 6.3 惡意套件處理

- **本地黑名單**：`oo block <hash>` 加入本地拒絕清單。
- **社群黑名單**：黑名單本身也是 n/ Combo，可被發現和合併。
- **隔離執行**：沙箱機制限制套件的路徑存取權限（見 SPEC_08）。

**重要**：黑名單是工程機制，不是語言規格。

---

## 7. 持有聲明與其可選證明 **[Reference Recommendation,2026-07-28 由 Core Requirement 降級]**

> **本節整體降級,且原標題「GPP 驗證」已退役。** 降級為**過渡處置**:待
> **[REAL_01](./REAL_01_Ouroboros_Engineering.md)** §7.6 判別表所啟動的整併完成後,
> 本節餘留部分應併入 §4.2(協定面)與 APP_02 §6(電路面),此節取消。詳見討論 026。

**本節回答的是 REAL_01 §7.6 的問題 #4——「你真的有那份東西嗎」——不是 #1「你是誰」。**
後者已由 §4.1.1 的金鑰對與 §4.2.2 的簽章階梯回答,與本節無關。

> **更正(2026-07-28,規範性)**:2026-07-11 兩軌修訂稱本節之證明對象為**身分幾何**。
> **該命名有誤。** APP_02 §6.2 之電路,其公開輸入僅 `fingerprint_commitment, n, k`,
> **不提證明者**;故其所證為**持有**,不是「是誰」。此誤名使兩個不同義務(#1 認證、
> #4 持有)在同一小節共用一個詞,並於 §4.1.1 為節點身分定下材質後顯影為直接衝突。
> 更名依 APP_02 §0 之命名規則:**執行軌之物不得以「證明」命名**,故協定面所出示者為
> **持有聲明**,`GPP` 退出協定層而僅存於 APP_02 §6 之電路名。

> **兩軌修訂（2026-07-11，APP_02 §0）**：**質量（mass）退出證明範圍**：它是執行軌的
> 路由啟發值，可聲稱、可謊報，謊報只影響路由優先序，不影響內容真實性。
> **2026-07-28 補**:**持有的性格與質量相同**——謊報服務清單,你在 `#fetch` 處發現,
> 而取回的物件自我認證(REAL_03 §6.6),故代價是**一次白跑的來回**,不是正確性。按
> 兩軌原則自己的判準,持有本就應與質量同時退到執行軌;它未退,只因當時它叫「身分」。

### 7.1 選用之持有聲明擴展 **[Reference Recommendation,2026-07-28 改寫]**

§4.2 之 `@ServiceAdvertisement` 為**必備**部分。節點**得**另行附帶下列**選用**欄位以
供路由參考;它們**不改變** §4.2.2 之受理判定,亦不得使任何檢查被放寬:

```nlang
@ServiceAdvertisement & {{
    ;; 執行軌之聲明——皆為主張,收方必須主動判斷（REAL_01 §7.6.1）
    custody_hint: {{
        fingerprint_commitment: @hash  ;; ω/q-Gram 指紋承諾（Hash(Gram_ω ‖ q)）
        witness_proof:          b""    ;; 選用：F₂ STARK（電路見 APP_02 §6）
        ts:                     @int   ;; Unix 秒
    }}
    mass_hint: @float                  ;; 幾何質量（執行軌啟發值）
}}
```

**更名對照(2026-07-28)**:`gpp_proof` → `custody_hint`(執行軌不得以「證明」命名,
APP_02 §0);`identity_proof` → `witness_proof`(所證為**見證之知識**,不是身分)。
簽章與 `ttl` 不在本擴展內——它們屬 §4.2 必備部分,由 §4.2.1 定義其所簽。

> **`witness_proof` 之現況**:如 §7.2 所載,APP_02 §6.2 之電路不繫證明者,故一份有效
> 證明可被任何持有者出示。在 §7.5 之綁定完成前,它**至多**是一個可轉讓的路由提示。

### 7.2 四條舊義務的現況 **[2026-07-28 全面改寫]**

原文列四條「基礎驗證義務」。**其中兩條已遷出本節,一條無從滿足,一條如寫不健全**:

| 舊 # | 舊義務 | 現況 |
| :-- | :--- | :--- |
| 1 | 簽名驗證(`signature` vs `node_id` 之公鑰) | **屬問題 #2 認證,已遷出**——規範形式見 §4.2.1–§4.2.2。**在 §4.2.1 之前本義務無從滿足**:封包不攜帶公鑰,而 Ed25519 不對雜湊驗簽 |
| 2 | 時間驗證(±60 秒) | **已遷出**,見 §4.2.2 第 5 步 |
| 3 | 指紋承諾與服務 CAID 之 symplectic 刻畫相容 | **今日無從滿足,且理由已寫在 APP_02 §6.7 開放問題 1**:「物理格式尚未攜帶 $\omega/q$ 摘要——『封套沒有說謊』目前缺電路」。**沒有東西可供比對** |
| 4 | STARK 驗證(若引擎支援) | **如寫不健全**:APP_02 §6.2 之電路公開輸入不含 `node_id`,故證明**不繫於證明者**,任何持有一份有效證明者皆可出示之。見 §7.3 |

`mass_hint` 不在任何驗證義務內(2026-07-11 質量退位),`custody_hint` **亦同**——
兩者皆為執行軌之聲明,只作路由權重輸入。

### 7.3 舊「信任分級」為何整座梯子種類有誤 **[2026-07-28 改寫]**

原文以三個 Level 給出 0.1 / 0.5 / 1.0 的**信任權重**。該表有兩個獨立的問題:

*   **最底一階已不存在。** §4.2.2 使簽章成為**入場閘**:不符者 `#rejected`,不是「以 0.1
    的權重收下」。Level 0 不是一個信任層級。
*   **其上兩階量錯了東西。** Level 1/2 所量者為**持有聲明**之可信度,而持有屬執行軌;
    **執行軌的聲明推導不出信任權重**,它至多推導出**路由優先序**。把路由品質稱作
    trust,正是本節誤名的下游後果。

且 Level 2(權重 1.0)建立在一份**可被複製轉讓**的憑證上(§7.2 舊 #4)。一群女巫節點
重放同一份證明即可全體取得最高權重——**以可複製之物為女巫防禦,在構造上不成立**。

**過渡處置**:本表**不得**用於任何安全判定。引擎得將驗證結果作為路由優先序輸入並記錄
其層級,但**不得**據此放寬 §4.2.2 之任何檢查,亦**不得**以此充當 ORDER_00 §1.1 所要求的
外部錨點。原文「高安全模式對 `@Auth`/`@StandardLibrary` 必須 Level 2」一句**撤銷**:
它要求的是一個不提供該保證的機制。

### 7.4 與 LADD 的協作

持有聲明是 LADD L3+ 層優化的輸入:

*   **[APP_05](./APP_05_LADD_Global_Logic_Lattice.md)** §4.1 的引力路由算法使用 `mass_hint` 計算轉發權重（執行軌）。
*   **路由約束(2026-07-28 改寫)**:持有聲明經檢驗不符者,**降低其路由優先序**。
    原文作「視為身分不可信——逐出路由」,該語出自誤名時期;**持有不符不蘊涵身分不可信**
    (它可能只是資料已被 GC、或聲明過期),而身分由 §4.2.2 的簽章單獨判定。逐出屬
    SPEC_15 §7 之反模式機制(黑名單、tiebreaker、hop 預算),不由本節發動。
*   **快取策略**：檢驗通過的結果可快取，**驗證快取時效建議值：300 秒**，避免重複開銷。

    > **【2026-07-28 更名】** 原作「TTL 建議值:300 秒」。此量與 §4.2 廣告之 `ttl` **是兩種不同的東西,且不同種**:此處是**時長**(收方對持有聲明驗證結果的快取),`ttl` 是**計數**(格論量/傳播界,§4.2.7)。**[APP_05](./APP_05_LADD_Global_Logic_Lattice.md)** §5.1 將兩者放進同一個結構,而一個讀者必須追溯出處才分得出——與 disc 026 所診斷者同型(**[REAL_01](./REAL_01_Ouroboros_Engineering.md)** §7.6)。故此處不再稱 TTL;**`ttl` 一詞在本規格保留給 §4.2 之欄位**。

### 7.5 未決 **[2026-07-28]**

*   **綁定證明者(舊 #4 之修補)**:將 `node_id` 納入電路公開輸入或 Fiat–Shamir
    transcript,可使證明不可轉讓。**但這是路由衛生,不是安全修補**——若持有屬執行軌,
    綁定所買到的是「阻止一群分身重放同一份聲明來吸引流量」,而非任何身分保證;
    若欲以之充當女巫防禦,則 ORDER_00 §1.1 已論證內部機制供不出來,綁定後**仍然不夠**。
    故其成本預算應按執行軌衡量。
*   **本節之最終去向**:見本章 §7 篇首之過渡聲明。

---

## 8. L3 收斂層：基礎分散式收斂

### 8.1 基礎 `&` 運算協議 **[Core Requirement]**

當節點收到遠端的合併請求時，執行基礎格論運算：

1.  **接收請求**：`{ %op: #unify, %target: CAID_A, %with: CAID_B }`
2.  **本地計算**：嘗試計算 `CAID_A & CAID_B` 的結果
3.  **回傳結果**：
    - 成功：回傳合併後的 CAID 與內容
    - 衝突：回傳 `_|_` 與 `%cause` 結構（見 REAL_04）

### 8.2 與 LADD 優化的介接

基礎 L3 收斂（本文）：
- 直接對 CAID 進行合併運算
- 無額外路由優化
- 適用於小規模或區域網路

LADD 優化收斂（APP_05）：
- 利用譜引力預測合併可能性
- 熱帶剪枝避免無意義的衝突
- 測地線路由選擇最優節點進行合併

**兩者關係**：LADD 是 L3 的**優化擴充**，基礎收斂必須先正確實作，才能導入 LADD 優化。

---

## 9. 版本管理與遷移

### 9.1 Hash-based 版本控制

在 n/ 中，「升級套件」= 把 `deps` 中的 hash 換成新的 hash，然後觀測收斂結果是否有 `_|_`。

```bash
# 預覽升級是否產生衝突
oo upgrade @ai.agent <new-hash> --dry-run

# 確認無衝突後執行
oo upgrade @ai.agent <new-hash>
```

### 9.2 雜湊演算法未來相容性 **[Core Requirement]**

n/ 的 hash 格式天然帶有演算法前綴：

```nlang
hash:sha256:v1:9f86d081...
hash:blake3:v2:1234abcd...
```

本地儲存必須將演算法納入路徑（見 §5.2）。

---

## 10. 與規格書的對應

| 本文章節 | OODP 層級 | 對應規格 |
| :--- | :--- | :--- |
| §3 物理層 | L1 | **[SPEC_13](./SPEC_13_Ouroboros_Discovery_Protocol.md)** §6（系統發現介面） |
| §4 節點路由 | L2-L3 | **[REAL_03](./REAL_03_CAID_Protocol.md)** (L2 定址) |
| §5 本地儲存 | L2-L3 | **[SPEC_13](./SPEC_13_Ouroboros_Discovery_Protocol.md)** §4 (CAID 格式) |
| §6 信任模型 | L4 | **[SPEC_13](./SPEC_13_Ouroboros_Discovery_Protocol.md)** §7 (信任格論) |
| §7 持有聲明〔原 GPP 驗證〕 | L3+ (執行軌) | **[SPEC_15](./SPEC_15_Anti_Patterns.md)** §7 (安全反模式)、**[APP_05](./APP_05_LADD_Global_Logic_Lattice.md)** §5 (GPP) |
| §8 收斂層 | L3 | **[SPEC_13](./SPEC_13_Ouroboros_Discovery_Protocol.md)** §6.3 (LADD 介接) |
| §9 版本管理 | L2 | **[SPEC_13](./SPEC_13_Ouroboros_Discovery_Protocol.md)** §5 (精煉) |

**與 LADD 的關係**：
- 本文：OODP L1-L3 **基礎實作**（必須實作）
- APP_05：OODP L3-L5 **譜幾何優化**（選擇性實作）

符合規格的引擎**必須**實作本文定義的基礎傳輸與收斂，可選地實作 APP_05 的 LADD 優化。
