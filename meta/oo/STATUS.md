# meta/oo — 已裁定／開放問題（設計帳）

> 由四篇原料整理：`observation_result.md` · `commit.md` · `dot_oo.md` ·
> `control_plane.md`（不含入口索引 `oo.md`）。
> **不是規格**；是開工前的設計帳。若與正文衝突，以正文為準並回頭改這裡。
> **本檔自 2026-08-13 起不再承擔施工排序**；當前 Active／Ready／Blocked 與唯一
> 「下一弧」見 [`../WORK_QUEUE.md`](../WORK_QUEUE.md)。
>
> 整理日：2026-08-05。
> **更新 2026-08-07**：v0.11.0／v0.11.0-draft.1 已切（W0 完成、W3 半完成）；
> 併入 `docs/discussion/028`（Unison 比較）下沉的四筆 → D36、O31、O32、W8′、W0′，
> 並升 W3′ 的理由。028 §8（供應鏈）另開 O33、O34。
> **更新 2026-08-08**：O32 重掃為空（D36 成立）；W3′ 經偵察拆為 a／b（D37–D39），
> b 阻塞於 W12，先前「現況為不合規」係誤判已更正。
> **更新 2026-08-08（下午）**：W3′-a 已切 v0.12.0／v0.12.0-draft.1（零代修）；
> W8′ 偵察完成 → M1–M9、O35–O36，且 O31 的裁定形狀已由量測收斂。
> **更新 2026-08-13**：v0.13.0–v0.19.0 已完成 W8′-a、W4 系列、O42、
> parser fence 與 MBU 弧。施工面移至 `meta/WORK_QUEUE.md`；下方 §3–§6 保留為
> 2026-08-09 前依賴推導的歷史快照，不再是現行佇列。
> **更新 2026-08-13（下午）**：**O43–O47 回寫**——這五個編號開在 O42 的工單
> （`nlang-tools/docs/a_snapshot_is_not_a_reading_handover.md`）裡而從未回到本檔，
> 因此 2026-08-13 上午的盤點讀不到它們。其中 **O45（`%partial` 可觀測性）仍開放**，
> **O47 結案狀態待確認**。防這一類再發生的規則見 `WORK_QUEUE` §2.4。

**讀序（若只讀一輪）：**  
`observation_result`（值）→ `commit`（時間/歷史）→ `dot_oo`（耐久/身分）→
`control_plane`（誰驅動、寫入模型）。

---

## 1. 已裁定（含「已結、不再當開放問題」）

### 1.1 跨篇方法與本體

| # | 裁定 | 出處 | 備註 |
| :-- | :-- | :-- | :-- |
| D1 | 原料 ≠ 規格；〔量〕/〔讀〕分開；判別法 =「拿掉這句別句會變假」 | 四篇 §0 | 繼續這樣做 |
| D2 | **控制面是 CLI 的投影**，不是平行 API | control_plane §1.2、§3；用戶 2026-07-27 | 端點 = 動詞投影，不考古舊六端點 |
| D3 | **本機面 / 服務面兩套機制**已切好：本機 = 逐次 `--grant`；服務 = 令牌（未實作前不適用，且不得為形式在本機引入令牌） | REAL_01 §7.0；control_plane §1.3 | 「§7.1 全是紙」不準——本機閘已出貨 |
| D4 | `authorized_keys` = **服務面令牌簽發者**，不得與 `architects` / `affiliation_roots` 混用 | discovery_trust / SPEC_13；control_plane §3 | |
| D5 | 歷史命名空間 **全部歸 `~%Engine`** | commit §1.6；用戶 2026-08-03 | 語義等弧 A；搬家可先做空殼 |

### 1.2 值 / 觀測（observation_result）

| # | 裁定 / 已結 | 出處 | 備註 |
| :-- | :-- | :-- | :-- |
| D6 | 觀測結果有 **Q1 位置 / Q2 為什麼停 / Q3 可重現（%effect）** 三軸；Blur 答 Q2 卻被實成 Q1 | observation_result §1、§9 | 承重描述，非「已改引擎」 |
| D7 | SPEC_08 §3.2.1 的 **原因 / 狀態**（`#fuel_exhausted` vs `#incomplete`/`#blur`）**已正確**；病在 TAG_REGISTRY / SPEC_04 不用同一套詞 | observation_result §5 | 推廣詞彙 **不需新裁定** |
| D8 | `#fuel_exhausted` 的 bottom/blur 歸屬是 **策略**，不是它本身的性質 | observation_result §4 | |
| D9 | `%effect ≠ #pure` = 「可定址、不可重現」；填 commit §1.8 漏格 | observation_result §9；commit §1.8 更正 | |

### 1.3 版本控制 / 歷史（commit）

| # | 裁定 / 已結 | 出處 | 備註 |
| :-- | :-- | :-- | :-- |
| D10 | 與 git 的差在 **合併是否全域決定性全函數**，不在「快照 vs 補丁」 | commit §1.1（已更正） | git 物件模型也是快照 |
| D11 | n/ 值層 = **方向相反的 CvRDT**；`pin` 破壞交換律 = 特權 = 必須留審計痕 | commit §1.1.1–§1.1.2 | |
| D12 | 最小單位是 **○（savepoint）**；● 是 ○ 上「可定址且宣告可分享」的謂詞 | commit §1.5 | |
| D13 | 中間歷史 = **REDO log（操作 + 視界參數）**，不是 UNDO；住斷言層 | commit §1.5.4–§1.5.5 | **「要不要日誌」已結：要** |
| D14 | savepoint 在 commit 後原則上不存活，**可由 ● 記下的歷史重建** | commit §4 已結 | |
| **D42** | **注入與觀測是兩個獨立動作，拆。**（O13／A4，2026-08-27 用戶）**注入**＝與當前宇宙節點做 `&`、**不求值**（`SPEC_10` §2.2 早已如此定義，含 $\sqcap$ 公式）；**觀測**＝給一條已定路徑、消耗燃料收斂它。**一次做兩步的複合入口（CLI 或 API）容許存在，但那是 UX 層，不得是語義層** ——用戶原話「最後要不要有個 cli 指令可以一次做兩步，那就只是 UX 的問題」 | commit §1.10.1、§4.1 A4 | §1.5.7「同 sketch 異 digest」即兩者各自的痕跡 |
| **D43** | **持久 ≠ 固化。** 每一個 ○ **都已經是持久的**；`固化`（○ → ●）是另一件事。**● 的前提是「能不能算出 CAID」（能不能），不是「可分享」（要不要）** ——§1.5.2 的兩個條件因此分工明確，而會失敗的是前者。該前提已有答案（`#incomplete` 不可定址／`#blur` 可定址，`SPEC_08` §3.2.1）與動詞（**Lift**，§172）⟹ ○ → ● 分解為 **Lift → 鑄 CAID → 宣告可分享** | commit §1.10.2–§1.10.3；observation_result §5 | **⚠ 使 §1.5.7 的舊結論失去地基**（見 O79） |
| **D44** | **⊥ 的觀測結果也必須產生 ○。** 理由不是體驗：`%cause` 掛在那個 ⊥ 值上，**若失敗的觀測不產生任何東西，`%cause` 就沒有可指的對象，那次失敗不可檢查**。⟹ 這補的是 **D33／B1 已裁而從未有落點的「回報」那一半** | commit §1.10.4；§2.1.3 | ⊥ 帶確定 `%cause` 不隨燃料變動 ⟹ **可定址** ⟹ 承載 ⊥ 的 ○ 有資格轉 ● |
| **D47** | **○ 的產生判準是一動作一條款。**（O80，2026-08-28 用戶裁）**注入 ⟹ 格上的位置變了**（單調收窄）；**觀測 ⟹ 有 thunk 被真的化約**。**兩條都在本地可判定，都不需要 CAID**——這是本裁定的全部要點，因為 ○ 必須承載 `#incomplete`，而 `#incomplete` 沒有位址。**判別器程式碼裡已經有**：`lib.rs:418` `solid_combo_expansion_cost` 在遇到 `Thunk`／`Ref`／未決 spread 時回 `None` ＝「需要真正的 forcing 路徑」 | commit §1.10.5；observation_result §1 | **⚠ 兩個被否決的候選與一致性檢查，見下** |
| **D48** | **Q-014 取候選丙：不存狀態，存注入。**（2026-08-29 用戶裁定，`WORK_QUEUE` Q-014／`commit.md` §1.2.2）`evolve` **不再讀-改-寫任何共享格子**；它鑄一個**不可變的注入檔**（本地隨機 id、`atomic_write`），工作集 ＝ 未提交注入的 fold。理由不是行數（丙 280–400 ＋ 12–18 支探針，與甲＋A 同量級），而是**只有丙由構造修掉那個已量到的資料遺失**〔量 2026-08-29，40 並行 evolve：今天存活 4–6／40〕——甲給一個可交換的物件，但**有物件不等於有人交換**，那還要 Q-016；丙讓 **Q-016 的 meet 那一半整個消失**（§1.1.8 ⑤：meet 可交換 ⟹ 衝突圖沒有邊 ⟹ 協調是空的）。**⚠ 裁定之後、寫工單之前，驗收方查出三處與既有規範衝突，丙的形狀因此收窄**（詳見下列三點與 `WORK_QUEUE`）：**(1)** `SPEC_10` §2.2.1 **[Core Requirement，2026-08-08]** 逐字：「$Staged_{old} \sqcap Definition = \bot$ 時，演化**不得**發生」⟹ **「衝突變 ⊥」不是新選項**（**⚠ 2026-08-30 就地更正（驗收方前一日講得太死）**：§2.2.1 **整節規範的是「回報」**（必須指出座標／必須到葉／必須含錯誤碼／不得洩漏實作表示／各回報面一致），而「演化**不得**發生」那句的原文緊接著是「**這一半引擎向來如此；本節規範的是它必須說出什麼**」——**它是被當成既有事實陳述的，而那個「向來如此」成立的條件是單一寫者加一個共享格子**，正是丙要拿掉的。準確的說法：**該 MUST 仍然拘束，但這一節從沒設想過第二個寫者，對並行情形給不出指引**。⟹ 這是今天第三條同型條文（§4.1.2 寫於 local／published 之前；§3.1 順序 MUST 寫於注入模型之前；§2.2.1 寫於有第二個寫者之前））；今天的「先到者贏」〔量：第二筆 rc=1、不進工作集〕**才是合規的**。丙必須**先驗證再寫**（那不是讀-改-寫一個共享格子，是一次容忍過期的驗證讀）。**(2)** `SPEC_10` §3.1 **[Core Requirement，2026-08-28，D43／D47]** 兩條 MUST——Savepoint **必須**在 Commit 之後繼續存在、且**必須**帶有可判定的先後序 ⟹ **注入不等於 ○**：commit 清空注入目錄會違反耐久條款，隨機檔名違反順序條款。**驗收方原本宣稱「○ 由構造免費落出」，那是錯的**，且那兩條 MUST 是驗收方自己昨天寫進規格的。**(3)** `SPEC_10` §3 第 2 款逐字：「Staged：存儲自上一個 Commit 以來新注入但未提交的定義**集合**」⟹ **丙不是激進，它是第一個與既有定義相符的實作**——今天那個被改寫的單一檔案才是偏離。**⟹ 因此 Q-014 須拆成兩件**（拆弧的唯一正當理由：**這是兩件不同的事**——不同的規格節、不同的 MUST、不同的失效模式）：**工作集（Staged）的資料遺失** ＝ 丙，**與 ○ 無關**；**Savepoint 的身分與順序** ＝ W10 原題，仍需 A／B／C。**殘留待答（已寫進工單問題，不得默默決定）**：兩個並行 evolve 各自驗證通過而**合起來**為 ⊥ 時，§2.2.1 由誰兌現 | commit §1.2.2；`WORK_QUEUE` Q-014 | 用戶 2026-08-29 |
| **D49** | **並行注入合起來為 ⊥ 時：兩筆都接受，fold 在該座標回報 ⊥，由操作者解決。**（2026-08-30 用戶裁定；D48 的殘留待答）三個候選中——**1** fold 時拒絕較晚者、**2** 接受並回報、**3** evolve 當場取更強的驗證——**取 2**。**理由**：**它是唯一不需要序、也不需要協調的**，而那兩樣正是丙的賣點。**1 需要「較晚」＝需要序，而丙沒有序**（附錄一 Q19：`readdir` 不給插入序）⟹ 漏回 Q-014b；**3 根本不是第三種機制**——「驗證與寫入對其他寫者原子」需要一個序列化點，而序列化點就是共享格子⟹ 那是把 Q-016 的協調拉進本弧，是射程擴大不是選項（**驗收方原本把它列成第三個候選，那是錯的**）。**義務（隨本裁定產生，不得略過）**：本裁定**違反 `SPEC_10` §2.2.1「演化不得發生」的字面**⟹ **必須修 §2.2.1**，補上並行情形下該條的意思。**那不是繞過條文，是把一條單寫者的條文誠實地擴充到它從沒面對過的世界**——與 §4.1.2 需要的處理同型。**滿足的部分要說清楚**：2 滿足 §2.2.1 **實際規範的每一條**（指出座標、到葉、含錯誤碼、不洩漏實作表示、各面一致），違反的只有那句被當成背景事實陳述的前提。**給工單的建議（非裁定）**：注入**按座標分組**，依 §2.2.1 自己的話「衝突揭露的最小單位也應當是座標」，使爆炸半徑關在一格、其餘欄位不受影響 | `SPEC_10` §2.2.1；commit §1.2.2 | 用戶 2026-08-30 |
| **D50** | **○ 的前驅是複數（`parents`），不是單數。**（2026-08-30 用戶裁，O83）**用戶給的理由是來歷**：`.parent` 是從 `Commit` **搬**過來的——「原本只有 `commit.parent`，新加了 savepoint，當時覺得應該共用同一個結構、用不同欄位」⟹ **搬的是欄位，不是設計**；**單數是 `Commit` 的原設計，與現在的模型不合**。**⟹ 驗收方複驗，而佐證比理由更強**：`REAL_01` §2 **控制面**逐字寫的是 `"parents": ["hash:…"]` **陣列**，而 `value.rs:1765` 是 `Option<ContentHash>` **單數**——`commit.md` §2.6 早就把這一列記為「面上寫的 vs 核心的實情」不合。⟹ **規範面本來就是複數，單數只活在 Rust；搬家時搬走的正好是不合的那一半。** **後果 ①**：O83 前半隨之定案——`SPEC_10` §3.1 的**順序（MUST）改寫為 parents 款**，**全序讀法作廢**（交付方 Q2 讀成全序，理由成立但前提被本裁定換掉）。**後果 ②**：**A 與 C 只記位置、不記前驅，表達不了 `parents`** ⟹ **B2 是唯一與本裁定相容的形**。**後果 ③（必須在裁 B2 之前答的新問題）**：複數要求鑄 ○ 時知道**我看到的 tip 集合**，而取得 tip 集合最直接的做法是一個 `tips` 檔——**那正是丙剛拿掉的共享可變格子** ⟹ **必須先量**：tip 集合能不能由不可變的 ○ 集合**純導出**（X 是 tip ⟺ 沒有任何 ○ 的 `parents` 含 X），代價是什麼；以及 Q7 已量到的 O(N²) 體積會不會連帶變成每次鑄造 O(N) 次讀。**後果 ④**：A2 從「**搬家**」變成「**換形**」——不是把單數欄位搬到 ○，是 ○ 用複數、`Commit` 那個單數欄位不再設定 ⟹ `commit.md` §4.1 A2 的「parent 是搬家不是刪除」須就地標注 | `commit.md` §1.7.2／§2.6／§4.1 A2；`REAL_01` §2；`WORK_QUEUE` Q-014b | 用戶 2026-08-30 |
| **D51** | **combo 位元組相同而 `parents` 不同時,仍然鑄一顆 ○——甲。**(2026-08-30 用戶裁,Q-014b 追加偵察 Q16)⟹ **`D47` (a) 加一款:覆蓋關係改變也產生 ○**;iff 從一款變兩款,而 D47「一動作一條款」的性格因此改變(匯流不是一個動作)。**否決的兩個**:**乙(不鑄)＝今天的行為**〔量 2026-08-30:兩個並行 `oo evolve` 同一個檔 → **注入 2 個檔、○ 1 顆**〕,它讓分叉**永遠不合流**——DAG 說「還在分歧」而值說「已經收斂」,**與本卡標題所指的病同型**;**丙(把新 parent 加進既有那一顆)出局**——那是改寫一個不可變檔 ＝ D48 剛拿掉的讀-改-寫,與 B1 出局同一個理由。**⟹ 用戶當場往下推,而那一推是承重的**:「之後類似的狀況都能(該)走這條路徑,最後可能會像 os 的 `fork()` 一樣。」⟹ **一般化 ＝ 歷史層不繼承值層的冪等性**(`commit.md` §1.11,升為 §0.1 第 12 句):meet 冪等是值層不需要協調的理由,**而事件不冪等**;**反覆出現的錯誤形是拿值層的性質當歷史層的判準,已知五個實例**,**且 D47 自己指認過這個類別錯誤一次卻只修了一半**(判準從 combo 換成位置,兩者都在值層)。**`fork()` 的類比只成立到一半**:分叉是一等事件、parent 邊無條件記錄 ✓,但 **n/ 沒有 join 動詞**,匯流只能事後由「`parents` 有兩個」認出來 ⟹ **§0.1 第 3 句就地更正:不是只剩撤回沒有落點,是撤回與匯流兩支都沒有。****代價已量,必須記**:○ 的成長從此與**並行度**掛鉤而不只與工作量掛鉤,而〔量,Q-014b Q7〕`savepoints/` 已是 O(N²)、`oo gc` 不走訪、**沒有任何路徑刪 ○** | `commit.md` §1.11、§0.1 第 3／12 句;`WORK_QUEUE` Q-014b | 用戶 2026-08-30 |
| **D52** | **Q-015 取候選丙:○ 長出提交邊。**(2026-08-31 用戶裁,Q-015 偵察 Q9)某顆 ○ 記錄「我變成了 commit C」;`Commit.parent` **不再設定**(§1.7.7,`None` 貢獻零位元組);歷史走訪改走 ○ 上的提交邊。**否決的三個**:**甲(A2 不做)**——0 行全綠,但留下 Q-015 偵察 Q2 量到的事實:**磁碟上除了 `parent` 沒有第二份提交序**(無 reflog;`CommitMeta.timestamp` 是毫秒〔量〕相鄰兩次提交差 **54 ms**、同秒可撞)⟹ 不能當序;**乙(第三份 append-only 事件日誌)**——唯一自帶一張圖的,但跨版本貴一個數量級(舊倉無事件檔 ⟹ 必須從 `parent`＋○ 重建或雙讀),且**它是再開一份真相**,而 `savepoint.rs` 的模組註解上週才為此立碑:「There is no `LOG`: that was a second truth and a crash window.」——上一弧的教訓是**拿掉**第二份真相,本弧不宜加第三份;**丁(`CommitMeta` 加 `Option` 來源欄)**——最便宜(40–80 行/5–12 支測試)、舊 commit 零成本(手寫 `Debug` 的 `None` 不進雜湊),但**方向與 §1.7.2 相反**(把本地時間序記回 ●,而 §1.7.2 逐字裁「`Savepoint` 有 `.parent`,`Commit` 沒有」),且新 commit 成紀元(欄位若永遠 `Some`,同一宇宙由新舊引擎提交得到不同 commit CAID)。**取丙的第三個理由**:它是**唯一讓 A2 的原話變成真的**——§4.1 寫「歷史走訪須先改走 ○ 鏈」,而今天走不通正是因為 ○ 鏈裡沒有 commit;丙把 commit CAID 放進 ○,走訪才有東西可走,squash 的祖先檢查也才有地方搬。**⚠ 開單前必須先報價的一項**:偵察的 80–150 行**可能沒把「squash 祖先檢查改走 ○」算進去**(Q1 量到 `parent` 恆 `None` 之後 squash 永遠 `not an ancestor`) | `commit.md` §1.7.8;`WORK_QUEUE` Q-015 | 用戶 2026-08-31 |
| **D53** | **鑄造圖與呈現圖不必同形,而呈現圖必須是鑄造圖的純函數。**(2026-08-31 用戶,裁 D52 時一併給出;全文 `commit.md` §1.7.8–§1.7.9)用戶原話:「**實際鑄的節點/邊 和 `git log` 畫出來的節點/邊不一定要長一樣。一個是真正的實作,一個是為了讓人容易理解的樣子。**」**直接後果:它拆掉了丙報價裡的那個「缺點」**——偵察把「commit 要依 D51 再鑄一顆 ○」列為代價,而在本裁定下那是**正確答案**:提交是一個事件,§1.11 已裁事件不冪等 ⟹ **它本來就該有自己的一顆 ○**(鑄造圖);那顆 ○ 一進一出、只帶「我變成了 C」⟹ 畫圖時收成**一條邊上的標籤**(呈現圖)。**同一件事在鑄造圖裡是一個點,在呈現圖裡是一條邊**——用戶:「現在是看點還是看邊。」**⚠ 命名**:§1.7.1 已占用「兩張圖」(本地/全球,§0.1 第 6 句),本對**正交且整個活在本地圖之內**,**不得共用名字**(專案已為同名兩義付過一次帳:`GPP` 的「身分」)。**可武裝的紅線(用戶「說不定之後還要算有幾個洞」的形式化)**:歷史 DAG 是一維 CW 複形,$\operatorname{rank} H_1 = E - V + C$,而一個獨立環路**恰好是一次分叉又合流** ⟹ **H₁ 數的就是「有幾次分歧被真的收斂掉」**。把一進一出的 ○ 收成一條邊 ＝ **邊收縮** ＝ **同倫等價** ⟹ H₁ 不變;而刪掉一個分叉/匯流點會讓 $E-V+C$ 掉下來 ⟹ **紅線:呈現圖的 $E-V+C$ 必須等於鑄造圖的****⟵ ⚠ 2026-09-03 射程已由 D59 收窄:只拘束「同一個關係的兩種畫法」;拿它比兩個不同關係的圖是範疇錯誤**,**少一個洞 ＝ 藏起一次匯流,多一個洞 ＝ 無中生有一次匯流**。**且 D50 就是讓洞成為可能的那一則裁定**:單數前驅 ⟹ 鏈 ⟹ $H_1 \equiv 0$,**一個洞都不可能有**。**未驗證的猜測(不得引用為結論)**:meet 結合且交換 ⟹ 高維胞腔全被填滿 ⟹ 拓撲全部塌到度數 1,那將是 §1.11 在拓撲語言裡的同一句話 | `commit.md` §1.7.8／§1.7.9;D52 | 用戶 2026-08-31 |
| **D54** | **鑄造圖的節點集合＝○,提交是註記不是節點——甲。**(2026-08-31 用戶裁,Q-015 附錄一 Q14;交付方逐字不選邊並證明紅線分不出來)**先確立這一題為什麼需要裁**:D53 的紅線要算 $E-V+C$,而那要求「圖」有定義。**H₁ 裁不了它**——甲 $H_1 = e_p - n_○ + C$;乙(節點＝○∪●)在 D52 之下 ● 之間無邊、每個 ● 恰一條提交邊、$C$ 不變 ⟹ $(e_p+n_●)-(n_○+n_●)+C$ ＝ **同值**(驗收方複驗算式成立)。⟹ **紅線健全,但它不是這一題的判準。** **取甲的理由**:**D52 之下 ● 之間一條邊都沒有**(`Commit.parent` 不再設定)⟹ 它們不承載結構,承載的是**標籤**,而**標籤屬於呈現圖不屬於骨架**;且 **§1.7.2 已裁本地時間序住 ○ 不住 ●**,把 ● 放進骨架等於把那則裁定又鬆開一次。`commit.md` §1.7.8 的行文本來就是甲(交付方同此判讀,但明文寫「這不是我裁的」——**那是對的態度**)。**後果**:(i) 探針算 H₁ 只讀 `.oo/savepoints/` 的 `parents:`,提交邊**不計入邊數**;(ii) ● 仍然出現在呈現圖裡(`oo log` 印 commit CAID／message／Date),它們是**由 ○ 上的註記導出的**,**不違反 D53 的純函數要求**;(iii) 若有人把 ● 之間的 `parent` 邊畫進鑄造圖,H₁ 會上升——**那直接違反 D52** | `commit.md` §1.7.8;D52／D53 | 用戶 2026-08-31 |
| **D55** | **提交 ○ 帶兩條邊:時間覆蓋與祖先——甲。**(2026-08-31 用戶裁,Q-015 驗收回合 2)**本地圖裡有兩個關係,而 S2 把它們壓成了一句話。****時間覆蓋**(`parents:`)＝誰在誰之後被鑄,**rollback 不留痕而且正確**(被放棄的那幾顆真的發生過);**祖先**＝現在的 HEAD 從誰下來,**rollback 就是這條關係上的一次跳躍**。**兩次交付各對一半,而兩次都做了被要求的事**:交付 1 父＝先前 HEAD 的提交 ○ ⟹ 祖先對、**拓撲錯**〔量:線性 session 的 $H_1$ ＝ 0/0/1/3/8,十次提交宣稱八次不存在的匯流〕;交付 2 父＝所提交的 tip ⟹ 拓撲對($H_1\equiv 0$ 至 N=20)、**祖先錯**〔量:五顆＋rollback 回第一顆＋再一顆,基線 `final gen1` 兩列,交付 2 `final gen4 gen3 gen2 gen1` **五列**;且**不是顯示問題**——`squash` 同一條走訪 ⟹ **接受被回溯掉的 commit 當 base 並真的產出 squash commit**〕。⟹ **沒有任何單一條邊能同時當這兩者。** **⚠ 落地時最容易做錯的一格**:**祖先邊不得進入鑄造圖的邊集**——D54 已裁節點＝○、邊＝`parents:`,祖先邊與 `commit:` 同屬**註記**;若把它算成邊,菱形就回來了,**A.1 原樣復發**。**連帶**:交付 2 為了救 R-b 而加的 `abandoned` 過濾,在甲之下**應不再需要**(祖先邊指向鑄造當下 HEAD 的提交 ○,回溯段自然不在鏈上)⟹ 須重量後移除,**不要留成遮蔽用的死碼**。**取甲的理由不是折衷**:§4.1 原話「parent 是**搬家**不是刪除」、D50 改成「**換形**」——**搬過去的正是祖先那條邊**,而時間覆蓋是 ○ **本來就有**的另一條。**一顆 ○ 上兩條邊,因為那裡本來就有兩個關係。** 不動身分、不動 CAID | `commit.md` §1.7.10;D52／D53／D54;`SPEC_08` §6.2 R1 | 用戶 2026-08-31 |
| **D56** | **§4.1.2 的射程軸改為「操作者是否被告知」;而排除款以回報為前提。**(2026-08-31 用戶裁,Q-017 偵察 ＋ D46 對帳)用戶原話:「**操作者如果不知情,感覺就是落入「不誠實」的那塊。所以先讓操作者知情,才能同時表達,是操作者主動提交進去的。**」**這不是補一款,是指出既有那款的邏輯結構**:§4.1.2 排除「**顯式**地固化一個含 `_|_` 的內容」,而「顯式」不是世界的一個狀態,**是一個關於操作者意圖的主張**——而那個主張只有在操作者**被告知**時才為真。**唯一能讓他知道的就是同節的「回報的內容(MUST)」**⟹ **排除款無法在不先滿足它所排除的那條 MUST 的情況下進入**。⟹ **今天的行為不是「不在射程內」,是一個不該存在得了的狀態**:引擎跳過回報,於是製造出一個「排除款適用、但它的前提從未成立」的格。**與既有的誠實性軸同族**:`SPEC_08` §6.2「**會說謊的審計面**」與「標記必須反映事實而非旗標」——**回報成功而寫進 ⊥,是對「發生了什麼」的一個假斷言**。**直接後果**:(i) `c: c + 1` 那一格**進**射程,**「回報的內容(MUST)」當場適用**⟹ 本卡從「要不要立法」變成「**兌現一條既有的 MUST**」;(ii) **丙(預設拒絕＋旗標)出局**——中止正是 §4.1.2 拆掉的那一半;(iii) 甲／乙退回為實作選擇(報告後落地 rc=0 vs rc≠0)。**⚠ 一個未答的後半**:用戶說「才能**同時表達**,是操作者主動提交進去的」——「主動提交」要不要**被記進紀錄**(● 或 ○ 上),還是「回報發生過」就足夠?**前者動 commit 物件,後者只動輸出** ⟹ 射程差一個量級,**開單前須裁** | `SPEC_10` §4.1.2／§2.3；`SPEC_08` §6.2；`commit.md` §2.1.3(B1) | 用戶 2026-08-31 |
| **D57** | **「被告知後仍提交」是審計,不是授權;記在 ● 上。**(2026-08-31 用戶裁,承 D56)用戶提問:「使用者表達已被告知且同意進行,算不算是一種授權和審計?這應該就是要留在歷史的原因。不然,一個需要使用者同意的操作,只要這個動作的結果被紀錄下來,是不是能反推「已經有同意」這件事。」**⟹ 反推不成立,而且是量出來的**〔量 2026-08-31,v0.41.0 標籤二進位,known-answer 已過並帶對照〕:同一段含 ⊥ 的內容(`c: c + 1`)在兩個獨立的倉各自提交,**根逐位元組相同**(`cabe2ee25f29cc9a…` 對 `cabe2ee25f29cc9a…`)——因為**同意不是一個值,它進不了根**。⟹ **「靜靜地落地」與「告知後仍決定提交」在紀錄上是同一個東西**,**資訊真的不在裡面,不是不夠聰明** ⟹ **不單獨記下就永久不可回復**。**是審計不是授權**:授權是「出示能力以允許一件本來被禁止的事」,而**提交 ⊥ 並不被禁止**(§2.3.1 逐字:撞到 ⊥ 者仍**必須**寫入 ⊥ 與其 `%cause`)⟹ **不得有 `--grant`、不得有旗標**;而 `SPEC_08` §6.2「**標記必須反映事實而非旗標**」正面禁止以旗標在場作為標記依據。**歸屬 ● 的理由是規格自己的**:§6.2「審計標記不得入值,其歸屬為 **Commit**」＋`REAL_01` §7.3(2026-07-27 註記)「本機面的審計面是 Commit,**而非工作區內的一個檔案**——後者位於**斷言層**、**無從驗證**,且對**能寫入它的對手**不提供任何保證」⟹ **○ 就是工作區裡的檔案**,記在那裡任何能寫 `.oo/` 的人都能偽造。**代價**:`CommitMeta` 加一個 `Option` 欄位,照 `abandoned`／`privileged_effect` 的**手寫 `Debug`** 模式⟹ **`None` 時不進 digest** ⟹ **每一顆不帶 ⊥ 的 commit,CAID 逐位元組不動**〔量,紅線〕`x: 0` 根仍 `31745ef0…`、3 物件;**只有帶 ⊥ 的 commit 會有新 digest,而那正是規格今天完全沒有規範的那一類** | `SPEC_08` §6.2；`REAL_01` §7.3／§6.3.3；`SPEC_10` §2.3.1／§4.1.2；D56 | 用戶 2026-08-31 |
| **D58** | **Q-016 的 `pin` 半邊取候選乙：`pin` 在 evolve 當下吸收該座標既有的注入。**（2026-09-03 用戶裁定；用戶原話「ok 乙」＋「**你好像講過是算子（應該是講 CALM 那次），難怪會覺得對不起來**」）**缺陷不需要並行，兩個指令就到**〔量 2026-09-02，v0.42.0 標籤二進位，known-answer 已過（`oo --version` 逐字 `oo v0.42.0`、標準根 `7038e250…` available）〕：**循序**兩次 `evolve --pin --grant pin`（先 `x: 2` 後 `x: 3`）⟹ `oo status` 得 **`x: 2`**，rc 全 0、零輸出；20 次試驗**晚的贏 12、早的贏 8**，而 **20／20 與「隨機 id 字典序最大者勝」相符**。〔讀〕`injections::paths()` 的 `out.sort()`（檔名＝16 bytes 熵的 32-hex）＋ `universe.rs:940–950` 在 `pin_pending` 時改以 `replace_merge` 依該序 fold；**該處程式碼自書 `Q-016 owns the order of concurrent pins`——但它不只是並行的，循序也壞**。〔對照組〕不帶 `--pin` 的兩次衝突 evolve 在**演化邊界**即被拒（`Error: Evolution Conflict … #conflict at x`、rc=1、不寫注入）⟹ **只有 `pin` 造得出兩筆互相衝突而共存的注入**，序敏感性為 `pin` 獨有。**取乙的理由是三個互相獨立的證人，且沒有一個產生於今日**：**(1) 型別**——meet 是 ACI ⟹「對一個**集合** fold」有定義；replace 兩者皆非 ⟹ 對集合 fold **沒有定義**，而引擎照做了，缺的那個序由熵補上。**(2) 甲在四週前已被裁掉**——`commit.md` §1.1.6 ③（用戶 2026-08-04）逐字：LWW-Register **確實**能以純 CRDT 實現 `pin` 的效果，代價是把「**誰有權**」換成「**誰比較晚**」，而 n/ 明文拒付；**候選甲（注入帶單調序）就是那條路**。**⚠ 而今天的實況比 LWW 更差**：兩個運算元**都**具 `pin` 權限 ⟹「誰有權」分不出；集合無序 ⟹「誰比較晚」拿不到；**兩邊落空，裁決者是熵**（`SPEC_13` §5.1.2「用身分打平手是可磨的」是最近的親戚）。**(3) 丙（對 `pin` 路徑上鎖）對已量到的缺陷零效果**——缺陷是**循序**的。**乙的後果**：(i) 非交換那一步回到**本來就有序的地方**（evolve 是一個指令，與磁碟上既存注入之間有 happens-before）；(ii) **集合仍是集合、fold 仍只做 meet** ⟹ `SPEC_10` §3 第 2 款與 D48 一字不動；(iii) `commit.md` §1.7.3 的圖景**重新成立**——`pin` 的痕跡回到**提交粒度**，那正是該節量測它的那一層；(iv) **並行那一半不需要新機制**：兩個並行 `pin` 各自吸收後 ＝ 兩筆同座標且不可比的注入 ⟹ **D49／`SPEC_10` §2.2.2 逐字治之**（兩筆都留、fold 於該座標報 ⊥、離開碼非零、由操作者化解）。**⟹ W12 的措辭須更正**：`pin` **不需要序列化**，它需要的是**不要被放進一個沒有序的容器裡**。**射程不變式（不是機制）**：**對任一座標，工作集的值不得依賴注入的走訪序**；且**吸收的單位是座標不是檔案**——一個注入檔可含多個座標，整檔吸收會連同檔裡未被 `pin` 的座標一併吞掉。**建議（非裁定）**：吸收以「**指名被吸收的注入 id**」表達，可保注入不可變且不引入序。**紅線**：不動身分（`x: 0` 根 `31745ef0…`／3 物件／標準根 `7038e250…`） | `commit.md` §1.1.6 ③／§1.7.3／§1.7.11；`SPEC_10` §2.2.2／§3 第 2 款；D48／D49 | 用戶 2026-09-03 |
| **D59** | **$H_1$ 相等只在「同一個關係的兩種畫法」之間有效；不同的形用各自的判準。**（2026-09-03 用戶裁定，收窄 D53 的紅線）用戶原話：「**D53 那條數洞是上一輪我隨口提，結果你說剛好用得上的，所以當下我也沒有多想。而且感覺他也沒有違反「鑄造圖／呈現圖不必同形」。我的理解剛好是反過來讀：如果不同形，那各種「形」用各自的判斷方式，可能才是對的。**」**⟹ 驗收方把「不必同形」用反了**：D53 的紅線寫「呈現圖的 $E-V+C$ 必須等於鑄造圖的」，而那是**從後門把同形塞回去**。**紅線本身沒有錯，錯的是它的射程**：它的推導是「一進一出的 ○ 收成一條邊 ＝ **邊收縮** ＝ **同倫等價**」——**而邊收縮只在同一張圖上有定義**。⟹ **準確的射程：$H_1$ 相等檢查的是「這張畫是不是由收縮得到的」，因此只拘束同一個關係的兩種畫法；拿它去比兩個不同關係的圖，是範疇錯誤。** **同一筆帳在低一階已經付過**：**D55「祖先邊不得進入鑄造圖的邊集」**——理由一模一樣（兩個不同的關係混在一張圖上算拓撲，菱形無中生有）。**本條是它往上一階的同一句話。** **⚠ 直接後果，且是本裁定的動機**：`oo log` 畫的是**祖先**，`--graph` 的骨架是**時間覆蓋**——**兩者不是同一張圖的兩種畫法**，故 `oo log` 恆為一條鏈**不構成違反**。⟹ **Q-016b 若裁「收斂」，並行仍忠實地活在鑄造圖的 `parents:` 裡，而呈現為一條鏈——兩張圖都為真，沒有紅線被違反**（驗收方 2026-09-03 曾把這列為「收斂的代價」，**那是錯的，已收回**）。**既有探針不受影響**：Q-015 的 `h1()` 只讀 `.oo/savepoints/` 的 `parents:`（D54 (i)），從未跨關係比較 ⟹ **無需重寫任何探針**。**各關係的有效判準見 `commit.md` §1.7.12 對照表**；**未答的一格**：祖先今天是單數故其 $H_1 \equiv 0$，**那是元數的後果不是關係的性質**——若日後有匯流提交，祖先變複數，$H_1$ 在那張圖上才開始有內容 | `commit.md` §1.7.12；D53／D54／D55 | 用戶 2026-09-03 |
| **D60** | **Q-040 取甲：現在就花掉一個 layout（`layout=5`），不併批。**（2026-09-06 用戶裁定；用戶原話「甲就好」）**待裁的不是設計是價格**——形狀由 **D58** 給定（特權意圖與它所修改的值同住不可變成員），依「修類別不修個案」不需要新的設計裁定；而〔讀〕注入解碼器對**任何未知的 metadata 行**逐字 `bail!("unknown metadata")`（`injections.rs:105`，**且該嚴格性是對的**——它正是「宣告仍然為真」得以強制之處）⟹ **在框裡加欄位即破壞性框變更** ⟹ `layout=5` ⟹ **又一則破壞性條目、又一次 90 天時鐘重啟，而距上一次只有一版**。**取甲而非乙（併批）的理由是誠實**：乙**沒有指名一個具體的弧**就等於「以後再說」，而 `WORK_QUEUE` §2.4 記過那一整類帳最危險之處正是**那個以後沒有佇列位置**；且 90 天時鐘量的是**穩定性**，連兩版重啟**正確地反映了這一段確實不穩定**——**壓著不動好讓時鐘漂亮，是把儀器當目標**。丙（另尋載體）須先證明存在，今日無候選。**⚠ 用戶是在較弱的版本上裁的，而裁定之後量到更強的版本，一併記於此（不改裁定，只改嚴重度）**〔量 2026-09-06，v0.43.0 標籤二進位，known-answer 已過〕**原證據是競速**：兩個並行 `evolve --grant effect_override:{io,nondet}` ⟹ sidecar 從不取到聯集，10 次試驗中只持 `io` 的提交**成功 5 次**。**新證據是決定性的**：`evolve --grant effect_override:io` 後，**不刪** sidecar 而**無任何 grant** 提交 ⟹ rc=1 正確拒絕（逐字 `discharged #io`）；**刪掉 `.oo/effect_pending` 之後同一個無 grant 提交** ⟹ **rc=0、`Commit successful`、且該 Commit 連 `privileged_effect` 標記都沒有** ⟹ **特權 discharge 過的內容以零能力、零審計痕進入歷史**。**⚠ 而這正是 2026-07-26 已為 `pin` 做過的那次修補**：`main.rs:1050` 的註解逐字「the capability must be presented HERE, through the trusted channel — not inferred as authority from the durable intent」，並指出該目錄 **any n/ program can write (`~%Io./write_file`)** ⟹ **同一個論證從未被套用到 `effect`**；與 **D57** 把同意標記歸 ● 而非工作區檔案是**同一條理由**（`REAL_01` §7.3：斷言層對能寫入它的對手不提供任何保證）。**⟹ 嚴重度由「競速下的授權繞過」升為「刪一個檔即可的授權繞過」，而本弧因此有決定性的紅，不再只有統計紅。****不得過度宣稱的一格**：意圖搬進成員**不使它免於竄改**（改寫成員仍在斷言層），得到的是**不再可分離**——移除授權事實必須改寫那個承載值本身的物件，於是任何覆蓋該物件的完整性檢查都會蓋到它 | `commit.md` §1.7.11；`SPEC_10` §2.2.3；`REAL_01` §7.3；`SPEC_08` §6.2；D57／D58 | 用戶 2026-09-06 |
| **D61** | **提交的臨界區鎖留著；而它不是違反 `SPEC_10` §4.1.1，它是第一次讓該款成真。**（2026-09-06 用戶裁定，Q-016b 軸 A＋C 驗收）**⚠ 驗收方前一則把它說成「`flock` 在事實上回答了軸 B」，說得太重，就地更正。** **準確的歸屬**：§4.1.1 第 1 款要求晚到者算 $C_{new} = HEAD_{current} \sqcap Staged_{incoming}$，而 §4.1.2 的量測註記逐字記過它為何一直是空的——「該實作合併的是行程內載入的根，**從不重讀 HEAD**，故 §4.1.1 之『晚到者』半邊**亦不存在**」。**加鎖之後晚到者確實重讀了 HEAD、也確實算了那個 meet**〔量 2026-09-06，交付建置，三輪 20 並行工作者〕`oo log` ＝ **3**（base ＋ 兩個排隊落地者）、根含 **20／20** 座標 ⟹ **第二個提交的根就是「新 HEAD ⊓ 剩下的工作集」**。⟹ **式子成立了，第一次。** **而兩件因此變成假的或缺席的，屬誠實性編輯不屬裁定**：**(i)** 「**樂觀** (Optimistic Merge)」這個標籤現在錯了——實作是**臨界區**不是 OCC，**規格必須說出引擎實際做的事**；**(ii)** §4.1.1 **完全沒設想**「晚到者的工作集已被前一個提交消費」那一格（它假設晚到者手上還有 `Staged_incoming`），而本弧已做出正確行為（具名回報 `working set consumed by a concurrent commit`、rc=1、**不得**說成「本來就沒有」）⟹ **須補一款**。**⚠ 而軸 B 的真問題沒有被答，只是碰不到**：「**n/ 的歷史能不能合法分叉**」在單一容器上被鎖擋掉了，但它會回來，有三個具體理由——**(a)** `flock` 是**勸告鎖**且 **`v0.44.0` 不取它**，舊二進位與新的並行時照樣競態；**(b)** 它是**每容器、每機器**的，共享檔案系統上的兩台機器不受保護；**(c)** **分散式那條線沒有鎖可用**——● 在節點之間流動時兩個節點各自提交，**那才是 D50 的複數 `parents` 與 D55 的兩條邊真正要面對的場合**。⟹ **Q-016b 於上述兩件收尾後結卡；分叉那一題換一張卡並掛在分散式線上**（在那裡它是真問題，在單容器上不是）。**⚠ 隨本裁定記下的射程教訓（第六次同型，形狀是新的）**：工單 S4 寫的是**三條禁令**（不得加 compare-and-swap／不得動 HEAD 的形／不得改成多 tip），交付方**三條全遵守**，並找到第四種機制達成驗收方想保留的結果 ⟹ **禁止清單也是機制**，「不要做 X、Y、Z」與「要做 X」有同一個失效模式，該寫的不變式是「**不得決定那個未裁的問題**」。**同時要說公道話**：交付方明文指出「只縮小 `clear` 關不掉 R1」（last-writer-wins 之下，被覆寫的那個成功提交已 fold 且已消費的座標，不在新 HEAD 也不在工作集），驗收方複驗成立 ⟹ **驗收方要求的 R1 在它自己宣告的射程內無法滿足** | `SPEC_10` §4.1.1／§4.1.2；D50／D55；`WORK_QUEUE` Q-016b | 用戶 2026-09-06 |
| **D62** | **Q-002 取甲：三種條件三個名字。**（2026-09-07 用戶裁定；用戶原話「看到一半時的直覺是甲」）**① 形狀超過柵欄 ＝ `#stack_overflow`（不動）／② 宿主拒絕給資源 ＝ 新名（`#host_resource_denied` 或等價）／③ 剖析器 panic ＝ 新名（`#internal_error` 或等價）。** **理由不是完備性**——`TAG_REGISTRY` §2.7.3 那張表**已經**用「**誰決定**」把 `#max_depth_exceeded`（操作者）與 `#stack_overflow`（實作）切開；②③ 在同一根軸上是**兩個不同的「誰」**（宿主／實作自己的錯誤），把它們併起來會在下一次重演 §2.7.2 那個更正。**⚠ ② 這一格難在標籤字面為真而補救為假**：512 MiB 的保留就是 §2.7.4 末條 MUST（「上限與其所保護的資源是同一個決定」）的兌現機制，保留不到時實作**確實**到不了它宣告的 256 層 ⟹「我到不了那裡」為真；**但登記簿把該標籤的操作者動作寫死為「攤平結構／換實作／調旋鈕沒有用」，而這一格唯一有效的動作就是調一個旋鈕**（行程資源上限）⟹ 正是 §2.7.1／§2.7.2／§2.7.3 反覆論證的那個病：**把一個可行的補救換成一個無效的補救**。**⟹ 一句可重用的話：原因標籤是一句關於補救的話，所以它可以字面為真而仍然是錯的答案。** **離開碼：本格 rc≠0**（用戶：「這邊的話 rc!=0 看起來是對的」）；**而通則另議，見 D63 候選**。**三者皆不動身分、不是紀元**（新增 cause 變體不改既有值的位元組）。**紅線**：真陽性 ① 的既有兩支探針（`a_limit_you_cannot_catch_probe_test.rs`／`limit_you_cannot_choose_probe_test.rs`）不得改；根 `31745ef0…`／3 物件／標準根 `7038e250…`／`layout=5`／`encoding=5` 不動 | `TAG_REGISTRY` §0.2／§2.7.3／§2.7.4；`SPEC_10` §2.2.1；`REAL_02` §3.2.3；`nlang-tools/docs/a_ceiling_that_was_not_yours_recon.md` | 用戶 2026-09-07 |
| **D63** | **O84 取甲：離開碼由載體決定，寫成 `TAG_REGISTRY` §0.2 的一款。**（2026-09-07 用戶裁定；用戶原話「覺得應該是甲」）**值的載體（⊥／`#blur`／`Top`／值）⟹ rc=0；邊界的載體 ⟹ rc≠0。** `SPEC_10` §2.2.2／§2.2.3／§4.1.3 三條個案 MUST 降為本款之例。**這不是引入新約束，是把一個既存事實變成可被檢查的事實**〔量 2026-09-07，v0.45.0，12 格〕**9／12 今天已遵守**，不合的 3 格正好是 Q-002 那一族。**而 `⊥` 是一個值** ⟹ rc=0 那半邊一直都是對的，不必改。**⚠ 取甲的可執行後果**：真陽性 ① 也得改（今天 rc=0 ＋ 節點級 `_\|_`，依 §2.7.4／§0.2 兩者皆不合），**那是腳本看得見的行為變更**，須明文記帳；`oo lint` 同在名單上（印 `PARSE-SKIP` 卻以 rc=0 ＋ `diagnostics: 0` 收尾）。 **⟹ 而丙被裁為另一題（用戶：「丙說的應該是另一個問題」），且它已經有家。**〔驗收方回查〕用戶記得「上次討論過類似的，但忘記是 `\|>` 還是 CLI 的 pipe」——**是 CLI pipe**：`WORK_QUEUE` Inbox「`oo` 在下游提早關閉管線時 panic，而管線的離開碼仍是 0」（量 2026-08-28，v0.37.0），其〔未判定〕逐字為「**正確行為是安靜退出（忽略 SIGPIPE）還是具名報錯**」——**那就是離開碼問題換一個載體再問一次**。**`\|>` 那條線與離開碼無關**（O78 的 `\|>` 議題是態射的資料參數位置，不是回報）。**排序 ＝ 排在 CLI 指令的調整之後**（用戶明文），錨點為 **弧 D 第 2 項（`init` 的形狀）**，而該項自身依 §4.4「先 1 再 2」等位址字面。**⟹ 丙不進本弧射程，也不因 D63 取得排序權。** **⚠ 但 SIGPIPE 那一列與 D62 ③ 是同一族的第三個成員**：**引擎以宿主的 panic 回答一個它其實有話可說的問題**（另兩個：剖析器執行緒 panic 被冒名為 `#stack_overflow`；`main.rs:385` 的 `.expect("spawn oo-main thread")` rc=101 且無任何 n/ 答案）⟹ **無論 SIGPIPE 日後裁成靜默或具名，「panic」都不是那兩個答案之一** | `TAG_REGISTRY` §0.2；`SPEC_10` §2.2.2／§2.2.3／§4.1.3；O84／D62；`commit.md` §4.4 弧 D 第 2 項；`WORK_QUEUE` Inbox SIGPIPE 列 | 用戶 2026-09-07 |
| **O84** | **待裁（通則）：離開碼要不要明文由載體決定？** 〔起因 2026-09-07，用戶讀 Q-002 偵察後：「離開碼感覺問到一個比較大的問題⋯**之前其實都沒有認真討論過這個部份**」〕**查：全規格 grep 只有四處**——`SPEC_10` §2.2.2／§2.2.3／§4.1.3 三條**個案** MUST（皆逐字「離開碼非零」）＋ `TAG_REGISTRY` §0.2 **一格表格**。**沒有通則。但那格表格就是通則**：§0.2 把載體分成「消費者看到的形 ＝ **值本身**」（⊥／`#blur`／`Top`／值）與「＝ **診斷訊息與退出碼**」（邊界）⟹ **離開碼不是待發明的規則，是載體軸的一個推論**：值的載體 ⟹ 產生了一個東西 ⟹ rc=0；邊界的載體 ⟹ 宇宙裡什麼都沒有 ⟹ rc≠0。**`⊥` 是一個值，一次求出 `_\|_` 的計算成功了** ⟹ rc=0 一直都是對的，**這半邊不必改**。〔量 2026-09-07，v0.45.0 標籤二進位，離開碼直接取，**12 格**〕**值的載體 4／4 合**（`3`／`#conflict`／`#blur`／`#conflict` 皆 rc=0）；**邊界的載體 8／8 合**（語法錯誤／檔案不存在／座標衝突／空提交／三個 `#privileged_required` 皆 rc=1）**除了柵欄那一族 3／3 不合**（`evolve`／`eval`／`fmt` 400 層皆 **rc=0 ＋ 節點級 `_\|_`**）⟹ **通則今天已被遵守 9／12，不成立的正好就是 Q-002 那一族。** **而那一族違反的是一條已寫好的 MUST NOT**：§2.7.4 逐字判剖析期為**邊界**，§0.2 逐字禁止邊界「鑄節點級 `_\|_`」，而 `main.rs:126` 逐字 `println!("{}", Value::from(BottomCause::StackOverflow).to_nlang(0))` ⟹ **鑄了，且該給的兩樣形一樣都沒給**——**自 `v0.18.0` 起出貨的 MUST NOT 違反，不是本偵察造出來的。** **最鋒利的一格**〔量，兩個載體並列〕：`#stack_overflow` **確實有兩個載體**——求值期（`max_unification_depth: 100000` ＋ 1000 項鏈）得 `status` 裡的 `big: _\|_ (%cause: #stack_overflow)`，**它在工作集裡**，rc=0 ✅；剖析期（400 層）得 stdout 同一句、**什麼都沒進宇宙**、rc=0 ❌ ⟹ **兩者在 stdout 與離開碼上皆不可分**，而 §0.2 下一條逐字是 **「載體必須可判別（MUST）」**。**唯一能分開它們的東西，就是那張表右欄已經寫著的離開碼。** **候選**：**甲** 寫成 §0.2 新款（三條個案 MUST 降為本款之例）／**乙** 不寫通則只修本弧三格／**丙** 更強，寫進 `REAL_01` 的 CLI 介面契約（成本最高，且須先與「CLI 拼法非 Layer 1」對齊）。**驗收方推薦甲**——12 格裡 9 格已遵守，寫下它主要是把一個既存事實變成**可被檢查的**事實，不是引入新約束 **⟵ 2026-09-07 已裁 ＝ 甲，見 D63。** | `TAG_REGISTRY` §0.2／§2.7.4；`SPEC_10` §2.2.2／§2.2.3／§4.1.3；D62；`nlang-tools/docs/a_ceiling_that_was_not_yours_recon.md` §7 | **✅ 已裁（D63）** |
| | **D47 否決了兩個候選，第二個的理由來自 D46。** **甲（digest 變了）**：`#incomplete` 無 CAID，那一格問不出答案。**乙（格上位置變了，通用版）**：**強制求值一個 `#pure` thunk 從來不改變格上的位置**——thunk 與其值同指稱，不可解的 thunk 本來就是 Top ⟹ 乙會判「觀測不產生 ○」，而那正是要 ○ 的原因。**注意乙的失敗理由與 D46 的成立理由是同一句話** | | |
| | **D47 的深層理由：○ 記認識，● 記存在。** 強制一個純 thunk 改變的是**我知道什麼**，不是**它是什麼**。⟹ **D46 與 D47 是同一句話的兩面**（深度不進 ●／深度就是 ○ 的內容）。**舊判準「改變 combo 才產生 ○」＝ 注入那一條款被誤當通則**——類別錯誤，不是不夠精確 | | |
| | **D47 的觀測條款是被量測改寫的，原稿是「消耗了燃料」。**〔量 2026-08-28，v0.37.0〕觀測邊界的燃料是**固定進場費**：`c: 3`（原子）與 **200 步加法鏈同門檻**（fuel=0 → `#blur`／fuel=1 → 成功），30 欄位 combo 於 fuel=1 亦成功 ⟹ 「消耗了燃料」等價於「每次觀測都產生 ○」，**判不出差別**。**附帶：`fuel_remaining` 的三個家不因本裁定承重**（判別器不是一個燃料讀數） | | |
| | **兩項一致性檢查（皆通過）**：**①** D44（⊥ 的觀測結果也產生 ○）——thunk 被強制並坍縮成 ⊥，**化約確實發生** ⟹ D44 是本條款的實例，不是例外。**②** 燃料燒完、只前進一半的觀測——判別器在「需要 forcing」時就回 `None`，**⟹ 產生 ○**，正是「記錄我看到哪裡」的本意 | | |
| | **⚠ D47 未解、留給弧內的一件**：若 ○ 要**存**局部收斂結果，須能**獨立取得 Q1**——而今天「走到哪裡」埋在 `BlurDetail.partial`，即 `observation_result.md` §1 判為**放錯軸**的那個欄位（Q1 的答案被降級成 Q2 答案的一個欄位）。**這是實作面的錯位，不是開放的設計題** | | |
| **D46** | **固化深度不進 ●，除非那一格不可重現。**（O79，2026-08-28 用戶裁「丙」）**判準是 `commit.md` §1.8 的軸——「重不重算得回來」，不是「在哪一層」**：**①** `#pure` 的一格，強制後的值相對於 thunk **不是新資訊**（可重算），把它存進 ● 就是把一個可計算的關係存成物件——§1.8 逐字說 `DERIVED` 邊不該那樣做 ⟹ **● 保留 thunk**；**②** `#io`／`#nondet`／`#state` 的一格**不可重現**，若 ● 只留 thunk，那次觀測到的東西**永久遺失** ⟹ **強制後的值必須進 ●**。**機制已經存在，不必發明**：`SPEC_08` §4.2.4 的 `#io` → `#cached`（逐字「曾有干涉結果已被固化為穩定 CAID 投影」）就是「不可重現的觀測進入身分」的既有拼法。**甲（§1.5.7 舊裁）與乙各自從 §1.8 那張表的一半推廣，兩者皆被本裁定取代** | commit §1.5.7、§1.8、§1.10；`SPEC_08` §4.2.4；observation_result §9 | **⚠ 三個直接後果，見下** |
| | **D46 的後果 ①：今天的引擎違反它，而且該違反是一個已量到的無聲資料遺失。**〔量 2026-08-28，v0.37.0〕`commit` 強制求值每一個 thunk；算不出來的一格變 `_`，**定義永久不可回復**（Inbox 該列）。⟹ 那列**不需要單獨開卡，它由本裁定的落地一併修掉** | | |
| | **D46 的後果 ②：`SPEC_10` §1.1 #1 須改寫。**「一個 Commit 捕獲的是節點在**特定物理視界下的坍縮觀測結果**」在丙底下**對 `#pure` 的一格不成立**——那些格根本不該被坍縮。§1.2（evolve 期存 thunk）不受影響 | | |
| | **D46 的後果 ③：D45 的射程註記須補一句。**`固化` 的第四義（`%effect` → `#cached`）**不只是要被擋在改名射程外，它要被引用**——它是本裁定 ② 的機制。**同音字蓋住了答案** | | |
| | **D46 順帶收窄的一件**：**Lift 的用途變清楚了**——`#pure` 的一格燒完燃料只要留 thunk，重跑即可，**沒有東西要 Lift**；**Lift 只為不可重跑的那些格存在**。⟹ D43 的三步分解（Lift → 鑄 CAID → 宣告可分享）中，第一步是**條件性**的。**O80 不受影響**（○ 仍須承載 `#incomplete`），但它從此純粹是 ○ 這一層的問題 | | |
| | **⚠ D46 未答的邊界（開單時須答）**：**發散的純 thunk 算不算可重現？**〔量 2026-08-28〕`c: c + 1` 今天在 commit 得到 `_\|_ (%cause: #divergent)`。它是決定性地發散 ⟹ 依判準應保留 thunk，但那樣 ● 就不記得它會發散，每次都要重新發現。**便宜與否是實作題，判準本身不含糊** | | |
| **D45** | **`固化` 一字退休**，或只保留**求值義**（`commit.md` §2.4 全篇那樣用）。正準三字＝**注入／觀測／commit**（第三個早有名字，§4.3.1 已裁「● 不需要新詞」）。**⚠ `固化` 的第四義**（`SPEC_08` §4.2.4，`#io` → `#cached`，英文 Solidification）**在 `%effect` 軸上，不隨本裁定改語義，只得改名或明確分義** | commit §1.10.6–§1.10.7 | 〔量 2026-08-27〕`固化` 79 行／15 檔／四義；`GLOSSARY` 用它定義四個詞卻無它自己的詞條 |
| D15 | 全球邏輯格 = 本地歷史 **扣掉特權邊** | commit §1.7.3 | |
| D16 | 軸是「重不重算得回來」：自我認證 / 可重算 / 斷言（+ effect 第四格） | commit §1.8 | |
| D17 | 弧 A 日誌檔名用規格既有 **`wal/` + `audit.log`** | dot_oo §2.1、§3；用戶 2026-08-04 | 勿再發明名字 |
| D18 | `Commit.parent` 退場策略：**不再設定**（非移除），零紀元成本 | commit §1.7.7 | 舊 CAID 不動 |
| **D32** | **〔量〕§4.1.2 的中止有實作但抓不到**——只在合併結果不是 Combo 時觸發，而兩個 Combo 相 meet 恆為 Combo；且 commit 合併的是行程內 `self.root`，**從不重讀 HEAD** ⟹ 前置機制也不存在 | commit §2.1（2026-08-06） | **「local 嚴」是紙上的**；§2.3 診斷已更正 |
| **D33** | **B1 已裁**：§4.1.2 綁了兩件事。**收斂撞 ⊥ 必須停下並回報**（觀測的責任，**今天沒做到**）／**要不要固化由操作者決定**。粒度也錯：固化的單位是 **○** 不是「提交」 | commit §2.1.3；用戶 2026-08-06 | 要修的是**報告**不是**中止** **⟵ 2026-08-26：射程已由用戶擴張，見 O77**——本條不只治「並行提交的自動收斂」，含**內建在迭代中施用呼叫者態射而撞到 ⊥** |
| **D34** | **B2 已裁**：local **改條文符合實際**（合法但必須被告知）；published **補 `refine` 的「可執行/通過檢查」** | commit §2.1.3 | 順著 D33 成立 |
| **D35** | savepoint 的動詞：**● 不需要新詞**（`savepoint`/`commit` 就是 DB 那一對）；**○ 的產生不需要動詞**（演化的副作用）；缺的是**列出／回到／丟棄**三個 | commit §4.3.1 | 命名未裁，見 O30 |
| **D36** | **自動精煉無衝突時引擎的靜默，是語義級保證**——它說的是「**沒有義務需要處理**」，不是「有義務但引擎忘了說」。傳統語言直覺會覺得怪，但那正是格與 patch／todo 模型的分野 | disc 028 §4.3；用戶 2026-08-07 | **已由 O32 重掃證實**（2026-08-08）。⟹ 說話的時候就得說出東西來，見 W3′-a |
| **D37** | **W3′ 拆為 a／b**：「evolve 邊界說得出矛盾在哪」與「提交時報告 ⊥ 座標」**是兩件不同的事**——拆弧的唯一正當理由。b 阻塞於 W12 | 偵察 2026-08-08；用戶已批 | 依 `feedback_eager_in_deep_water` 之判準 |
| **D38** | **座標必須到葉**，不是到頂層欄位。今日訊息印的是 `f.key`（**你剛打的那個字**）。**⚠ 本條的第一版寫「引擎從來沒算過」——實測推翻，是相反的**：`unify_combo` 在回程逐層累積 `BottomDetail.path`，直接呼 `engine.unify` 量到 **`path = Some("p.q.deep")`**，精確到葉。**引擎算出來了，然後在兩個邊界上把它丟掉**：①`Universe::evolve` 回傳 `Result<(), BottomCause>`，整個 detail 被壓成無 payload 的 enum ②`Bottom::to_nlang` 只印 cause＋message，不印 path | 偵察 2026-08-08（讀碼→實測，兩次） | ⟹ **修在邊界，不動合併核心**。關掉 O33 前半 |
| **D39** | **本弧須補規格條文**（SPEC_10 evolve 側）：§4.1.2 只治提交，evolve 邊界的回報**在規格裡沒有家**。⟹ 這**不是**符合性工作，是**語義變更**，切版走 **minor**，與 W0′ 的 patch 不同 | 偵察 2026-08-08 | VERSIONING §2／§6 |
| **D40** | **CALM 給 D33 的「要修的是報告不是中止」一個結構性理由,而不只是設計偏好。** 塌陷在 Complete CALM 的判準下**恆為單調**(⊥ 是精煉序的 ⪯ 最大元,`o ⪯ ⊥` 恆成立)⟹ **正當與不正當的塌陷都合法**。故三者之中:**格**說不出來(⊥ 是最大元)、**判準**說不出來(兩者都單調)、**審計行**說不出來——**如果沒有人寫**。⟹ **若引擎不報,則沒有任何機制能分辨這兩者;回報是那個區分唯一能住的地方。** 〔量 v0.22.0,重新確認 D32/D33/D38〕`app: { ok: 1, bad: 1 & 2 }` 提交**成功**、`Bottom` 落在磁碟上的根裡、`log` 讀得回來,**全程無提示**。**⟹ W3′-b 的定位改變**:它現在阻塞於 W12 且被當成周到之舉,而按本條它是**唯一的稽核者**;D38 已量到引擎算得出葉座標然後在兩個邊界丟掉 ⟹ **缺的不是能力,是接線** | commit.md §1.1.7 ④″;D33／D38 | **「格表達不了自己的失敗,記錄才能」的第四個證人**(前三:§1.7.3 特權不留邊／disc 027 撤回住審計行／§1.8 軸是重不重算得回來) |
| **D41** | **「⊥ 能不能入 commit」與「逾時該回 `#incomplete` 還是 `#blur`」是同一個提問**:哪些非答案可以變成**耐久事實**。SPEC_08 §176 早有那一格(`#incomplete`＝暫態·無 CAID·**嚴禁提交** vs `#blur`＝快照·有 CAID·可提交),**而那張表就是 `Obs(H)=∅` 與 `o∈Obs(H)` 的區分**,寫在任何人聽說 CALM 之前。〔量 v0.22.0〕`ObservationState::Incomplete` **宣告了但全樹無人建構**、`is_transient()` **無呼叫者** ⟹ **那一格是空的**;逾時改鑄帶 CAID 的 `#blur`。前者 D33 已裁(可入歷史,操作者決定,但必須被告知),後者(**Q-019**)未裁 | commit.md §1.1.7 ④‴;SPEC_08 §176;Q-019 | **兩者應一起裁**,否則同一條界線會在兩處長出不同的答案 |

### 1.4 耐久 / 身分（dot_oo）

| # | 裁定 | 出處 | 備註 |
| :-- | :-- | :-- | :-- |
| D19 | **兩個 `.oo` 要拆**：HOME 側改 **`~/.nlang/`** | dot_oo §1.1、§3 | 遷移須同批 fs_guard |
| D20 | 工作區 `.oo/` 內 **三類威脅模型可以分開**（怎麼分未定） | dot_oo §1.3、§3 | |
| D21 | **保留點**（`.oo`），理由 = 命名空間保留 + shell 誤刪防護，不是「內部細節」 | dot_oo §1.4、§3 | 閘擋程式、點擋 shell |
| D22 | 搬移認領 = **`oo node <verb>`、後置**（如 `adopt --from`） | dot_oo §1.6、§3 | 介面已定；索引形狀未定 |
| D23 | 節點金鑰不得在工作區樹內；操作者 ≠ 節點（既有規格） | REAL_02 §4.1.1；dot_oo §1.2 | |

### 1.5 控制面（control_plane）

| # | 裁定 / 已結 | 出處 | 備註 |
| :-- | :-- | :-- | :-- |
| D24 | **〔量〕** 並行 evolve → staged last-writer-wins、無聲遺失；物件寫入亦裸 write | control_plane §1.1 | 類別 = 資料遺失/損壞 |
| D25 | 單寫者是 **使用假設，不是機制**；控制面存在目的會製造第二寫者 | control_plane §0、§1.1 | 須回改 commit §1.1.6 |
| D26 | **lock-free CAS + meet 重試** 才是 SPEC_10 §4.1 要的；鎖是「拒絕/阻塞」語義 | control_plane §1.4 | 與 CvRDT 對齊 |
| D27 | **`pin` 不可走 meet 重試**，須真正序列化 | control_plane §1.4.2 | 核心/外掛接縫 |
| D28 | 借 git：**鎖檔 + 舊值比對 + rename**；**不借**「失敗就停、人來 merge」 | control_plane §1.5 | |
| D29 | 響亮失敗（禁止靜默覆蓋）無論最終模型都要 | control_plane §1.5.1 | |
| D30 | REAL_01 舊 `nlang/discover` 的 horizon 欄位 **已被後續裁定廢掉** | control_plane §2.2 | 勿復活 |
| D31 | 「⊥ 與提交」= **雙規 + 雙未執行 + 本機 `--grant commit` 已退役** | control_plane §2.4 | 改 commit §2.1.2 用語 |

---

## 2. 開放問題（仍待裁定或設計）

### 2.1 值 / 觀測

| # | 問題 | 出處 | 依賴 |
| :-- | :-- | :-- | :-- |
| O1 | Blur 是否拆成「標註」vs 獨立型別？（定址時才需要獨立型別的理由已有） | observation_result §7.1 | 先 D7 詞彙推廣 |
| O2 | 「不知道→top、矛盾→bottom」是否升成規則？ | observation_result §7.2 | 影響 strict 下 fuel |
| O3 | `TopCaused` 的 cause 是否開放？ | observation_result §7.3 | O2 |
| O4 | `Approximate` 策略：修還是廢？ | observation_result §7.4 | |
| O5 | `%cause` 三套型別（Bottom/Blur/TopCaused）是否統一詞表？ | observation_result §7.6 | |
| O6 | Q1×Q2×Q3 每個組合是否可達（〔量〕未做） | observation_result §9.3 | |
| O7 | `%effect` 與 disc 023「觀測障礙」是否同一路線？ | observation_result §9.3 | |
| **O82** | **規格把值放在格裡、把邏輯放在 topos 裡,而從未說過這兩者是什麼關係。**(2026-08-29,用戶帶回外部提問「Inductive Types 怎麼做」引出;用戶定位:「全域轉量子邏輯、正交模格之後,規格書沒有仔細說局部是什麼,反而都放在附錄章節」)〔量 2026-08-29〕**規範層(`SPEC_*`／`REAL_*`／`SYNTAX_*`／`GLOSSARY`／`TAG_REGISTRY`)`Heyting` 0 次、`Topos` 0 次、`直覺主義` 0 次**;唯一講局部的是 `SPEC_06` §1.3.1 ＝ **分配格**,而 `APP_04` §2.2 說 **Heyting**、`COSMOLOGY/02` §1–2 把兩者當同一個東西寫成「分配格(Heyting 代數)」,**單一 MASA 的投影格其實是 Boolean**(比三者都強,排中律成立)⟹ **三個互不相同的答案,三個位階不同的家,從沒對齊過,而兩個非規範的家講得比規範的家多。** ⟹ **待答三問**:**(1)** n/ 的值住在正交模格 $\mathcal{L}$ 裡,還是住在 Sheaf Topos 的**物件層**? `APP_04` §2.2 把**邏輯**放進 topos、§1.2 把**值**放進子空間格,而 Curry–Howard–Lambek 要求兩者同範疇;**`APP_04` 自己也對不齊**——§1.2 節點↦子空間、§6.2 Data↦向量(物件 vs 子物件),Trinity 的「同構」只在 rank-1 成立而 `@int` 不是。**(2)** `SPEC_12` §1.1 守欄(2026-07-16 裁)逐字要求帶因 Top／帶因 `_|_` **不參與等值**、消費即蒸發 ＝ **證明無關被立法**;證明無關 ＋ 偏序 ⟹ 至多一支箭頭 ⟹ 沒有項可歸納 ⟹ **結構歸納不是「還沒做」而是被裁定擋住**(n/ 有的是 Knaster–Tarski／Park 不動點歸納,嚴格更弱)——**這個代價當時有沒有被看見?** **(3)** `SPEC_12` §3.2 結構遞迴逐字是**最大**不動點的描述(「無窮可能性」「不具備停機問題」)⟹ **n/ 的遞迴型預設是 ν 不是 μ,與 ML／Coq 對調**,而 `SPEC_04` §6.3 已給出理由(無全域截面⟹惰性被逼出)——**這是否要升成明文?** 〔順帶量到,不另開〕**(a)** Park 規則要**完備**格,而 `SPEC_01` §2.5.1 只說正交模、非分配,**沒有一處宣稱任意 join／meet 存在**——投影格模型本身完備 ⟹ 文件缺口非數學缺口,但 `SPEC_12` 整章的不動點語言其存在性保證沒被寫下來過;**(b)** `COSMOLOGY/02` §3 把排中律失效**放在單一視角內**示範,而**單一視角是 Boolean 的**⟹ `#blur` 的直覺主義理由掛在錯的樓層,正確來源是 **Kripke／層語義 over 視角偏序 ＝ Invariant 2 單調性**(更強、更 n/ 原生);`COSMOLOGY` 帶 [思辨性] 警告且 `#blur` 規範定義不依賴該段 ⟹ **地基敘事的缺陷,不是引擎的**,已入 `WORK_QUEUE` Inbox。**⟹ 這一則的牙齒**:它把「n/ 有沒有歸納型」「`#blur` 憑什麼直覺主義」「`%cause` 為什麼進不了等值」**證明成同一個問題的三張臉**;而 **D44(⊥ 也產生 ○)因此不是補丁而是後果**——見證在格內被立法為不可見,只能落格外,**v0.38.0 出貨的 ○ 層是它第一次有地方站,當時不是為此做的**。接 `disc 030`(n/ 的值不是任何 CTT 的型別:非 Kan ＋ 有向 ＋ 脈絡)——**證明相關正是 HoTT 那條軸** | commit §1.1.9;§0.1 第 11 句 | **不堵弧 A**;但 O1／O5(`%cause` 詞表)與 D45 在它底下,且 §1.1.8 ⑩ 的冪等分岔點與本則是**同一條公理的兩個否定結果** |
| ~~**O83**~~ | **✅ 已裁 2026-08-30（用戶）→ D50。複數。** **`SPEC_10` §3.1 的「順序（MUST）」把兩個不同的東西壓成一句話，而用戶 2026-08-03 原本想講的是其中一個。**（2026-08-30，用戶提問引出：「感覺原本想講的意思是 Savepoint 一定有個 Parent……感覺其實是兩個問題，但都剛好對到同一個描述」）**兩件事**：**(i) parent ＝ 一個函數**，本地宣告的邊，「這一顆從哪一顆來」；**(ii) 可比 ＝ 一個關係**，「任兩顆判得出先後」。**parent ⟹ 可判定的偏序（祖先關係），但不 ⟹ 全序；全序 ⟹ parent（前一顆），但把分叉壓成一條線。** ⟹ **用戶的 `oo log` 直覺正是判別器**：畫樹要的是**覆蓋關係**（哪條邊該畫），而裸的偏序只給**可達性** ⟹ 「單純說是偏序」畫不出樹，說 parent 就畫得出。**原意有出處且逐字**：`commit.md` §1.7.2「**直接後果：`Savepoint` 有 `.parent`，`Commit` 沒有**」（用戶 2026-08-03），理由在 §1.7.1 的兩張圖——**本地圖 ＝ 時間序，你造的，宣告的；全球圖 ＝ 特化序 ⊑，內容決定的**；而 §1.7.5 逐字「⊑ 是偏序，父不唯一 ⟹ 推導出來的不是樹，是格」，故**父邊只住本地圖**。⟹ **§3.1 那一款寫成「序」是把本地圖的邊描述成了全球圖的關係。** **⚠ 這則裁定直接改變 Q-014b 三個候選的排序**：**A（單調檔名）與 C（序欄位）給的是全序**，而並行兩筆的先後是**誰搶到較小的名字** ⟹ **把一個分叉畫成一前一後，且那個先後由排程決定**——**D49 昨天剛逐字拒絕過同一句話**（「以到達順序裁決等於讓答案取決於排程」）；**B2（每顆記前驅）給的就是 parent**，並行兩筆同父 ⟹ **forest，而那是真相**。且 **B2 是三者中唯一不需要換掉寫入原語的**（隨機 id 不碰撞，不需要 `O_EXCL`；A 需要，見 Q-014b 偵察 Q9）。**⚠ 但 B2 有一個比 §1.7.2 更深的問題，是丙的注入模型新造出來的**：○ 的 body ＝ **全部未提交注入的 fold**，故分叉之後鑄的那一顆**同時反映兩支** ⟹ **它有兩個父**。`parent` **單數表達不了**（`commit.md` §1887 逐字記著 `Commit.parent` 是單數 ＝ 「鏈，不是 DAG」，而線上格式的 `"parents"` 是陣列）⟹ **待答：`parents` 複數（DAG，git 的形），還是接受 parent 只是「我看到的那個 tip」而 body 才是真相？** | `commit.md` §1.7.1／§1.7.2／§1.7.5；`SPEC_10` §3.1；`WORK_QUEUE` Q-014b | **堵 Q-014b 的候選裁定**；解開後連帶 A2（`Commit.parent` 退場需要歷史走訪改走 ○ 鏈，見 `WORK_QUEUE` §3 的 A2 複驗） |

### 2.2 版本控制 / 歷史

| # | 問題 | 出處 | 依賴 |
| :-- | :-- | :-- | :-- |
| ~~**O79**~~ | **✅ 已裁 2026-08-28（用戶）→ D46。丙。** **固化深度進不進 ●？**（2026-08-27 由 D43 逼出）§1.5.7 原本裁「**● 必須要求全部固化**」，理由是否則 ● ＝ (程式 ＋ 某人的觀測深度) 的函數。**D43 把判準換成「能不能算出 CAID」之後，那個理由失去地基**——未求值的 thunk 是一個確定的描述，**它算得出 digest**（§1.5.7 自己量過：`1aeb5c4d…`），真正不可定址的是 `#incomplete`。⟹ 舊結論的**論據**已不成立，但**替代它的是什麼未裁**：兩個候選（● 全固化／● 只記注入的 sketch、thunk 就讓它是 thunk）都堵得住「同 sketch 異 digest」那個洞，因為病灶是**部分**固化。**⚠ 這是推翻一則已寫下的裁定，不是補一格空白**。**⟵ 2026-08-28 量測已交（v0.37.0 標籤二進位），四項全部改變了這一題的形狀**：**(1)** §1.5.7 的承重量測**在 v0.37.0 上不再重現**——固化與未固化兩支今天得到**逐字相同**的根 `067627389c…`，因為 **commit 會強制求值每一個 thunk**（staged 逐字 `c: a + b`，root 逐字 `c: 3`）⟹ **甲是今天的實際行為，且規格沒寫**；**(2)** 驗收方原判「`SPEC_10` §1.1 與 §1.2 相隔八行在打架」**是錯的**——§1.2 講 evolve 期儲存（staged 確實存 thunk，已量），§1.1 講 commit，**若 commit 強制則兩者一致**；**(3)** 乙／丙的「未歸約 meet 進 ● ⟹ 正準化負擔」**今天不存在**——注入時就歸約，兩序同根（`@int`／`3` → `654720559d…`；`@int`／`1|2` → `2cab2cd75e…`）；**(4)** **甲的代價已量到，且是無聲資料遺失**——算不出來的 thunk 在 commit 時變 `_`，定義永久不可回復（見 §6 Inbox 該列）。⟹ **丙直接修掉一個已量到的缺陷，甲則是那個缺陷的成因** | commit §1.5.7（已就地標記）、§1.10.2 | D43 已裁；**擋著 A2／A3 的耐久格式**。**用戶 2026-08-27 表態傾向丙，未正式裁** |
| ~~**O80**~~ | **✅ 已裁 2026-08-28（用戶）→ D47。丙（量測改寫後的版本）。** **○ 的產生判準怎麼表述？**（2026-08-27）舊判準「改變 combo 才產生 ○」可統一成「**digest 變了才產生 ○**」（注入改 sketch 連帶改 digest，觀測只改 digest）——**但它對 `#incomplete` 不成立，而承載 `#incomplete` 正是 ○ 存在的理由之一**。○ 的身分是本地鑄的 id（§1.5.3），**不得拿只有 ● 那層才有的東西當判準**。待答：燃料燒完、只前進一半的那次觀測算不算「有變化」？若算（本文傾向算，否則就失去「我看到哪裡」這個記錄），判準得用**局部收斂結果**表述——而那個東西今天住在 `BlurDetail.partial` 裡 **⟵ 2026-08-28 展開＋量測（v0.37.0 標籤二進位）。** **甲（digest 變了）已知失敗**（`#incomplete` 無 CAID）。**乙（格上位置變了）也失敗，而理由是 D46 自己送的**：強制求值一個 `#pure` thunk **從來不改變格上的位置**（thunk 與其值同指稱；不可解的 thunk 本來就是 Top）——那正是 D46 說它不該進 ● 的理由 ⟹ 乙會判「觀測不產生 ○」。**丙＝一動作一條款**（注入 ⟹ 位置變；觀測 ⟹ 有真的化約發生）。**深層理由：○ 記認識，● 記存在**——D46 與本題是同一句話的兩面，而舊判準「改變 combo 才產生 ○」是**注入那一條款被誤當通則**，類別錯誤不是不夠精確。**⚠ 量測改寫了觀測那一條款**：〔量〕觀測邊界的燃料是**固定進場費，不隨學到多少而變**——`c: 3`（原子）與 **200 步的加法鏈在同一個門檻**（fuel=0 → `#blur %cause: #fuel_exhausted`；fuel=1 → 兩者皆成功），30 欄位的 combo 於 fuel=1 亦成功 ⟹ **「消耗了燃料」等價於「每次觀測都產生 ○」，判不出差別**。**故條款須改寫為「有 thunk 被真的化約」**，而該判別器**程式碼裡已經有**：`lib.rs:418` `solid_combo_expansion_cost` 逐字在遇到 `Thunk`／`Ref`／未決 spread 時回 `None`（＝需要真正的 forcing 路徑）。**⟹ 附帶收穫：`fuel_remaining` 的三個家在改寫後不再承重**（判別器不是一個燃料讀數），驗收方原先列的前提①作廢 | commit §1.10.5 | **仍卡在 `observation_result.md` §1**（`BlurDetail.partial`＝Q1 的答案被降級成 Q2 答案的一個欄位）⟹ 若 ○ 要**存**局部收斂結果，須先能獨立取得 Q1。**A3 的真正前置，此前任何一份帳上都沒有記過** |
| ~~O8~~ | **✅ 已裁 2026-08-06 → D32–D33。** 不是「哪條對」，是一條規則綁了兩件事 | commit §2.1.3 | **落地形狀變了**：規格是編輯，**引擎多一件小交付**（提交時報告 ⊥ 座標）⟹ 不再是純規格 |
| ~~O9~~ | **✅ 已裁 2026-08-06 → D34。** | commit §2.1.3 | |
| **O30** | **`oo log` 要不要也顯示 ○？** 本文傾向不要——四個證人說 local/published 是最承重的一條牆，CLI 該讓它看得見 | commit §4.3.1 | 併入 CLI 主題（W22） |
| O10 | **B3** 全球圖索引 commit CAID 還是 `root`？ | commit §4.2 | 可能讓 A2 多餘 |
| O11 | **B4** `DECLARED` 邊信任根（模式已有 affiliation_roots） | commit §4.2 | |
| O12 | 弧 A：○ 樹住哪、怎麼耐久；日誌正準形（詞彙 + 是否記輸入來源） | commit §4.1 A1–A3 | |
| ~~**O13**~~ | **✅ 已裁 2026-08-27（用戶）→ D42–D45。** **`oo evolve <路徑>`**：注入與固化拆成兩動作 **⟵ 當日展開，形狀如下**：**注入 ＝ 與宇宙做 `&`、不求值**（`SPEC_10` §2.2 早已如此定義，含 $\sqcap$ 公式），**觀測 ＝ 收斂一條已定路徑**；§1.5.7「同 sketch 異 digest」就是兩者各自的痕跡。用戶當日另定四件：**持久 ≠ 固化**（每個 ○ 都已持久）、**● 的前提是「能不能算出 CAID」不是「可分享」**、**⊥ 的觀測結果也產生 ○**（補 D33／B1「回報」那一半的落點）、**`固化` 這個字應退休**。**射程因此含詞彙遷移**（用戶：「感覺可以併」）——〔量〕`固化` 79 行／15 檔／**四義**，`GLOSSARY` 用它定義四個詞卻無它自己的詞條，`Staged` 與 ○ **耐久性相反**。**唯一該擋在射程外的是第四義**（`SPEC_08` §4.2.4 `%effect` → `#cached`，英文 Solidification）。**它推翻 §1.5.7 一則已寫下的裁定**（固化深度進不進 ●）⟹ 開單時必須明說。全文 commit §1.10 | commit §4.1 A4、**§1.10** | 決定 A3 詞彙；**A3 另卡 `observation_result.md` §1**（`#incomplete` 無 CAID ⟹ ○ 的產生判準不能用 digest 表述） |
| O14 | `refine`「可執行/通過檢查」那一格放什麼、誰跑？ | commit §4.4 | |
| O15 | REPL 觀測 / 演化切換 | commit §4.4 | |
| O16 | pin 傳播規則 | commit §4.4 | **先做 A 再看是否還在** |
| O17 | `CommitKind::Refine` / `DECLARED` 是否更像 advert 而非 commit | commit §1.8.1 | 評估，非必須先裁 |
| **O33** | ~~座標粒度~~ **前半已裁 → D38（到葉）。** 剩下的是「**哪兩個來源**」——需要 meet 保留出處 | disc 028 §9(4)；偵察 2026-08-08 | **不擋 W3′-a**，另議 |
| ~~**O32**~~ | **✅ 已重掃 2026-08-08，為空 → D36 成立。** 三種形狀：①耐久狀態中的 `pending` 字樣 ②原始碼中的義務型詞彙 ③**行為實測**——一次真正會收窄的 `oo evolve`，**逐字零輸出、exit 0、`.oo/` 未多出任何檔**，commit 後 `staged` 消失。`pin_pending`／`effect_pending` 方向**相反**：那是操作者 → 引擎的意圖，由 commit 消費，不是引擎交還的待辦 | disc 028 §4.3–§4.4；用戶 2026-08-07 | **最近的反例是 `#blur`／`#incomplete`「請加燃料重觀測」，但那是答案的性質、不是會累積的清單**——patch 的病在於帳會長，這裡不會 |

### 2.3 耐久 / 身分

| # | 問題 | 出處 | 依賴 |
| :-- | :-- | :-- | :-- |
| O18 | **節點身分：路徑 = 索引還是定義？**（候選已寫） | dot_oo §1.2.1、§4.1 | 介面 D22 已定 |
| O19 | **換機器的刻意身分繼承** | dot_oo §1.6.1、§4.2 | 與 O18 同一機制兩方向 |
| O20 | 工作區三類 **怎麼分**（子目錄 / format / peers 外移？） | dot_oo §4.2 | D20 只裁「可以分」 |
| O21 | `nodes/` 回收與鑄造時機 | dot_oo §2.3、§4.3 | 與測試洩漏同源的一半 |
| O22 | `.nlang/` 改名的機械清單與遷移 | dot_oo §4.4 | 必須含 fs_guard §2.2 |
| ~~**O23**~~ | **✅ 已裁(2026-08-14,用戶):兩個號碼,而第二個必須由容器宣告。** **(1) `.oo/format` 只宣告佈局**——`.oo/` 底下有哪些檔、各是什麼意思。〔量〕format 2 與 3 的佈局逐檔相同 ⟹ **該軸自 2026-07-28 引入至今仍是 1**,先前兩次撥動(Q-010a 去 span／Q-010b 根以摘要指名標準根)改的都不是佈局。**(2) 物件編碼由 `.oo/objects/` 這個容器宣告**,不由 `.oo/format`,也**不由物件自己**。**(3) 每個可變檔自陳自己的格式**(`HEAD`／`staged`／`peers`…)——用戶提案,成立。**(4) CAID `v1`/`v2` 不需要新號碼**,它已在 `HEAD` 與 commit 物件內自陳。**承重理由:CAS 物件是唯一不能自陳的一類,而理由是內容定址本身。** 〔量〕位址不是檔案的雜湊——路徑 `b7741081…` 對檔案內容 sha256 `350c42c7…`;`content_hash()` 走 `bn_serial`(BN/),磁碟上的 JSON 只是容器 ⟹ **編碼與身分正交**。故自陳版本欄只有兩個放法,都不可行:**進雜湊**=把一個與位址無關的東西塞進身分,且同一個值的兩種編碼成為兩個宇宙;**不進雜湊**=直接違反 REAL_03 §6.7「不得存在未進入雜湊的欄位」(判例 span)。而「容器宣告其內容物的格式」在倉裡**已有先例**:`objects/sha256/` 這層目錄就在宣告雜湊演算法。**須明說的界線**:CAS 物件**得**指名它所依賴之物(那是內容,如根的標準根摘要——它由值決定,故仍是位址的函數),**不得**宣告它自己如何被編碼(那是容器的後設資料)。**副產物一:Q-012(JSON → n/)在身分軸上是零破壞**,只破壞讀相容——位址由 BN/ 決定,與容器編碼無關。**副產物二:M1 第二步失去意義**——佈局缺席視為 1 是對的(該軸真的沒變過),而物件編碼宣告缺席就是「不知道」,引擎不會再去猜那一格 | dot_oo §4.5;Q-011 偵察 §6.5 | **實作與 O36／B／C／D 同弧**;`.oo/format` 須由 3 退回 1 並把編碼宣告搬進 `objects/` ⟹ 又一次格式面破壞 |
| **O34** | **撤回在去中心的世界裡有沒有可能？** 「沒有中央處可發布撤銷」能否用**簽署的撤銷廣告**繞過，還是那只是把問題換成「誰有權撤銷別人的物件」 | disc 028 §8.3、§10(7) | 接 027 §3 |
| | ↳ **後半已自答**：走訪對**缺席**不記錄而對**不可解碼**記錄，**此不對稱是規範要求的**——REAL_03 §6.6 明文允許「不存在／不透明」依 §9.1 續行。不是遺漏，且 **W0′ 交付不得把它改成致命**（工單 R3） | | |
| ~~**O31**~~ | **✅ 已裁（2026-08-26，用戶）。** `.oo/` 的耐久編碼改用 n/ 自身。**三題全裁**：**①編碼形＝(b)+(c) 合成一件**——框（耐久形自報身分）＋兩個**只在框內合法**的字面（`system` 軸、效果標籤）。**(c) 單獨不可行**〔量 2026-08-26〕：`SYNTAX_05` §3「`~%` 唯引擎鑄造」且 conformance `L2-60`／`L2-61` 釘著，讓它到處合法＝刪兩個向量、廢所有權條款。**(a) 特權解碼器否決**（把 O35 的「讀取是解碼不是求值」換成「讀取有兩種解碼」）。**框住值的外面**〔量〕任何住在值裡的欄位都進身分（`{ a: 1 }` `fd335de1…` → `{ %kind: …, a: 1 }` `1882fd8d…`），而 `%val` 更會把框自己吃掉（`{ %val: X, …}` 一律投影為 X）；**兩個位子都要**——`objects.format` 的 `encoding=5` ＝倉級閘，**檔案開頭的 token ＝物件級自報**（先例：今天的 `"standard-root:<hex>"`），理由是 **O35 已裁「線上就是 store」** ⟹ 物件會離倉旅行，到了對方手上仍須自報。 **②射程**：commit 物件與 `.oo/staged` **都納入**（用戶理由：之後拆 Q-014 savepoint 會比較順）。 **③不拆**，補字面／換編碼／拿掉 hex／遷移與跨版本／GC 走訪一致全部同批。 | disc 028 §6；用戶 2026-08-07 開題、2026-08-26 裁定；偵察 `nlang-tools/docs/a_store_written_in_another_language_recon.md` | **不是紀元弧**〔量 2026-08-26，v0.35.0〕根物件位元組 428 B → 762 B 而 CAID 不變 ⟹ 編碼 ⟂ 身分。**紅線**：根 `932a9f9d…`／標準根 `7038e250…` 不得移動；(c-2) 標籤字面**必須標它而不加欄位**，否則 26 處全移動。**讀相容破壞是響亮的**：`ensure_supported_encoding` 對範圍外拒絕開啟 |

### 2.3.1 W8′ 偵察（量測日 2026-08-08，引擎 v0.12.0）

> 全部為直接量測。**沒有一項是讀碼得出的結論。**

| # | 量到什麼 | 數字／證據 | 後果 |
| :-- | :-- | :-- | :-- |
| **M1** | **存進去的 99.25% 不是使用者的資料，是內建命名空間** | `app: { k1: 1 }`（源 17 B）→ 根物件 **252,587 B**。頂層 `system` 佔 **61,922 B**／緊湊總長 67,981 B；`data.app` 只有 **508 B**。26 個內建模組（`Math` 11,327 B、`List` 9,550 B、`String` 7,989 B…）逐字存在每一次提交裡 | 目錄大小與內建表同階，與使用者資料無關 |
| **M2** | **`system` 參與 CAID** | `serialize_combo` 以 prio 1 收 `cv.system`；直接量測：同一份 `data` 加一個假內建 → digest 由 `b94b5915…` 變 `10c883f8…` | **引擎多一個內建，過去每一次提交的根 CAID 都會移動**。這不是 W8′ 造成的，是 W8′ 撞見的 |
| **M3** | **連整數字面值都以未強制 Thunk 存入** | `k1: 1` 存為 `Thunk{ expr: "1", closure: [1 幀] }`，CAID `20dd58c0…`；同一個 `1` 強制後為 `17564af7…` | **強制後存 ⟹ CAID 全面失效 ⟹ W8′ 是破壞性的**（L1 舊債，首次有數字） |
| **M4** | **n/ 印表機印不出實際被存的東西** | `Value::to_nlang` 的收尾是 `_ => format!("{:?}", self)`；11 個變體中 **Thunk／Code／Ref 三個掉進去**。實測輸出：`k1: Thunk { expr: Expr { kind: Atom(Int(1)), span: Span { start: 13, end: 14 } }, closure: [ComboVal { … RwLock { data: None, poisoned: false, .. } … }] }` | **與 W3′-a 同一缺陷類**（`{:?}` 洩漏實作表示），但這次在**印表機本身**。且漏的正是 `span` 與 `RwLock` |
| **M5** | 體積比（同一個根物件） | JSON pretty **252,587** ／ JSON compact **67,981** ／ `to_nlang` **22,269 B** | n/ 形是最小的，但 M4 使這個數字**還不能當結論**——它是印不出來的那版的長度 |
| **M6** | 反向不存在 | 有 `Value → n/ 文字`（印表機），**沒有 `n/ 文字 → Value`**。反向要經「剖析成 Expr → 求值」 | **讀取會變成執行。** 這是 O35 |
| **M7** | `legacy_fields`／`legacy_local` 在 `bn_serial`（prio 5/6）與 `lattice_sketch` 內，卻 `#[serde(skip)]` | 實測：填了之後 CAID `7d0e3849…`，JSON 往返回來變 `b94b5915…`（＝沒填的那個）⟹ `get_value` 的 CAID 檢查會拒收 | **目前不可達**（全樹只有這四處讀、**沒有任何一處寫**）。是陷阱不是活缺陷；**若哪天有人寫它，物件會在寫入當下就變成讀不回來的** |

**M1–M3 合起來說的一句話：** `.oo/` 現在存的不是值，是**值加上它的整個求值環境**，而身分也是按這個算的。
**改編碼不會動到這件事**——JSON 換 n/ 只換了 M5 那個數字。

#### M2 跨版本重算（量測日 2026-08-08）

實際建了四個舊引擎（v0.2.0／v0.2.55／v0.3.0／v0.7.0，git worktree + 獨立
target），對**同一份 `app: { k1: 1 }`** 各自 `init/evolve/commit`，比對根摘要
與 `system` 子樹雜湊。

| 版本 | 根 CAID | `system` 模組數 | `system` 雜湊 | `data` 雜湊（控制） |
| :-- | :-- | --: | :-- | :-- |
| **v0.2.0** | `5051b07e…` | **25** | `6e06c85a…` | `f6b25067…` |
| v0.2.55 | `6e8eae8b…` | 26 | `212ddd35…` | `1d442eae…` |
| v0.3.0 | `6e8eae8b…` | 26 | `212ddd35…` | `1d442eae…` |
| v0.7.0 | `6e8eae8b…` | 26 | `212ddd35…` | `1d442eae…` |
| v0.12.0 | `6e8eae8b…` | 26 | `212ddd35…` | `1d442eae…` |
| *控制：v0.12.0 但 `k1: 2`* | `01c0f5c9…` | 26 | `212ddd35…` | `d394095…` |

**結論分三段，不要合併：**

1. **機制屬實，且已隔離。** v0.2.0→v0.2.55 之間根 CAID 確實移動過，但那次
   `data` 子樹也變了（277→516 B），**不能歸因**。改以單物件消融隔離：取
   v0.3.0 的根，**只從 `system` 拿掉 `~%Effect` 一個模組**，摘要由
   `6e8eae8b…` 變 `40668b53…`；**控制（拿掉一個不存在的鍵）摘要不動**。
   ⟹ **多一個內建模組，單獨就足以移動每一次提交的根 CAID。**
2. **但它沒有在累積。** v0.2.55 → v0.12.0 **九個 minor 之間逐位元組相同**——
   內建表凍結了，`system` 雜湊十週未動。**這是下一次新增內建的風險，不是既成的損害。**
3. **`~%Effect` 是有史以來唯一一次。** 25→26 發生在 v0.2.x 內部。

**⟹ M2 的正確位置：不是 W8′ 的一部分，是 W8′-b（裁 O35）必須一起裁的東西。**
「存的是值加環境」與「環境進身分」是同一個決定的兩面。

##### M2 順帶量到的兩件事（都不在原本的問題裡）

| # | 量到什麼 | 證據 | 類別 |
| :-- | :-- | :-- | :-- |
| **M8** | **v0.2.0 的值物件，今天的引擎讀不出來** | `oo inspect` → `#object_undecodable: … invalid type: string "Pure", expected u8`。**控制：v0.2.0 自己的引擎讀得出來**。成因＝`EffectTag` 的 serde 形由字串改為 `u8`。斷點落在 **v0.2.1–v0.2.52 之間**（未細切；v0.2.55 已是新形），**而那整段都早於 `.oo/format` 的出現（v0.2.53, `374bd47`）** ⟹ **凡是自稱 format 1 的倉，編碼都是新的；format 1 不歧義** | 舊倉不可讀，但**不是標籤說謊** |
| **M9** | **今天的引擎會在唯讀路徑上，替沒有 `format` 檔的舊倉蓋上 `1`** | `storage.rs:136` 註解寫著「Every existing store is version 1; announce rather than refuse」——**這個假設是假的**。實測：對 v0.2.0 倉跑 `oo log`（唯讀），`.oo/format` 被創出來且內容為 `1`；同一個引擎隨即對該倉的值物件回 `#object_undecodable` | **寫下了一個假的裁決**，同 REAL_03 §6.6「裁決必須為真」之族；且**是在讀取路徑上寫別人的倉** |

**M9 的可滿足性已具備**：`ensure_format` 可改為「缺檔 ⟹ 探一個物件；解得開才蓋章，
解不開則報 format 0 並拒絕開啟」。**但這需要裁定**——`.oo/format` 的語義是
「這個倉宣稱什麼」還是「這個引擎驗證了什麼」。列為 **O36**。

| # | 問題 | 出處 | 依賴 |
| :-- | :-- | :-- | :-- |
| ~~**O37**~~ | **✅ 已裁(2026-08-09,用戶):視界參數不進歷史。** 理由:**要保存視界參數的唯一條件是 CAID 一致性,而那已經記在 CAID 裡**(SPEC_08 §3.2.1:`#blur` 的 CAID 必須含視界參數)。使用者若真要提交,**自己觀測出來再存到別的欄位**。原問: 七個 `~%Config` 旋鈕**一律提交失敗**。若該進，`~%Config` 就成為被提交宇宙的一部分，**它會進根 CAID**，且每個歷史節點各自帶著自己的資源上限；若不該進，那它就該**明說**「視界參數是工作階段的，不是宇宙的」，而不是回一句 `Commit failed` | W4″ 偵察 | 擋在 W4″ 動工前 |
| ~~**O38**~~ | **✅ 已裁(2026-08-09,用戶):直接修,不設過渡。** 原問: 今天 `timeout: 1` 之下一個 2.3 秒的運算跑得完；修好之後同一份程式會開始撞視界。**行為改變的是「本來會成功的現在會 ⊥」**——這比 CAID 移動更難察覺。是否需要紀元／旗標過渡 | W4″ 偵察 | 擋在 W4″ 動工前 |
| ~~**O41**~~ | **✅ 已裁(2026-08-09,用戶):`timeout` 的創世預設改為 `#_`(正無限／不設限)**,旋鈕型別放寬為 `非負整數 \| #_`。**`fuel` 除外**——它是唯一一個「拿掉之後觀測不再必定終止」的旋鈕(SPEC_04 §6 觀測是構成性的),**所以那不是特例,是有理由的界線**。拼法依 **SPEC_01 §2.6**:`#_` 是**序位**的最大元(正無限),`_` 是**格**的 Top(萬有)——該節的 WARNING 表正是在防這個混淆;`1..#_` 已是既有的無上界區間寫法。**如此 §6.0.2「旋鈕必須治理它所命名的閘」零特例** | W4″ 殘留 (c) | 併入 W4‴ |
| ~~**O42**~~ | **✅ 已裁並完成（v0.17.0）**：`#blur` 身分收 CHS 的條件，不收時鐘鹽、剩餘量或原始碼 span 等讀數；局部結果以 CAID 入 CHS。原問題與完整量測見 `ENGINE_SYNC` 的 O42 弧。**⚠ 原文另帶一鉤子「`.%partial` 觀測不到」，本結案不涵蓋它——已獨立為 O45** | 2026-08-09 發現；2026-08-10 結案 | **不得再當開放題**；結果回寫 REAL_03 §7.3–§7.4 |
| ~~**O43**~~ | **✅ 已完成（2026-08-10）**：CHS 六項強制參數（`fuel`／`strategy`／`max_branches`／`max_unification_depth`／`max_lifting_depth`／`max_pattern_nodes`）補入 **REAL_03 §7.3**。與 O39 同族：宣告了閘，兌現另計 | O42 工單 `a_snapshot_is_not_a_reading_handover.md` §34／§97 | 讀取端是否實作見 §6 Inbox（`max_lifting_depth`／`max_pattern_nodes`） |
| ~~**O44**~~ | **✅ 已裁：規格側筆誤**，引擎 `universe.rs:98` 白名單為準 | 同上 §240 | 無 |
| **O45** | **`%partial` 的可觀測性——身分與可見性是兩件事。** 引擎全樹沒有 `%partial`，`p.%partial` 依 SPEC_08 §3.2.2 第 5 款被座標吸收律吞掉、回傳 blur 自身。⟹ **本體今天被雜湊、被儲存，此外從不被讀**。若採「本體不必保留」的處置，**會關掉 O45**；handover §342–345、§389、§445 三處刻意保留了它的可能 | 同上 §159／§252／§342／§445 | **開放**。對 v0.19 實跑後才可裁；見 `WORK_QUEUE` §6 |
| ~~**O46**~~ | **✅ 已完成**：合併保留兩者，結構為正準排序的記錄集合（REAL_03 §7.3，代修 M4 後做到；探針 R-6） | 同上 §112／§186 | 無 |
| ~~**O47**~~ | **吸收不得改寫快照**（`unify.rs` 三個 blur 分支曾把另一側揉進來；探針 R-7） | 同上 §126／§187 | **⚠ 結案狀態未經確認**：R-7 隨弧交付，但 `ENGINE_SYNC` 未逐號記載。開 Q-001 前順手對 v0.19 確認一次 |
| **O40** | **一次性求值器該不該看見倉?** 〔量〕`oo run`／`oo eval` **連已提交的資料都看不見**(`y: 5` 提交後 `oo eval 'y'` → `_`),而 **REAL_01 §1.1 記載了不存在的 `--load`／`--commit`**。看不見＝要修規格並讓 `--load` 真的存在;看得見＝`oo eval` 讀不到已提交資料就是缺陷 | W4‴ (b′);設計面見 `meta/oo/cli_surface.md` | **不擋 W4‴ 的 (a)＋O41** |
| ~~**O39**~~ | **✅ 已裁(2026-08-09,用戶):不廢止——掛引擎帳,之後實作。** 「引擎還有許多未實作,這只是其中一個」;廢止前要先論證當初的設計沒有必要,而那個論證沒有做。**不在 W4″ 射程。** 原問: `max_lifting_depth`／`max_pattern_nodes` 寫入端俱全、讀取端為零，對應的錯誤碼亦從未被鑄。**「宣告了一道不存在的閘」與「沒有這道閘」在規格上是兩件事** | W4″ 偵察；同 W8′ M7 之 `legacy_fields` 類 | 擋在 W4″ 動工前 |
| ~~**O36**~~ | **`.oo/format` 說的是「這個倉宣稱什麼」還是「這個引擎驗證了什麼」？** M9：缺檔時現況直接蓋 `1`，而那個假設可以是假的。若是「宣稱」，缺檔就該是 format 0 並拒絕開啟；若是「驗證」，蓋章前必須先解得開一個物件 | W8′ 偵察 M9 | 與 O23（三類分開後幾個版本）同族 — **✅ 已裁並於 v0.22.0 交付**：宣告是倉的**宣稱**，缺席即拒絕，引擎不得在讀取路徑上寫下未經驗證的宣告（O53 同批）|
| ~~**O35**~~ | **✅ 已裁(2026-08-13,用戶)；2026-08-14 澄清態射面。** **澄清（非重新裁定）**:A 的「值」在態射上指**被引述的形**(結構面:`%rules` → `%code`＋`%closure`),**不是被算完的形**。理由:意義面(算子所是的映射)是數學物件,無窮定義域無妨,但**耐久層只能裝有限製品**,故必須存呈現而非列舉(SPEC_05 §3.3)。而 A 原本要防的是「讀取即執行」,**被引述的本體讀回來不會被執行**——`%code` 條款「引述不是求值」正是此意。⟹ A 的原意保住,態射不必被逼進一個不可能完備的表。 **＝ A，存值，讀取是解碼不是求值。** 兩項適用範圍明寫：**(i) 線上就是 store**——OODP 送同一種物件、`peer fetch` ＝ `get_value`，故線上亦為解碼不求值，不得另裁；**(ii) `Code`／`rules` 仍會出現在值形裡**——「不可求值」指不 force／不 apply／不沿路徑跑，把 `^.…` 還原成 `Value::Code` **是解碼**，那是引號不是 Thunk。**B 的代價成立，選它等於否定前面走過的路**；**C 不是第三條路**（明示標記不進身分＝span 同一病，進身分＝兩個語義相同的惰性成為兩個宇宙），它是「惰性活過重啟」答**要**時 A 的窄例外。**編碼形不在本裁定內**：A-json（磁碟仍 JSON、存強制後 Value、解碼器是既有 serde）與 A-nlang（印表機輸出當耐久形，需新 decoder ＝ O31／W8′-c）分開；**O35 擋在 O31 前面此判斷成立，是 Q-001 偵察初版把兩者併成一格** | W8′ 偵察 M6；disc 028 §8；裁定見 `nlang-tools/docs/what_the_store_keeps_recon.md` §7.3 | **破壞半徑＝紀元級**：已簽名的根、advert、`refine` 邊、對等點手上的同一 CAID、P4 的根釘皆在內 ⟹ 發布須連 OODP／簽名一起排 |
| ~~**O48**~~ | **✅ 已裁(2026-08-13,用戶)：CAS 物件裡的每一位元組，要嘛進雜湊，要嘛不該在磁碟上。** `get_value` 重算的是 `content_hash()`（`to_nlang`／`bn_serial` 投影），寫入的是 pretty JSON ⟹ **`span` 只是現在看得到的洞**；serde 的 `skip`／`default`、pretty 空白、以後任何「方便放進去但不進 digest」的欄都是同一個洞。**O42 對 `#blur` 裁的就是這句**，不是「blur 特例不要 span」。〔量 2026-08-13〕兩份 span 不同的位元組落在同一個 CAID 下而完整性檢查通過；合法載荷的語義竄改則被 `#caid_mismatch` 擋下 | Q-001 偵察 §2；O42 | **工單仍只先拔 span**。閉包同裁（下列 O49）；M7 的 `legacy_fields`／`legacy_local` 同族，剝 serde 洞時順手釘「不准寫」，掛同一張工單 |
| ~~**O49**~~ | **⚠ 2026-08-14 重述（用戶同意）——原措辭與 SPEC_05 §3.3 牴觸，不得照原文施工。** Q-010b 偵察量到:**強制不消滅閉包,它把閉包改名**(`Thunk{expr,closure}` → 態射 Combo 的 `%code`／`%closure`),而該閉包**是承重的**〔量:捕捉 `y` 的態射施用得 `15`;`fact 5` 得 `120`,成立正是因為 `fact` 在自己的閉包裡〕。且 `%closure` 有明文(SPEC_05 §3.3,其理由正是「跨視角傳輸並保持語義一致」——即 O49 原文想否定的那件事)。⟹ **拆兩條**:**(i)** 普通值 Thunk 的閉包由強制消滅,**併入 O35＝A 不另立**;**(ii)** 態射的 `%closure` **保留**,改為**僅捕捉本體的自由變數(MUST)**。**「不得含自身」這個曾提出的措辭已撤回**——遞迴算子的自我參照**是**其本體的自由變數,該判準自然保住它;**判準要寫性質,不要寫症狀** | Q-010b 偵察 §2–§3；SPEC_05 §3.3 | (ii) 移動身分 ⟹ **必須與 Q-010b 同一紀元** |
| ~~**O49-原**~~ | ~~**✅ 已裁(2026-08-13,用戶)：閉包不是耐久內容。**~~ M3 的閉包是**外層作用域的拷貝，而拷貝裡又含該欄位自己**；即便暫時維持可求值形，把整棵宇宙塞進每個欄位也**沒有語義理由——作用域可由根＋路徑重建**。與 span **同一價、同一便宜，且不需先決定惰性** | Q-001 偵察 §1.3 | 留著它，A 的體積勝利有一半是幻覺（使用者資料 516 B 裡很大一塊是這面鏡子） |
| ~~**O50**~~ | **✅ 已裁(2026-08-13,用戶)：前者——根不存 `system` 的內容。** 定位：**Epoch 0 之前的現在選前者沒問題**；Epoch 0 之後才真的面對紀元級破壞，而跨紀元的預想本來就走遷移證明（ORDER_01）。**重讀：這比較不算語義變更，是引擎自己的相容性問題**——但用戶同時指出「我們自己連格式都還沒訂好，所以**反向可能破壞語義**」。**⚠ 待確認的補強**：若根**只是不存**，同一個根 CAID 被兩個內建表不同的引擎讀到會**靜默得到兩個意思**，那就真的是語義事故；**根存 `system` 的 digest（而非 61 KB 內容）**可同時得到三件事——根縮 91%、**新增內建時舊根指向舊表故歷史根不再移動**、引擎讀不懂時**說得出「我沒有 `212ddd35…` 這張表」**。所需雜湊即 M2 量測所用的逐 `system` 子樹摘要，**不需新機制**。**✅ 2026-08-13 用戶確認：含 digest** | W8′ M2；Q-001 偵察 §7.5 | 隨 Q-010b 同一紀元交付 |
| ~~**O50-舊**~~ | ~~**`system` 進不進根、進不進身分（M2）——與 O35 同級的第二題，仍開放。**~~ 〔量 2026-08-13〕`system` 61,912 B ＝ 根的 91%，26 個模組十二個 minor 未累積；**但選 A 不會自動拿掉它**，印表機的 21,837 B 裡 `system` 仍在 ⟹ **A 做完 M1 的 99% 還在**。兩個選項：根物件只存 `data`（及該存的軸）、`system` 由引擎版本重建；或維持現況並接受**下一次新增內建是破壞性的** | W8′ M2（STATUS 明文「必須與 O35 一起裁」）；Q-001 偵察 §4 更正 E3 | **不是 Inbox 順帶項**。漏裁則 A 的主要動機落空 |
| ~~**O51**~~ | **✅ 已裁(2026-08-13,用戶)：同 §7.4。** 用戶 2026-08-13：不是全域開關，而是 **staged／工作階段：Thunk 可以活**（SPEC_04「未觀測即不計算」）／ **commit：提交是一次固化邊界**，寫進歷史的是觀測結果不是尚未跑的程式。如此 **SPEC_04 不必為了 store 改寫**，而「無限結構跨 `.oo/` 是什麼意思」的答案變成**「它不能當 head，除非你觀測出一個有限的陳述」**。用戶在考慮**是否延後與 savepoint（Q-014）一起規劃**。〔量 2026-08-13〕**該延後的只有一半**：`.oo/staged` **今天已是耐久檔**（638 B，含 Thunk×2／closure×2／span×2，`commit` 後清除），**但它不在 `objects/` 底下、沒有位址** ⟹ **O35／O48／O49 治的是 CAS 物件，`.oo/staged` 明文不在其內**；而「`staged` 成為真正可替換的物件」本來就是 Q-014 的內容 | Q-001 偵察 §7.4／§7.6 | **建議：那句「commit 是固化邊界」現在就裁**——Q-010 要有完成條件就必須先知道**何時強制**；**射程句同時寫下**，否則 Q-010 交付方會連 `staged` 一起剝掉 Thunk |
| **O52** | **✅ 已裁(2026-08-14,用戶)：標準根是一個東西,一個 digest。** 覆蓋整個 `root_with_system()`,不是 `system`／`types`／`rules` 三個各自的 digest——**它們一起變動**(換一個 engine build 就一起換)。起因:Q-010b 交付把六個軸都對標準根做「相同就拿掉」,〔量 v0.20.0〕根 72,555 B 中標準根佔 **67,329 B 分佈三軸**(`system` 61,912／`types` 5,186／`rules` 231),而交付**只給 `system` digest,另兩軸裸奔**。**規格依據:SPEC_09 §2.5「所有符合規格的引擎必須內建其定義並對齊其 CAID」**——該 MUST 的 CAID 欄整欄是 `0001...` 佔位,**今天沒有可對齊的對象**;標準根的 digest 正是它伸手要的那個東西。**後果(明文債)**:標準根裡裝了什麼從此是規格說了算,不再是建構順序的殘留 ⟹ `/add` 孤兒與三個型別都須有人明說為何在裡面 | Q-010b 驗收 §7.6;SPEC_09 §2.5;O50 | **O50 是它的特例,不撤銷**。隨 Q-010b 同一紀元交付;digest 不合須**指名拒絕**(R4 已釘) |
| ~~**O53**~~ | **✅ 已裁(2026-08-14,用戶):容器的編碼宣告,信它但不得替它寫。** 宣告存在就照它讀;**缺席就是「不知道」並拒絕開啟**,引擎**永不**在讀取路徑上寫下一個它沒有驗證過的版本。不採「先解一個物件才算數」,因為**「解得開一個」不等於「解得開全部」**——M1 第 5 步造出的混合倉(format-3 與 format-2 物件並存)恰好會通過那道檢查,而它正是最需要被捕到的那種倉。被篡改的宣告仍騙得過引擎,但**騙不到 CAS 層**:每個物件讀回來都要重算位址;且該攻擊今天就存在(M3:label=1 開得了 format-3 倉),本裁定不擴大它 | Q-011 偵察 §3、§7B;O23 | 與 O36 同弧;`ensure_format` 的「缺席→寫 1」須整個移除 |
| ~~**O54**~~ | **✅ 已裁(2026-08-14,用戶):只 hydrate 帶 sentinel 的根。** 判準是**根自己有沒有那個標準根摘要**——依 O23 的界線,那是「物件指名它所依賴之物」＝內容,不是「物件宣告自己如何被編碼」＝後設資料,故合法且可從物件本身讀出。**format-2 的根是自足的,不得被碰。** 不採「依容器的編碼宣告決定」,因為那把一個**可以從物件本身讀出的事實**讓給了外部宣告,而 O53 已裁該宣告不被驗證。順帶消滅 M4 的假 corrupt 指控 | Q-011 偵察 §4(M4);O23 界線 | 純缺陷修復,不移動身分 |
| ~~**O55**~~ | **✅ 已裁(2026-08-14,用戶)並於 Q-025 交付(2026-08-15):引擎得持有一張歷史標準根表。** 讀到舊摘要就用對應的那一張。代價明記:**引擎體積隨版本成長,且「舊標準根」成為永久包袱**——每一張都必須逐位元保存,否則所有依賴它的倉變成不可開啟。**這是 `/add` 孤兒與 SPEC_09 型別表兩則能動的唯一前提**;不裁它,那兩則等於「開弧即毀掉所有既有倉」〔量:多一筆標準根條目 ⟹ format-3 倉被指名拒絕、format-2 倉被誣告 corrupt〕 | Q-011 偵察 §5(M5);REAL_03 §6.8 自陳缺口 | **與宣告弧是兩件不同的事,應分弧**;它是 `/add`／型別表的前置 — **⚠ 2026-08-15 補量，本列所記代價兩項皆須修正**：(i)「引擎體積隨版本成長」〔量〕＝ **0.18%／版**（一份標準根 67,329 B 對 37.5 MB 二進位）⟹ **體積不是這件事的成本**；(ii)「舊標準根成為永久包袱、每一張都必須逐位元保存」在 **O56 之後不再為真**——標準根若由規格定義並公佈 CAID，引擎保存的是它所宣稱實作的那幾版，而非一條無限增長的私有尾巴。**本列的裁定本身不變**（引擎得持有一張表，且 L1 無論如何先做），變的是為何要付與付多少 | **交付形式**:`StandardRootSet`(digest → ComboVal),解析改為查表;〔量,三個真二進位〕只持有另一份標準根的引擎**指名拒絕**,同時持有舊那份的引擎讀出**逐字元相同**的 commit 位址。**新增一版＝`from_roots([…])` 多一個元素,解析邏輯零改動。****非破壞性**:身分軸未移動(同源碼根位址皆 `6a7b18de…`)、讀相容雙向成立。詳見 ENGINE_SYNC。
| **O56** | **✅ 已裁(2026-08-15,用戶)：走 L3，但先只立框架不填清單。** 起因是用戶把提問換了位置——先分清「這是目前引擎自己的鍋，還是未來任何實作（不一定是我們寫的）也會遇到」。〔量 2026-08-15〕答案是**三層**：**L1**（引擎只持有一份）＝引擎的鍋，**§6.8 規範的是缺席時的行為不是持有量**，故非規格所逼；**L2**（無取得機制）與 **L3**（無內容定義）＝規格的鍋。〔量〕「標準根」一詞在核心規格全文出現 **11 次，10 次在 REAL_03、1 次在 SPEC_00 版本表**，**無一條**說明引擎如何取得一份它不具備的標準根；而 SPEC_09 §2.5 的錨點欄逐字標為 `(Placeholder)`，五列皆 `0001…`–`0005…`，**其上方卻是一條「必須對齊其 CAID」的 MUST**。**承重推論：L2 與 L3 是同一個問題的兩個解，不是兩件待辦**——內容由規格定出並公佈 CAID ⟹「具備」＝「實作了公佈它的那個規格版本」，取得機制不必存在。**規格已走了一半 L3，L2 從未存在。** 〔量〕**L3 做得到，因為標準根是純資料**：26 個系統 seed、**251 個相異的內建名稱參照**、`Thunk`／`Code`／`closure` 各 **0** 次 ⟹ **規格供給值，實作供給名字背後的行為**，不需要規格描述任何實作。**已交付**：REAL_03 **§6.8.2 新設**（四條規範：標準根是版本綁定之物／多重具備／「不具備」改指沒有任何規格版本公佈過／實作不得自行擴充），含**未兌現宣告**；SPEC_09 §2.5 佔位符明文標註。**清單不在本裁定內**——`/add` 孤兒與 SPEC_09 兩表屬另一弧 | Q-025 偵察 §6.5；REAL_03 §6.8 自陳缺口；SPEC_09 §2.5 | **§6.8.2 第一條 MUST 引擎當下未兌現且無施工位置 ⟹ 依 WORK_QUEUE §2.4 已開 Inbox 一列**；第二條（多重具備）＝ Q-025／O55，已 Active |
| **O57** | **✅ 已裁(2026-08-16,用戶,四則):退回值不得是贏的那一個。** **A** 排序欄位**存在但不可解析** ⟹ **降到最後,記錄仍保留**(§5.1.2 把它框在**排序**而非**有效性**;已簽名的半邊可能完好,壞的是收方自己的斷言半邊)。**B** 熵取不到 ⟹ **具名拒答,不回任何樣本**(§4.3.5.1 自己的論證:把任意截斷擺得像正準答案才是偽造)。**C** 該拒答用 **`#rejected` ＋ 新 `%reason: #entropy_unavailable`**,**不新增 status**——〔量〕REAL_02 §130 逐字禁止狀態集增長(「理由集得增長;狀態集不得」),而 `#rejected` 的定義即「理解了但拒絕受理,區辨在 `%reason`」,故新理由正是該節規定的擴充方式。**D** 客戶端讀不出 `%status` 形狀時 ⟹ **`#peer_unknown_status`**,不得落到 `#conflict`(那會被記成 `#peer_refused`)。**承重量測**:同一個 `unwrap_or` 寫法在 `peers.rs` 內**往兩個相反方向倒**——`ttl`/`services`/`provenance` 輸(安全),`ts`/`received_at`/`admission_seq` 贏(缺陷);**`provenance` 是唯一有人選過方向的一處**(帶註解),其餘由巧合決定。**用戶另提的跨版本相容條款規格已有且更好**:REAL_02 表已列「不認得的 `%status` ⟹ `#peer_unknown_status`,不記入完整性紀錄」,理由逐字「一個比你新的節點不是一個被弄壞的節點」,引擎 `oodp.rs:1677` 亦已實作 ⟹ 不需新條款。**已交付並收尾**:REAL_02 §4.3.5.1 新增正面條款＋§3.2 共用理由表新列;TAG_REGISTRY 新增 `#entropy_unavailable` | Q-027 偵察 §1;REAL_02 §5.1.2／§4.3.5.1／§130 | 四輪驗收,詳見 ENGINE_SYNC |
| **O58** | **✅ 已裁(2026-08-16,用戶):標準根應拆為獨立欄位,不再 ⊕ 進根。** 〔量 v0.24.0〕今天根的 CAID 算的是**完整值**:`標準根 ⊕ { app: {k1:1} }` 直接雜湊 ＝ `6e5ad5e3…`,**與磁碟上那個位址逐字相同**;而**加上 sentinel 反而不等**(`abce8321…`)。⟹ **(i) 標準根的 67 KB 整份在每一個宇宙的身分裡**;**(ii) `__nlang_system_digest` 是一個寫在物件裡、卻不進雜湊的欄位**——這**推翻 REAL_03 §6.8.1** 逐字所寫「該摘要由值決定,故仍是位址的函數,**是內容而非後設資料**」,且正是 §6.7 所禁(**判例 `span`**)在論證它不會發生的那一節裡再度出現。機制:`storage.rs:486` `project_standard_root`(寫差集,並 `system.clear()`)／`hydrate_standard_root`(讀並集)。**拆開買到四樣**:①不具備標準根也**算得出位址**(今天連驗證都做不到);②sentinel 不再是未進雜湊的欄位 ⟹ span 判例消失;③標準根成為**有位址的物件**——〔量〕它今天有 CAID(`65f52e2d…`)卻**不是物件**(`oo inspect` → `CAID not found in local store`;新倉只有 2 個物件),**是系統裡唯一一個有位址卻定址不到的東西**;④與原設計一致(用戶:`_: ~%Math` 就是 import,**被匯入的東西本來就不該 ⊕ 進根**)。**代價**:一次格式面破壞(既有根位址移動,讀相容由 O55 多重具備接住)、`~%` 導航改為跨物件 | Q-028 偵察;REAL_03 §6.8.1／§6.7;`storage.rs:486` | **不在 Q-029 射程**。它是丁-b(標準根走 OODP `#fetch` 取得)的前置——〔量〕`#fetch` **已實作**(`oodp.rs:368`,`store.get_value(&hash)`),REAL_02 §99 逐字「物件自驗證」⟹ **丁-b 缺的不是協定,是本條**。另需一個求值層的「持有其名但未實作」原因碼(今天只有 OODP 的 `#peer_not_implemented`) |
| **O59** | **✅ 已裁(2026-08-16,用戶,兩則):拒絕必須蓋住寫,而說不出口時不得改口說別的。** **A** 節點持有物件位元組但接不回標準根時,線上回 **`#not_found` ＋ 新 `%reason: #standard_root_unavailable`**——狀態不增長(O57-C),且請求方的補救確實是「換一台問」;**不採 `#rejected`**(那是「不願意」,這裡是「做不到」);**不採「交出未 hydrate 的形」**(最有用,但那是 O58／丁-b 的射程)。今天回 `#not_held`,**是假的**——它持有那些位元組。 **B** shadow-scan(`universe.rs:1063`／`:1082`)遇不具備時 **中止整個操作並具名**,不得靜默 `break`／`continue`;**亦不得走 `record_integrity`**——那會把「我沒有那份標準根」記成對方名下的完整性事件,與 Q-027 剛修掉的 `#peer_refused` 同一個錯誤。**承重量測**:六個症狀收斂為**一個成因**——`crates/oo/src/main.rs:1538` 的 `Err(_) => Universe::new(None, engine.root_with_system())`,**逐字就是 §6.8 第三條 MUST NOT 被寫成 fallback**;`Universe::load` 本身是對的,`oo rollback` 亦正確(它自己再呼一次 `get_root(…)?`) | Q-029 偵察;REAL_03 §6.8;O57-C | 探針已預先校準(4 綠/6 紅);`TAG_REGISTRY` 登記由驗收方負責 |
| **O60** | **✅ 已裁(2026-08-16,用戶,兩則):型別要履行它名字的承諾;而「一個 panic 殺掉整台節點」是另一件事。** **A** `ContentHash::parse` **必須驗 digest 長度**(sha256 ⟹ 32 bytes／64 hex),v1 與 v2 兩支皆然(兩支是兩個各自的 `hex::decode` 呼叫)。**不採「只擋空」**——〔量〕`hash:sha256:v1:ab` 今天被當成合法 sha256 CAID 收下並查倉,長度從未被驗;**不採「在 `hash_to_path` 加防禦」**——那是修個案,`digest_path` 那一家仍需各自防禦,且「`parse` 接受了不該接受之物」沒被回答。**承重量測**:正確的線上答案**已經寫好且只是到不了**——`oodp.rs:371` 的 `(None, Some(raw)) => refuse(Conflict, "unparseable_caid")`,`parse` 一旦正確拒絕它自己就會生效;且〔量〕`oodp.rs` 全檔 `unwrap`／`expect`／切片／`panic!`／`unreachable!` **共 0 處**——協定層是防禦性寫成的,panic 來自它底下的儲存層 ⟹ **修法不得把防禦加回協定層**。 **B** **節點的 panic 隔離另立一弧**。〔量〕全樹**無** `catch_unwind`、**無** `thread::spawn`、**無** `panic::set_hook`(`crates/oo/src/` 與 `oodp.rs` 皆零),`main.rs:521` 的 `for stream in listener.incoming()` 使 handler 跑在 accept 迴圈自己的執行緒上 ⟹ **任何一個 panic 都是整台節點的死因,這一則只是第一個被找到的**。理由:「空 digest 被接受」是剖析驗證,「一個 panic 殺掉整台」是架構,**兩件不同的事**;且「協定層零 panic 形」是一個維持得住的性質 | Q-030 偵察;`value.rs:1987`;`storage.rs:476`／`:193`;`oodp.rs:371`;`main.rs:521` | 探針 2 綠／5 紅已校準。**射程外的孿生**:`storage.rs:193` `digest_path` 有同樣的 `[0..2]`,三個呼叫點只有 `object_exists_digest` 有 `len() < 4` 防護——**今天不可達**(輸入由磁碟目錄結構重建,恆為 2+62;`gc.rs` 另有 `is_hex64`),本弧不改 |
| **O61** | **✅ 已裁(2026-08-16,用戶):SPEC_08 §4.1「`%effect` 欄位必須參與 CAID」指的是 `.%effect` 讀得到的**有效值**(寫下的 ∪ 傳染的),不是只有寫下來的那個 meta entry。** **承重量測——`%effect` 今天有兩個家,只有一個在身分裡**:`ComboVal.meta`(`value.rs:808`)住**你寫下來的** `%effect`,`serialize_combo` 逐字含它(`bn_serial.rs:251`,meta 軸優先序 2);`ComboVal.effect`(`value.rs:812`)住**引擎算出來、傳染上來的**效果,**序列化完全不碰**(`Value::Atom(kind, _effect, _rank)` 亦丟棄)。〔量 v0.25.0〕`{ %effect: #pure, v: 1 }`／`#io`／`#nondet`／無標註 四者提交後根位址**全不同**(`d48f4deb…`／`d8ce838f…`／`7b86d440…`／`b25bfaf…`)⟹ **寫下來的那半已合規**;而同一個 `%builtin` 態射的 `#pure` 形與 `#io` 形雜湊**逐字相同** ⟹ **傳染的那半不合規**。**⚠ 本條同時更正驗收方先前一句過寬的話**:Q-028 偵察記為「效果不進 BN/ 雜湊(洞 A)」,**那只對一半**;精確的分界是上述兩個家。 **為何不能是 (a)(只指寫下來的欄位)——兩個理由**: **① 時代錯置〔量,git 日期〕**:「CAID 參與義務」條款為 **2026-04-01(`43ecd54`)**,而讓 (a) 說得出口的儲存詞彙——REAL_03 §6.7「不得存在未進入雜湊的欄位」(2026-08-13)與 §6.8(2026-08-14)——**晚了四個半月**。寫該條款時「meta map 的一個 entry 對引擎的 `EffectTag` 欄位」**還不存在可以指的東西**;四月的「欄位」只能是語義欄位。**(a) 是把八月的儲存區分讀回四月的語義條款。**(用戶指出;正是 **disc 023 兩地層**現象在**一個句子內部**的重演——條款是地層一「截面」,措辭「參與 CAID 的規範化計算」是地層二「儲存」形狀。) **② 本體(disc 023 §3 字典,本條的正面理由)**:`#pure` ＝「值完全由 CAID 決定,全域截面存在」;活動效應 ＝「光靠內容無全域截面」⟹ **效果標籤是這個值對『我的 CAID 夠不夠決定我』所作的陳述**。故 §4.1 的 MUST 不是「再多雜湊一個欄位」,而是**「這個位址決不決定得了這個值」這句話必須在位址裡面**。(a) 之下,一個**有**全域截面的值與一個**沒有**的值取得**同一個位址** ⟹ **那個位址斷言了一個其中一方並不具備的決定性**——與本紀元其餘各條同一形狀:一個機制對自己說了一句不真的話。⟹ **(b) 是唯一能讓效果標籤真的意謂 023 所說那件事的讀法。** **循環之疑已由規格自解**:§4.2.4 逐字「活動標籤**在觀測結果中**被轉化(固化)為 `#cached`」、§4.3 逐字「此操作**不影響原始定義的 CAID**,僅影響觀測結果」⟹ **規格早已分開『原始定義』與『觀測結果』**;(b) 落的是**定義側**,固化仍只在觀測側,不成循環。**這也解釋了引擎為何養出兩個家**:它把這個對的分野實作成「一個進雜湊的 map／一個不進的欄位」,然後**讓傳染值住進了不進的那一個**——分野對,東西放錯邊。 **射程因此比原估窄**:不是「把效果加進雜湊」,是**把傳染值從錯的家搬到對的家**;meta 軸本來就進雜湊,**不必新增機制** | SPEC_08 §4.1／§4.2.4／§4.3;disc 023 §3;`value.rs:808`／`:812`;`bn_serial.rs:68`／`:251`;git `43ecd54` | **代價 ＝ `WORK_QUEUE` §9.0 那個交點**:標準根有 **26 個非純態射** ⟹ 搬完其 digest 移動 ⟹ 每一個根的位址移動,**與 O58 是同一次身分搬遷,應同弧付一次**。 **本條不收兩件事**:(i) **#13 `%builtin` 偽造**——機制是名字偽造非效果傳染,SPEC_05 §3.3 另有 MUST NOT,獨立排;(ii) **#23 靜態守護看不穿態射應用**——本條改變它的形狀(傳染值落成欄位後,繭的比對看得到它)但**未裁它**,守衛要不要改動態仍待議 |
| **O62** | **✅ 已裁(2026-08-16,用戶):正準形是「欄位缺席 ＝ `#pure`」。** 寫入時**正規化掉**顯式的 `%effect: #pure`;只有**非單位元**的效果落成欄位。 **起因是一個為了問這一題才量出來的既有違反**:〔量 v0.25.0〕`{ %effect: #pure, v: 1 }` 與 `{ v: 1 }` 的 `.%effect` **皆讀出 `#pure`**(同一個可觀測),而提交後的根位址分別為 `d48f4deb…` 與 `b25bfaf…`——**兩個位址**。REAL_03 §6.7 逐字「**位元組為值之函數(MUST)**:對任一 CAID,其在 CAS 中的位元組必須**僅為所指之值的函數**」,自述為「同一個值的位元組必須唯一」;而**它們是不是同一個值,O61 已經裁過**(效果是每個節點都有的、關於自身 CAID 是否足以決定自身的陳述,`#pure` 是其中一個取值)⟹ **§6.7 今天被違反著。** **為何是 (i) 而非 (ii)「每個節點都帶 `%effect`」**:SPEC_08 §4.1 自己已經選了——「未標注者預設 `#pure`」;且 `#pure` 是效果格的**單位元**,而格論的重點正是**單位元不必寫**。(ii) 等於在每個算式裡寫 `+0`。**不採 (iii)「兩者是不同的值」**:那要重新解釋 §4.1 那句話,且與 O61 衝突。 **要吞的代價**:引擎會拿掉使用者寫下的 `%effect: #pure`。**那不是丟資訊,是去掉一個冗餘編碼**(同 `oo fmt` 已在做的事);不去掉才是丟東西——丟掉的是「一個值一個位址」。 **⚠ 兩條界線,工單必須逐字帶進去**: **① 「非單位元」不等於「非 pure」**——`#cached` 也**必須**落成欄位(它不是單位元,§4.2.4 的固化語義靠它)。判準是**格的 ⊥**,不是「純不純」。 **② 正規化只得在守衛之後、且只在有效效果確為 `#pure` 時發生。** 依 §4.1「顯式寫入的 `%effect` 優先於引擎標籤」,使用者得寫下一個**假的** `#pure`(傳染值為 `#io`)——那正是 §4.3 靜態守護要抓的謊。**若正規化在守衛之前就把那個欄位抹掉,正規化器就成了共犯**:謊被消滅而非被抓到。⟹ 順序是**先判定有效效果 → 守衛 → 僅當有效效果 ＝ ⊥ 時去欄位** | REAL_03 §6.7;SPEC_08 §4.1／§4.2.4／§4.3;O61;〔量 v0.25.0〕 | 併入 O58 同弧(§9.0 身分只搬一次)。**本條順帶關掉一個既有的 §6.7 違反**,該違反此前無人記帳 |
| **O63** | **✅ 已裁(2026-08-16,用戶):跨越 Q-032 的舊倉走「格式閘」,不走「規則隨標準根走」。** 拆開改變了根的**位址規則**(舊 ＝ `hash(標準根 ⊕ 使用者內容)`;新 ＝ `hash(殘差 ＋ 指名依賴)`),故舊物件必須在**容器宣告的編碼**上被辨識並以舊讀法讀取。 **起因是 Q-032 第一輪的跨版本矩陣**〔量 2026-08-16,兩個真二進位〕:舊引擎 `v0.25.0`(標準根 `65f52e2d…`)建倉,新引擎讀之——`status` 回 **`65f52e2d… (available)`**(**歷史列已加,`supports_standard_root` 為真**),而 `log`／`evolve`／`commit` 皆 **`#caid_mismatch`**:`requested 0ebe51f5…` 對 `recomputed b7025e4f…`。新引擎讀寫**自己的**倉正常(新標準根 `2da5b713…`)⟹ **新規則自洽,壞的只有舊→新**。 **承重推論:持有舊的「值」不夠,還要持有舊的「規則」。** O55 裁的表是 `digest → 標準根值`;本弧第一次讓它需要 `digest → 值 ＋ 讀法`。 **為何不是 (i)「規則隨標準根走」**:(1) 它會直接推翻 **§6.8.2 多重具備**逐字所寫「**新增一版＝ `from_roots([…])` 多一個元素,解析邏輯零改動**」——每加一代就多一條永久維護的解碼分支;(2) 標準根表是為**值**而設,不是為讀法。 **為何是 (ii)**:(1) **O23 已裁**「物件編碼由 `.oo/objects/` 這個容器宣告,不由 `.oo/format`,也不由物件自己」——這正是它存在的理由,而〔量〕**編碼軸自 2026-07-28 引入以來從未真的被撥動過**,本弧是第一次;(2) **REAL_03 §6.8 第四條 MUST 已預備此事**:「格式須先宣告(MUST)……使不認得本形式的引擎在**閘上**拒絕,而非按舊讀法解讀出一個不同的根」——**本次失敗正是它的鏡像:新引擎按新讀法解讀出了一個不同的根** | Q-032 第一輪驗收;O23;O55;REAL_03 §6.8 第四條／§6.8.2 | **本條不改 O55／O56**:歷史標準根**值**仍須持有(C3 看守),格式閘另治位址規則。二者是兩層,不得互相取代 |
| **O64** | **✅ 已裁(2026-08-20,用戶):`str` 這個概念的系統模組正準拼法為 `~%Str`,引擎改名。** 〔量 v0.26.1〕同一個概念今天有**三種**拼法:型別 `str`(`@str` 的 `%name` 讀回 `"str"`)、builtin registry 前綴 `str.`(33 筆)、而模組叫 `~%String`。**既定模式是「TitleCase 的型別名」**:`list`→`~%List`、`complex`→`~%Complex` 皆兌現;`str`→`~%String` 是**唯一的例外**——`String` 是 `string` 的 TitleCase,而**沒有 `string` 這個型別**。SYNTAX_00 §49 的命名規則亦逐字要求 `~%` 一律 TitleCase 單首字大寫。〔量〕規格全文 `~%Str` 出現 **1 次**(SPEC_09 §5.1 核心系統組件清單)、`~%String` **0 次** ⟹ **規格站在 `Str` 這邊,偏離的是引擎** | SPEC_09 §5.1;SYNTAX_00 §49;〔量 v0.26.1〕 | **動標準根內容 ⟹ digest 移動**。依 `WORK_QUEUE` §9.0 與 O65／O66 及 `%differential` 改名**共乘同一次身分搬遷** |
| **O65** | **✅ 已裁(2026-08-20,用戶):`/add` 自頂層規則軸移除。** 它是**頂層唯一的算術態射**,其餘同族(`/sub`／`/mul`…)只存在於 `~%Math` 內 ⟹ 頂層那一個是不一致而非設計。用戶原述其來歷為「為了讓 readme 範例能動,暫時做的機制」,而最初的設計想法是**不做任何預設的態射,全部放到 `~%` 底下**(交集進路徑欄位即 `import`,SYNTAX_05 §50 逐字)。 **代價已量**:〔量 v0.26.1〕符合性語料**只有 1 個向量**依賴頂層 `/add`(`conformance/L2/06-currying.n` 的 `out: (/add 3) 4`),改寫為 `~%Math./add` 即可;規格 12 個檔提到 `/add`,多數為 `/` 前綴的舉例(如 SYNTAX_00 §21 的 `/add: x y -> x + y`,那是**使用者定義**的示範,不依賴標準根)。 **⚠ 不與「使用者能否定義自己的 `/add`」混為一談**:後者是 v0.26.1 的查找順序修正已解,與本條獨立 | REAL_03 §6.8 自陳缺口第一項;SYNTAX_05 §50;〔量 v0.26.1〕 | 同 O64:共乘同一次 digest 移動。**須同時改寫該符合性向量** |
| **O66** | **✅ 已裁(2026-08-20,用戶):移除引擎本地合成的 `~%Official` 空殼。** **本條不是新裁定,是兌現一條既有的 MUST NOT**:SPEC_13 §135 逐字——引擎於本地合成一個同名節點並自任為其成員「**不是該物件的退化版本,而是另一個東西冒用其名**」,且指定了正解:「本地觀測該路徑得 `⊥ #missing_key`,即『本引擎不持有該全域物件』之誠實答覆」。 〔量 v0.26.1〕現值為 `{{ }}`(空的閉合 combo,CAID `27c48162…`),`~%Official.anything` 才回 `#missing_key`;而真正缺席的模組(控制組 `~%NoSuchModule`)回 `_` ⟹ **路徑本身解析成一個被合成出來的物件,不是誠實的缺席**。 **驗收方先前的誤判記此**:Q-033 偵察與其複驗一度把它列為「待裁項」,實則 SPEC_13 §135 已裁並寫了理由 | SPEC_13 §135;ORDER_01 §117／§88;〔量 v0.26.1〕 | 同 O64。**耦合已量**:該空殼在標準根的 26 個模組裡 ⟹ **它在每一個根的身分裡**,移除必然移動 digest,**不能當獨立清理做** |
| **O67** | **✅ 已裁(2026-08-21,用戶):位址字面的正準拼法＝ `_{…}`，界定符之爭結束。** **位置早已定**(§3)：動 **anchor 家族**——`_.` 是這個宇宙的根，`_{位址}.` 是那個宇宙的根；文法上只是 `anchored_path` 多一個分支，**前綴本體論一個字不改**。**`_"…"` 出局**(§7.1／`quoted_names.md` Q1：引號專指純粹的名字，而位址不是名字——「一個概念一種正準拼法」)。 **用戶所收的是唯一還在的代價**：〔量〕`_{sha256:426d}` 今天**就是**一個欄位為 `sha256` 的 combo，定為 compound-atomic 之後對解析器不歧義，但**對人眼像一個假的 combo**。 **三件量測讓這個代價縮到可收**：(1) **空白那一項不進取捨**——〔量〕`_ "…"` 與 `_ {…}` 一樣靜默，兩個候選一樣爛，故它從來沒有在分勝負；(2) **「萬物皆 Combo」碰不到它**——SPEC_03 §127 管的是**複合字面量的可觀測性**，而 `_{…}.` 是 **anchor 不是字面量**(規則要求尾隨的 `.`，`_{…}` 單獨永遠不是一個項)，且〔量〕既有家族本來就不是 combo(`@{1}`→`1` 是原子；`#{…}` 的內文法是 `poset_node`)；(3) **compound-atomic 之下 `:` 根本不會被讀成欄位**，它是惰性位元組——像欄位是給人眼看的，而 §4.1 已裁「演算法必須留」，於是「演算法 → 摘要」正好就是欄位的形狀。 **同批必須付的一筆**〔量 2026-08-21〕：`_ {addr}.Math` 靜默回 `_` 的正當性完全靠**「運算元未知 ⟹ 未知」**那一列，而該列**規格無條文、conformance 零向量**，卻已有三處引擎測試靠它綠著(兩處逐字寫 "Use Top as identity")⟹ **D.1 落地時須同批補一條 SPEC 條文＋至少一個 L1 向量**，否則第二個實作回 `⊥ #conflict` 會通過全部向量而給出另一個答案(與 Q-033 D1「一個名字兩種解析」同形) | `meta/oo/address_literal.md` §3／§4／§7.1／§7.2／§7.2.1；`quoted_names.md` Q1；`meta/oo/commit.md` §4.4 弧 D | **弧 D 第 1 項結案，第 2、3 項未動**：`init` 的形狀(〔量〕`oo init` 這個子命令**今天不存在**，宇宙由第一次 `evolve` 隱式建立 ⟹ 是「造一個」不是「改一個」)、以及「以位址取值」該叫什麼住哪裡(今天是 `~%Discovery./fetch`，標 `#io`，實為本地 CAS 讀取)。**本裁定不含文法實作**——那要另開弧 |
| **O68** | **✅ 已裁(2026-08-21,用戶):Q-035 五題,四題定案一題留白。** **Q1 ＝「包」是目標,但兌現方式是引用不是拒絕**——使用者宇宙不該有 `meta.builtin`,而這句話要靠「合法路徑根本不產生它」成立,不是靠一條會誤傷 `_: ~%Math` 的拒絕。〔量〕宇宙**內部**的別名已經是引用(case1／case2 兩個綁定共用一份,`builtin` 仍只出現 1 次);複製只發生在**跨越 `~%` 邊界**那一步(使用者根:對照 287 B ／ `add: ~%Math./add` 479 B ／ **`_: ~%Math` 11,625 B**,而 CAS 去重救不了,拷貝在根**物件內部**)。**本題掛弧 D(O67)之下,本弧不裁死 import 語義**;價碼已量過——同一招 **O50(v0.21.0)** 做過一次(根改存 `system` 的 digest,72,555 → 1,511 B,代價＝**破壞性 #14,身分軸**)。 **Q2 ＝框格錯誤,已拆**(開單方的錯):parse／evolve／commit 是「值**何時被寫下**」,apply 是「名字**何時被使用**」,而 SPEC_05 §3.3 那句話本來就是兩半 ⟹ **Q2a 寫入層(仍未裁)** 與 **Q2b 派送層(＝ Q3)**。〔量〕兩者不是替代:先提交一個偽造,再 evolve 一個**完全不含 `%builtin`** 的源碼 `out: boom 7` → **exit=7** ⟹ **任何放在 parse 的拒絕都會被一分鐘前提交的宇宙繞過**。(⚠ 同一實驗改用 `oo run` 回 `_`,那是 Q-018 不是拒絕。) **Q3 ＝ B,不再認名字,而憑證來自脈絡不是來自值。** 〔量〕`ComboVal` 十個可序列化欄位**無一指向標準根** ⟹ 「該**值**依賴哪個標準根」問不出來;但〔讀 `lib.rs:101`〕`EvalContext` 有 `pub standard_root`,而〔讀 `lib.rs:3064–3066`〕派送那一行**手上就有 `ctx`** ⟹ **不必新造機制、不必新增欄位、不必動身分**,規則形狀為「`%builtin` 之名只有在**這個宇宙的**標準根確實 project 了它時才派送」。**這與 O55 對齊**(`ctx.standard_root` 正是被指名的那一份 ⟹ 舊倉自動得到舊的名字集合)。**威脅模型同時被更正**:〔量〕`registry(245) ＼ 標準根(251) = ∅`,且七個危險名全部路徑可達 ⟹ **今天偽造拿不到 `~%` 沒給的能力**,它是**課責**缺陷不是提權缺陷;真正的洞是〔讀 `lib.rs:758/3066`〕**派送從不查根**——證據是那 6 個死名(`math.bitAnd` 等在根裡、不在 registry,故 `~%Math./bitAnd (1,2)` = `⊥ #conflict`)⟹ **根與 registry 是兩個獨立集合,今天恰好嵌套是巧合不是機制**,故 O55 的版本綁定對 `%builtin` today 失效。 **Q4 ＝ C,讀取路徑不拒。** **A 被排除的理由是原則不是成本**:SPEC_05 §110 逐字「本欄是引擎向標準庫投影自身實作的方式」⟹ 標準根裡的 `%builtin` **依定義永遠合法**,A 會拒掉一個定義上合法之物(且〔量〕`7038e250…` 自己 255 處命中)。**B 罰錯人**:〔量 §3.2〕`add: ~%Math./add` 與手寫偽造**逐位元組相同** ⟹ B 把合法拼法罰得跟偽造一樣重,而且罰的是**舊倉**。**A 與 B 破的都是可讀性**(§6.8.1 的區分:可重現性破 ⟹ 歷史還在;可讀性破 ⟹ 歷史打不開)。**C 之所以安全,是因為 Q3.B**:〔量〕已提交的偽造確實會走到 `lib.rs:3066`,故讀取路徑再拒一次買不到 Q3.B 沒買到的東西,只多付一份可讀性。**同族先例 ＝ O53**「引擎永不在讀取路徑上替它寫」——閘裝在**行使能力**處,不裝在**看見資料**處。**C 附帶義務**:讀取路徑**得**(SHOULD)於解出含 `meta.builtin` 的使用者物件時留下可查詢記號,**不得**因此拒絕開啟或改寫 ⟹ 日後要升級成拒絕時手上已有數字。 **Q5 ＝ A,本單不收效果閘。** 〔量〕`io.write_file`／`process.exit` **無 `--grant` 仍生效**,故 grant 今天連半個閘都不是;`disc.connect` 的 grant 屬 SPEC_08 §6 另一條線 | `nlang-tools/docs/a_name_is_the_only_credential_recon.md` §3／§4／§4.1;SPEC_05 §110–§115;REAL_03 §6.8.1;O50／O53／O55／O67 | **仍未裁 ＝ Q2a(寫入層)**:今天無歧義判準(合法別名與偽造同位元組),故 Q2a 的乾淨解也在弧 D 下游——弧 D 之後任何使用者資料裡的 `meta.builtin` 都無歧義是手寫的,parse 層拒絕才沒有偽陽性。**implementation 工單本輪只能做不受 import 語義影響者 ＝ Q2b／Q3.B 的派送閘 ＋ Q4.C 的記號**;落地時另有兩個前置:(i) `ctx.standard_root` 預設為空(`lib.rs:182`),須把「沒有標準根」與「標準根裡沒這個名字」分成兩個具名答覆(Q-031 同族);(ii) 那 6 個死名會通過名字檢查再倒在 registry 查表,兩個成因走同一個 `⊥` 出口——補實作或從標準根移除(**後者移動標準根 digest**) |
| **O69** | **⚠️ 已於 2026-08-23 由 O72 部分撤回——「`_: X` 是 mount」這一句不再成立;本條其餘量測與觀察仍有效,見 O72。** ~~**✅ 已裁(2026-08-21,用戶):回退格成立——`_: X` 就是「解析不到的名字掉進 X」,而且它是 mount 不是 cp。** **起因**:弧 D 第 2 項討論「複製的時機點」時撞到——〔量 v0.28.0〕**`_: X` 今天什麼都沒有做**(`_: ~%Math` 之後 `add`／`/add`／`_.add`／`_./add`／`sub`／`/sub`／`out: _` 全部回 `_`;使用者自訂亦然,頂層與巢狀皆然;對照 `k: 7` 得 `7`),**而它照抄不誤**(使用者根 11,625 B,對照空根 287 B,且 `x: ~%Math` 逐位元組相同 ⟹ **`_` 在複製上完全不特別**)。 **為何裁成立**:整個設計——無 import 關鍵字／交集即匯入(SYNTAX_05 §3)／能力放在 `~%` 底下／位址字面拼裝模塊——**只有在 `_: X` 真的做事的前提下才連得起來**。今天不做事的那個版本,是把一個**語義寫法**實作成了一個**複製動作**。 **承重的形狀是 mount／overlay 不是 cp**(用戶類比,經量測支持):名字在此找不到才掉進掛載點,**查找時才解**;本地欄位遮蔽掛載內容,而遮蔽方向 **v0.26.1 剛裁過**(投影順序)⟹ 回退格要接的就是它。 **n/ 比檔案系統強的一格**:掛載點是**內容定址**的——Unix 的 mount 指向可變的裝置,`_: _{位址}` 指向一個不可能變的 CAID ⟹「這個宇宙掛了什麼」是**可驗證的事實**而非須信任的宣告;此即 REAL_03 §6.8.1「物件得指名其依賴,該摘要是內容而非後設資料」。 **兩件同時量到的事實**:(i) **根在 `init` 之前就存在**——全空目錄無 `.oo/` 時 `_.` = `{}`、`~%Math./add (1,2)` = `3` ⟹「filesystem root 何時建立」在 n/ 的答案是**不建立,它一直都在**;要決定的是**歷史從哪裡開始**,不是 tree 何時被造出來;(ii) **`oo eval`／`oo status`／`oo log` 三個唯讀命令都會建出 `.oo/`** ⟹ **每個命令都在偷偷 init**,這與 **O53**「引擎永不在讀取路徑上替它寫」同族(O53 當時講的是 `.oo/format`,此處是整個目錄),另記 Inbox~~ | `meta/oo/address_literal.md` §8;`SYNTAX_05` §3;`lib.rs:3110` 既有 `it`／`_` 回退;v0.26.1 投影順序;O53／O67／O68 | **不要先實作**:它同時牽動裸名解析、遮蔽序與 `lib.rs:3110` 那條既有回退,三件都有前科。 **`init` 的形狀待此裁定落地後再談**——弧 D §4.2 原本的 A／B 兩條路已因本裁定塌掉一半(A 想避免的『引擎替你決定』今天已是既成事實;B 今天沒有東西可引入);引用式 import(O68／Q1)之下 `_` 裡放的是位址,**「複製的時機點」問題整個消失**,屆時「要不要獨立一個 commit」只剩一個純粹目的:**要不要讓那一步可以被替換** |
| **O70** | **✅ 已裁(2026-08-22,用戶):弧 D 第 3 項——以位址取值。五句。** **① 正準形是 `_{位址}.`,不新增態射。** 依據 §1 的判例:既有機制不是「呼叫」而是「宣告依賴」——標準根由根裡一個**寫進雜湊的欄位**指名(REAL_03 §6.8.1 稱之「指名依賴的欄位」),位址字面就是把它從引擎專用推廣到使用者可寫;且**只有語法能滿足 C2(靜態可見)**,態射呼叫把位址藏在字串引數裡,不跑起來就掃不到。 **② 位址是純資料**,持有與傳遞無效果(REAL_03 §6.8.1 逐字:該摘要**是內容而非後設資料**)。 **③ 解析(位址→值)貢獻零效果**,因為同一個 CAID 恆得同一個值——**這就是內容定址的定義**;§4.3 已裁「純;可得性不是效果」。 **④ 外層的效果來自被取回之值本身,不來自取的動作。**(**本句為用戶在裁定當下補入**;驗收方前一版措辭「fetch 取回的值仍然是純的」**是錯的**——純的是解析,不是被解析出來的東西。) 〔量 2026-08-22〕`identify (~%Math./add)` 與 `identify (~%Math./random)` **都是純資料的字串**,而 `(~%Math./add).%effect` = `#pure`、`(~%Math./random).%effect` = `#nondet` ⟹ **位址是不是純的,與位址代表之物是不是純的,是兩件事**;此前一直被混在一起談。 **⑤ 撥號是能力不是效果**,歸 SPEC_08 §6.1.4 能力格與 REAL_02 §4.2.6.1(`TAG_REGISTRY` 逐字「拒絕**必須先於連線嘗試**」);`~%Discovery./fetch` **保留但射程收窄為「位址是執行期資料」的取得**(那是 `_{…}` 依 C2 **故意做不到**的事,也是 Discovery 這個字唯一誠實的地方),且**不得再以 `#io` 代行能力閘**。 **⟹ 分界線不是「本地／遠端」,是「位址是源碼還是資料」。** **今天的量測(v0.28.0,兩環皆實測)**:(i) 取一個**純本地**的值會把外層染上 `#io`(`{ imported: ~%Discovery./fetch "<本地位址>" }.%effect` = `#io`,`disc.rs` local 分支就 return,零封包);(ii) 效果進位址(`{ v: 1 }` / `{ %effect: #io, v: 1 }` / `{ %effect: #pure, v: 1 }` 三個不同位址)⟹ **匯入會改變匯入者的身分,而改變的原因不是匯入了什麼,是用了哪個動詞去拿**。④ 自動解掉它:匯入 `#pure` 之物身分不動、匯入 `#nondet` 之物身分變動,**而那是對的**。 **與 O61 的關係**:O61 說「效果標籤是這個值對『我的 CAID 夠不夠決定我』所作的陳述」;本條說「**位址決定值**是內容定址的定義,故解析必然純」。兩句不衝突,講的是不同的層 | `meta/oo/address_literal.md` §1／§4.3／§4.4;REAL_03 §6.8.1;SPEC_08 §6.1.4;REAL_02 §4.2.6.1;disc 023 §3 字典;O61／O67／O69 | **⑤ 有破壞性,射程未量全**:改 `fetch` 的效果標籤會移動每一個用過它的宇宙的位址。〔量 2026-08-22,初步〕conformance **0/143** 命中;引擎測試 **14 檔**;規格條文 **5 檔**(REAL_01／REAL_02／SPEC_08／TAG_REGISTRY／CHANGELOG)。**既有倉受影響數未量**,落地前必量。 **弧 D 三項至此全部裁完(O67／O69／O70),但一行程式碼都還沒有**——文法、回退格、效果三者各自是弧,且 O69 已標「不要先實作」(牽動裸名解析、遮蔽序、`lib.rs:3110` 既有回退) |
| **O71** | **✅ 已裁(2026-08-23,用戶):兩件同源——`#standard_root_unavailable` 入登記簿,而登記簿改名 `TAG_REGISTRY`。** **① `#standard_root_unavailable` 得為語言層的 `%cause`,且必須登記。** 起因:弧 D-1 的 repair 讓它成為 `BottomCause` 的一個變體,而 `spec/CHANGELOG.md` §226「明文未做」逐字寫著它**刻意未登記**、「**REAL_02 §3.2 是這些理由唯一的家**」——那是 OODP 的**線上**理由表。**先例已在表內**:`#caid_mismatch` 標為「原因·**線上／⊥**」,一個碼跨兩層且明說。「持有但打不開」與「內容與 CAID 不符」是同族事實,兩層都會遇到,**用同一個名字說同一件事比造第二個名字誠實**。⟹ 判準因此定下來:**收不收一個 OODP 的 `%reason`,看它在不在兩層都成立,不看它從哪裡來**——這同時回答了本節既有的「`TAG_REGISTRY` 收不收 OODP `%reason`」一列。 **② 改名 `TAG_REGISTRY`。** **理由不是美觀,是檔名與內文互相矛盾**:〔量 2026-08-23〕該檔 **65 個碼**中 **57 個「原因」／4 個「狀態」／4 個「來歷」／1 個操作者／1 個實作,一個叫「錯誤」的都沒有**;而第 15 行逐字「來歷……(**非錯誤**)」、第 23 行升為 **MUST NOT**、§78–79 兩個標籤各自標著「非錯誤」。**矛盾出現在同一頁上。** **代價是實質的**:一個實作者看到檔名,會把 `#static_cycle`／`#no_coordinate` 這類**合法值的出身**當成要處理、要上報的東西——**名字在替一條寫下來的 MUST NOT 開後門**,與 **APP_02 §0**(2026-07-28,GPP)所立命名規則同族:*一個借來的名字使一條已存在的規則被跳過*。 **新名取自該檔 §0 自己的話**:§0 標題為「**三條軸與六個載體**」並自稱「本節是這些詞彙的**唯一定義處**」⟹ 它是**標籤登記簿**,不是錯誤代碼表。 **為何不用 `CAUSE_TAG`**(驗收方初議):只蓋 57/65,會把狀態與來歷那八個排除在外——**而那八個正是最容易被誤處理的八個,因為它們不是壞事**。 **為何不用 `TAGS`**(用戶):會被讀成「介紹 `#` 怎麼使用」的教學章,而非登記簿。 **旁證檔名確實使人找不到它**:§0 逐字記載 SPEC_08 §3.2.1 與 REAL_02 §3.2.1 **各自獨立地發明了同一個二分,連節號都相同** ⟹ 兩處作者都沒想到去一個叫 Error Codes 的檔裡找他們要的東西,**因為他們要找的不是錯誤** | `TAG_REGISTRY` §0／§15／§23／§78–79;`spec/CHANGELOG.md` §226;REAL_02 §3.2／§3.2.1;SPEC_08 §3.2.1;APP_02 §0 命名規則;O67／O70;弧 D-1 repair 1 | **改名是機械弧,另開工單,不在本裁定內做**。〔量 2026-08-23〕交叉引用 **135 處**:spec 內文 32(10 檔)、meta 55、引擎原始碼 42、conformance 出處欄 6。**兩處不是機械的,須逐一讀**:(i) 內文 **14 處「錯誤」**——有些是真的在講錯誤,有些是被檔名帶著寫的;(ii) conformance 出處欄那 6 處會動到 **REAL_05 §3 規範索引**,依該檔紀律屬規格變更。 **另有兩個廢止變體待清,不在本裁定內**:〔量〕`BottomCause::InvalidPath` 與 `::SemanticEclipse` **建構處皆為 0**(`TAG_REGISTRY` §108／§221 已明文廢止),與 `PathAnchor::Current` 逐字同形——**退場時只退了建構端**;須先量「移除變體會不會動身分」再併進下一次身分搬遷 |
| **O72** | **✅ 已裁(2026-08-23,用戶):不要 overlay 這一層——匯入走語言自己就能滿足的路。** **① `_: X` 不是掛載點,O69 的核心一句因此撤回。** 弧 D 第 2 項(回退格)**解散,不再是一個弧**。 **② 匯入的正準寫法是 `...`(spread)。** 〔量 2026-08-23〕`...` 不是 cp,它是**打開封閉之後的 meet**:`~%Cond & ~%Math` 是 ⊥ 而 `{ ...~%Cond, ...~%Math }` 有 **52 個鍵**且兩邊都通;`is_cocoon({...~%Math})` = `#false`;`SPEC_03` §69 早已明講它吃「Combo **或 Cocoon**」。**它保住格**:`{...~%Math, one: 99}` 與 `{one: 99, ...~%Math}` **給同一個 ⊥**——可交換、不覆寫、碰撞即 ⊥。效果逐欄位保留(`/random` 兩邊皆 `#nondet`),容器那層由 §16 的 cocoon 效果隔離擋著。 **③ 多重匯入不需要順序。** 〔量,兩檔真倉〕`a.n` 寫 `_: { ...~%Math }`、`b.n` 寫 `_: { ...~%Cond }`,兩次 evolve 後**座標的 `rules` ＝ 51,`add` 與 `if` 皆在**(Math 48 rules＋1 data,Cond 3 rules)。同名重複賦值即交集,`...` 已把兩邊打開⟹ **兩個 open combo 的交集正常合併,無序、可交換**。**「多重匯入需要序」這個動機因此消失**。 **④ 覆寫不進值語言。** 交換律與覆寫不相容(meet 可交換冪等,「近的贏」兩者皆非)⟹ 要覆寫就要全序,而全序就是 list。**本裁定選擇不要覆寫**:你 import 了 `~%Math` 又自己定義 `one`,那就是你跟庫對 `one` 不同意,**⊥ 是誠實的答案**。 **⑤ 與位址字面合成完整。** 〔量〕`{ ..._{sha256:…}.lib }` **通**,展平該位址底下那個座標⟹ **匯入＝spread 一個 `~%` 模組或一個位址**,全部已在語言裡,不新增拼法。 **⑥ 選項 C 因此收縮**:`_:` 只剩**預設分支**一個意思,C 的「值是 combo 就往裡找」不必做。 | `meta/oo/d2_what_underscore_means_brief.md` §4.5–§4.11;`SPEC_03` §69／§15.2／§16;SYNTAX_03 邊界 #2;O67／O69(部分撤回) | **⑦ 引擎既有的查找順序不是覆寫,是路徑決定的局部脈絡**(2026-08-24 結)。§4.7 量到先查 `ctx.root` 再查 `ctx.standard_root`——這不是一層蓋住另一層,是**求值路徑決定了你看到什麼**:從當前視角反推回去,路上最近的東西自然先被看見。標準庫是根所指名的脈絡(O50／Q-032),不在根裡面,所以從根往外看時它在更遠處。**SYNTAX_05／SPEC_09 改寫因此解鎖。** **同批要做的規格工作**:(i) `SYNTAX_05` §3 與 `SPEC_09` §5.2 的 `_: ~%Cond` 範例改為 `...`(兩處今天都跑不動);(ii) `...` 的三件事入規格——打開封閉性／可交換且碰撞即 ⊥／效果逐欄位保留(Inbox 已列,§2.5 第三例);(iii) `_:` 作為預設分支入規格(SPEC_07 §89 只寫了值不是鍵)。**皆需補 conformance 向量**,今天全部 0 命中 |
| **O73** | **✅ 已裁(2026-08-25,用戶):舊倉原地不動,遷移須明示,且遷移只動容器不動根。** **① `commit` 不得改動任何形式宣告。** 〔量 2026-08-25,v0.33.0＋v0.20.0 兩個真二進位〕現行 `commit` 把 `.oo/format` 由 `2` 改寫成 `layout=2` 並新增 `objects.format`;**切開來測,鎖住舊引擎的只有 `format` 那一行**——留著 `objects.format` 舊引擎照讀,而新引擎讀裸整數 `2` 也完全正常。 **② 寫進 pre-sentinel 倉的根必須維持自足**,不指名任何 digest ⟹ **幽靈 digest `47dc540c…` 從此不會被寫下,而不是被補上一個空表**。(該常數已驗為空標準表的雜湊:兩個內容無關的舊倉磚化後得同一個值,且 `objects/sha256/47/dc540c…` 不存在。) **③ 遷移須明示**(`oo migrate` 之類),且動手前必須說明「此後舊引擎打不開」。**④ 遷移只推進容器的佈局宣告,不動任何根** ⟹ `REAL_03` §6.8「以單一摘要指名(MUST)」**不適用於未進 sentinel 形式的根**,故不需要儲存任何空表。 **承重量測**:只把 `.oo/format` 改回 `2`、`HEAD` **留在新引擎寫的那個提交**,**舊引擎完整讀得回**(`log` 看得到該提交) ⟹ **新引擎寫的位元組本來就是舊引擎讀得懂的,鎖住它的純粹是宣告,不是內容。** **為何不選「把空表存成 CAS 物件」**:那讓每個舊倉永久帶一個無意義的空依賴,而且單向鎖仍是 `commit` 的副作用——本裁定要治的正是「一次成功回報的 commit 使倉不可開啟」。 | Q-038 recon(2026-08-24 首測／2026-08-25 於 v0.33.0 逐項重驗);`REAL_03` §6.8／§6.8.1;O23／O53／O54／O63 | **實作＝Q-038 工單**。**規格後果**:§6.8 第二條 MUST 須明說適用範圍(只治進了 sentinel 形式的根),否則 ② 與它字面衝突。**O53 未被違反**〔量〕:`status`／`log`／`evolve` 皆不動任何檔,只有 `commit`。〔量〕`/home/gali/nlang/.oo` 是**活的 pre-sentinel 倉**(2026-08-14 的真提交,今天兩個引擎都讀得到),在那裡跑一次 `commit` 即中彈 |
| **O74** | **✅ 已裁(2026-08-25,用戶):效果必須在 callback 存活;判準是障礙,不是靜態可見性。** **① 內建施用呼叫者態射時,必須把該次施用的效果 union 進結果——不論態射的回傳值有沒有被留下。** 〔量 v0.34.0,callback 矩陣〕今天效果**沿資料流**傳播:`~%List./map` 把回傳值放進輸出,效果跟著進去(`#io`);而 `~%Cond./if`／`~%Cond./match`／`~%List./filter`(不純謂詞)**值自己的 `.%effect` 是 `#pure`** ⟹ **值是假的,不只是閘瞎**。**⟹ 舊帳上的路徑 (a)「守衛改動態(值已帶正確效果,只差有人讀)」被量測推翻**——那句括號在五格裡只對一格。 **② `SPEC_08` §4.3 的「靜態守護」由本體降為實作註記**,判準改為 `TAG_REGISTRY` 那一句:**宣告的截面被非零障礙反證**。**理由取自 disc 023 §3 的字典表**,它逐字把這個守衛列為「宣告『有全域截面』(`#pure`) 但**障礙非零** → 說謊 → ⊥」,而 `#pure` 在該表的定義是「**值完全由 CAID 決定;全域截面存在**」⟹ **被檢查的是一個障礙是不是零,那是值的性質**;靜態只是今天查得到的**子集**。與 **REAL_03 §6.9(v0.33.0)** 接合:`#pure` 逐字就是「§6.9 對這個值成立且沒有格外的觀測座標」,**而一個靜態檢查無法見證一個關於值的宣稱**。 **③ 承重推論(給 ① 的本體理由)**:**障礙不是資料流性質**——`/filter` 為了決定去留看了三次時鐘,把布林丟掉不會讓那三次觀測沒發生過。故「回傳值有沒有被留下」與「障礙是不是零」無關,今天的實作把兩者綁在一起才是錯的。 **④ `/where` 的 IO 底線在 ① 完成後拆除。** 〔量〕它今天**連純謂詞都擋**(`~%Query./where ([{a:1}], (r -> #true))` ⟹ ⊥),那不是「對」是「保守」;① 完成後誠實的純用法必須寫得出來。**在 ① 完成之前仍不得拆。** **⑤ 繭不擋是對的且有本體理由**:disc 023 §4「cocoon = 自帶截面的局部 frame,傳染在邊界停」,不只是 `TAG_REGISTRY` 開了逃生門。 | Q-034 recon(2026-08-25,v0.34.0 逐格重量);disc 023 §3／§4;`SPEC_08` §4.3;`TAG_REGISTRY` `#effect_violation`;REAL_03 §6.9;O61 | **實作＝Q-034 工單**。**更正舊 recon 兩處**:(i)「28 個 callback 內建」數的是**實作端呼叫點**,`~%Option`／`~%Result` **不是使用者可及的模組**(`⊥ #missing_key`),使用者面為 `~%Cond` 3 ＋ `~%List` 約 16 ＋ `~%Query./where`;(ii) 該 recon 的「引擎算對了」只對 `~%List./map` 一格。**身分紅線**:〔量,既有〕效果**不進 BN/ 雜湊**(`bn_serial.rs:68` 逐字 `Value::Atom(kind, _effect, _rank)`) ⟹ 本弧不得使任何位址移動。**不併入本弧**:Q-034 recon 順帶量到 `~%List` 數個 callback 在恆真謂詞下給錯答案(`/partition`／`/group_by`／`/sort_by` 丟光元素),已另立 Inbox |
| **O75** | **✅ 已裁(2026-08-26,驗收方於收弧時判定;非新設計題,是把 O31 落地時撞到的界線劃清):** **未進入雜湊的位元組,分兩種。** 判準**不是**「它在不在雜湊裡」,而是**能不能在不改變解碼結果的前提下改動它**——`span` 可以(§6.6 重算照樣通過)⟹ §6.7 所禁;**決定解碼的自報**不行(必然改變解碼結果或使其不可解碼)⟹ §6.6 必然抓到,故可存在。 〔量,Q-012〕四種框竄改:改字樣／刪除／改為不存在者 → `#object_undecodable`(與完整性失效分開報);把 commit 物件的框換成值物件的框 → **`#caid_mismatch` 並報出重算的 digest**。 **並附一條界線**:框說的是「我是哪一種文件」,**不是「我是第幾版編碼」**——版本號仍然只住在 `.oo/objects.format`,故 §6.8.1／REAL_02 §5.1.1 的「CAS 物件不得自陳其編碼」在最窄的讀法下**根本沒被碰到**。 | 由 **O31 ②**(物件級自報)逼出;`REAL_03` §6.8.1 2026-08-26 更正＋新設 §6.8.3;`REAL_02` §5.1.1 界線澄清 | **這一格是我自己的裁定造出來的**:O31 ② 要求物件級自報,而那與一條既有 MUST NOT 表面衝突。劃界之後兩者都成立,但**若當時沒有去讀 §6.8.1,這個弧會在不知情的情況下違反一條規範條款。** |
| **O76** | **✅ 已裁(2026-08-26,用戶):傳遞慣例只有兩種,而「結構化統一」不是其中之一。** **① 兩種慣例 ＝ Curry(連續 juxtaposition)與「一個 Combo 引數」**;`(a, b)` 是後者的糖,鍵恰為 `0`、`1` ⟹ **tuple 不是第三種慣例**。**② 「關鍵字式」不是獨立的匹配機制**——`{target:, masa:}` 與 `{0:, 1:}` 走同一條路,差別只在鍵名。⟹ SPEC_07 §3 的「結構化統一(Structural Unification)」一詞刪除。**③ 態射必須在自己的值裡宣告它綁定哪些鍵(MUST)**,住處是 `%rules` 的模式鍵與 `%params`;應用側必須配對。 **考古(本條的起因,兩則皆已量)**:〔量〕規格 Genesis **2026-04-01** 就寫 `add {x: 2, y: 4}`,而引擎 Genesis **2026-05-23**(晚七週)的 `math.add` **逐字**是 `get_field("0")`／`get_field("1")` ⟹ **`{x, y}` 從來沒有在引擎裡當過引數鍵**,它大概來自 Rust 的區域變數名 `vx`／`vy`。**與頂層 `/add` 不同族**:那一個是實作先做並延續(Genesis `lib.rs:111`),這一個是**規格側寫了一個沒有實作對應的描述**。(用戶:PoC 期的提交在超專案未切 submodule 之前,兩邊都在從無到有,不必再挖。) **⚠ 本裁定的相反選項被量測擋掉了**:原擬「位置式是唯一正典,`{x,y}` 改 `{0,1}`」會使**七個具名內建違規**(`engine.project_down`／`project_up`／`set_strategy`／`check_oml`／`diff.patch`／`query.select`／`disc.find`)並**弄壞 conformance L2-104**(該向量正在釘 `project_down { target:, masa: }`,實跑通過)。**驗收方曾誤報「第三種慣例零實作」**,成因是只量了 `~%Math./add` 與一個使用者態射就下結論。 **⚠ 而本條並非全新裁定,它有一則 2026-07-25 的前身,驗收方開議時不知道**:〔讀 `crates/oo/tests/named_arg_reachability_probe_test.rs` 檔頭〕該弧逐字記著**「RULING (2026-07-25, user): fix at the APPLY layer — `unified_arg` also carries the argument's non-`%` named fields…(no spec change needed)」**,而其理由正是 **SPEC_08 §3.5 已以具名參數規定** `~%Engine./project_down { target: @Combo, masa: @caid }`。⟹ **具名鍵的正當性在一個月前就已裁**,O76 是把它一般化並補上『宣告』那一半。**該則裁定當時沒有取得 O 編號**(只住在 `ENGINE_SYNC` 與探針檔頭),正是 §2.4 末段那個漏洞的又一實例。 **驗收方的第二處更正**:先前把 `~%Math./add { x: 7, y: 17 }` 的結果記為「**把 combo 吞了兩次**」(`0: {x,y}` 同時頂層攤平 `x: 7 y: 17`),那是**誤讀**——那個雙重形正是 2026-07-25 修法的**實作結果**(`unified_arg` 同時攜位置包裝與具名欄位),`add` 不綁 `x`/`y` 故回部分施用的繭,行為正確。 | SPEC_07 §3;SYNTAX_09 §2 #1／§3／§4 #2;SYNTAX_11 §4 #4;〔量 v0.36.0〕;conformance L2-104 | **③ 是一條引擎未兌現的 MUST**:〔量 v0.36.0〕內建**全部不帶 `%rules`／`%params`**,而取鍵分兩群且互斥(**210 處 `0`/`1`** vs **具名者**——⟵ **2026-08-26 驗收更正:是 6 個不是 7 個**,`diff.patch` 的 `path`／`to` 是 diff **條目**上的欄,不是態射綁定的鍵;由交付方指出,成因是驗收方的擷取腳本不分「讀 `arg`」與「讀迴圈裡的條目」)⟹ 依 §2.4 已在 `WORK_QUEUE` Inbox 開列。**本裁定同時解掉「`~%List` 三種引數約定」那一列的裁定需求**——不需要選一種正準順序,需要的是每個內建把自己收的鍵宣告出來;錯的形因而不匹配,走既有機制(繭或 ⊥),不必新造。 |
| **O77** | **✅ 已裁(2026-08-26,用戶,一個字):D33 的射程含「內建在迭代中撞到 ⊥」。** ⟹ **Q2 不是新裁定,是 §2.4**——SPEC_10 §4.1.2 的「觀測撞到矛盾時停下並說出來」自 2026-08-06 起就是規範,本次只是確認它不止於並行提交那一格。落地位置**不在** SPEC_10 而在 **SPEC_08 §4.3**,與 O74 ①「施用即觀測」並列——**兩者是同一條責任的兩面**:O74 ① 治「觀測發生了卻沒被記下」,本條治「觀測崩了卻被當成一個答案」。 **判例**〔量 v0.36.0〕`~%List./count` 的謂詞回 `#false`／`5`／`⊥` **三者同得 `0`**;成因逐字是 `result.to_string_plain().trim_start_matches('#') == "true"`,**它問的是印出形不是真值**,同一行亦使字串 `"true"` 與 `#true` 同格。〔對照〕普通施用路徑三種撞 ⊥ 的方式**全部正確傳播** ⟹ 折疊只在內建自己的 callback 迴圈裡。 **⚠ 本條刻意不要求中止**:現況在格論上是單調的(⊥ 不是 `#true`,故不計數),**缺陷不在答案在不可分辨**——與 CALM 那一格(`commit.md` §1.1.7)同形,回報是那個區分唯一能住的地方。 | D33／SPEC_10 §4.1.2;O74 ①;`commit.md` §1.1.7;〔量 v0.36.0〕 | 與 **O76** 合成一件施工(Q-039):O76 治入口(合約沒宣告),本條治出口(⊥ 被折疊)。**兩者同一個成因**——內建繞過語言自己的施用機制,把它做對的兩件事各自重做且都做錯。 |
| **O78** | **✅ 已裁(2026-08-26,用戶,Q-039 偵察後兩題):** **① 射程含「統一」(選項 B)**——用戶語:「**這比較像是把沒說清的東西說清**」;**但判斷須有彈性**:現有的不一致究竟是「習慣」還是「必須」不得預設,而一致性的**粒度是模組**——「同一個模組底下,相似型的態射要傾向使用同一種」。**② 宣告住 registry,不進值(選項 i)** ⟹ **本弧不動身分、不是紀元**,不必掛在 D-3 ＋ Q-035 Q1 那次搬遷底下;O76 ③「住在態射自身的值」的**字面因此鬆綁**,其**目的**(合約必須是一件規格事實,不是一件 Rust 事實)由「規格定義 registry 宣告的形狀與內容」達成。 **③ 「習慣還是必須」已由量測回答**〔量 2026-08-26,v0.36.0〕:`|>` 把被管的值放進**下一個空位**(`lib.rs:3049` 逐字 `nf.insert((max_idx + 1).to_string(), arg.clone())`)⟹ **管道只與「資料放最後」組合得起來**。逐格:pred-first 群在管道下**全對**;**`/take_while`／`/drop_while` 在管道下回 `[]`(無聲錯)**;**`~%Query./where` 在管道下回 ⊥**。⟹ 那個不一致是**習慣**,但**兩種習慣不等價**,其中一種與語言的核心運算子不相容。最鋒利的一格:**`~%Query` 是查詢模組,最該被管道用,而 `./where` 今天在管道下是 ⊥**。 | Q-039 偵察 `nlang-tools/docs/a_contract_that_lives_in_rust_recon.md` §5／§8／§11;O76;O77 | **正準判準的候選(偵察方建議,待用戶確認即可寫成 MUST)**:**凡意圖被 `|>` 使用的態射,其資料參數必須是最後一個。** 它比「模組內一致」更強——給出「傾向哪一種」的理由而不只是「要一致」;模組內一致性可留為 SHOULD。 **⚠ 統一的射程大於已量到的那幾個**:`list.fold` 是**第五種形**(取兩鍵、嗅探哪個是清單、另一個必須是 `{ %val: 初值, %f: 態射 }`)⟹ 工單須逐個列出 40 個 `~%List` 註冊點的現行形。 |

> **O62 帳務更正（2026-08-19；不改裁定）**：O62 列中「非單位元不等於非 pure」是
> 開單早期的錯句，已由工單與 v0.26.0 規格收尾推翻。對現有五個基礎標籤，判準
> `effect != Pure` 與「非格的 ⊥」相同，兩者都**包含 `#cached`**。真正風險是因為
> `#cached` 讀起來像已固化而額外豁免它（例如 `effect != Pure && effect != Cached`）；
> 不得豁免，`#cached` 必須落成欄位。本註覆蓋 O62 列內該句，其餘裁定與先守衛後正規化不變。

> **交付狀態收帳（2026-08-19）**：O56 第二條多重具備已由 Q-025／v0.23.0 交付，
> 第一條「規格定義具體標準根並公佈 CAID」現為 Ready Q-033；O58／O61／O62／O63
> 已由 Q-032／v0.26.0 交付。各列較早寫下的「Active／未開弧／應同弧」只保存當時依賴，
> 不再表示現行施工狀態；現行排序唯一以 `WORK_QUEUE` 為準。

### 2.4 控制面

| # | 問題 | 出處 | 依賴 |
| :-- | :-- | :-- | :-- |
| O24 | 控制面要不要成為 **合法並行寫者**？（不要→互斥響亮錯；要→CAS+重試；可能是弧 A 副產品） | control_plane §4.1 | 止血可先於裁定 |
| O25 | 認證 = 能力格第二種出示？（令牌 = 可序列化能力） | control_plane §4.2 | D3 |
| O26 | mTLS CN「觀測身份」——**第幾個身分？** 未過六問表 | control_plane §4.3 | **不得當承重** |
| O27 | 端點表重畫成 CLI 投影（非實作舊六個） | control_plane §4.4 | O24、O25、O26 之後 |
| O28 | 多可變檔（staged/pin_pending/…）的原子邊界 | control_plane §1.4.3 | **弧 A 單一可替換物件** |
| O29 | reftable 式容器是否用於 savepoint 鏈（借結構、不借 physical log 形狀） | control_plane §1.5.3 | 弧 A；peers/directory 已是 append 先例 |
| **O81** | **n/ 的第二條隔離軸沒有家。**（2026-08-27，用戶提問引出）n/ 有**兩種** Isolation，而它們是分類法上不同的兩種：**值**的隔離是 **ACID 的**（○ 有 commit point，遲早合流；理論是 serializability），**容器**（`.oo/` ＝ 節點）的隔離是**虛擬化的**（永遠不合流；理論是 **non-interference**，失效模式是 **side channel**）。`commit.md` §1.5.1 借用了第一種且借對了，**但第二種在任何一份文件裡都沒有主詞**。〔證據〕四個已出貨的安全級缺陷全部住在第二條軸上——**Q-029**（拒絕只蓋住讀）／**Q-030**（47 位元組遠端終止節點）／**Q-031**（持有但開不起來）／**Q-035**（名字不是憑證），四者問的都是「低完整性的資料能不能影響高完整性的動作或觀測」＝**資訊流**問句；而 `WORK_QUEUE` §9.1 逐字記著「它們不是這條線的進度」——**因為它們根本不在那條軸上**。⟹ **待答**：這條軸的家在哪（`REAL_01` §7 本機面？`control_plane`？還是自成一節），以及它的**不變量**該怎麼寫（今天四個缺陷各自有修法，**軸本身沒有條文**）。**⟵ 2026-08-28：不變量有候選形狀了 ＝ frame rule**（commit §1.1.8 ⑩，用戶帶回 `separation algebras`）。**代數位置也定了**：格與 separation algebra 的共同母體是偏交換么半群，**分岔點只有「冪等」一條公理**（可消去 ＋ 冪等 ⟹ 只有單位元），而冪等就是 contraction ⟹ **n/ 值層極度 contractive，這正是 §1.1.8 ⑤「衝突圖沒有邊」的代數版**。**四個缺陷可統一敘述為 frame 沒守住**（$C$ 碰到 $P$ 以外的資源）⟹ 第一次有一條共同的規範形狀，而不是四條各自的修法。**⚠ 限制**：separation logic 為**可變狀態**而造，n/ 值層無 mutation ⟹ frame rule 在值層是平凡的，**只在容器（`.oo/`）與 `pin` 咬得到**——那兩處正好是 n/ 裡唯二「東西是資源不是資訊」的地方。**風險**：下一個同類缺陷仍會從別條線的偵察裡掉出來，而那是它第五次。**⟵ 2026-08-29：第二個結構候選，而且與第一個同構** ＝ **local／published 這條界線就是這條軸**（`commit.md` §1.2.2，用戶提問引出）。〔讀〕`refine` 第一步逐字 `verify geometric monotonicity (new & old = new)`（`universe.rs:1254`）＝ **檢查 `⊑`**，而 `commit` 走 **meet** ⟹ **引擎兩半都已實作，規格從沒說過那正是它們的分別**（§2.5 那一類）。**判準**：local 用 meet（⊥ 是正當答案，D33 已裁可入歷史）；published 用檢查（**⊥ 就是攻擊本身**——任何人都能遞一個值把你的狀態變成 ⊥，那正是 Q-030「47 位元組遠端終止節點」在值層的長相）。**與 frame rule 同構**：一個已發布的精煉不得碰它所證明之外的東西，⊥ 中毒逐字是 frame 沒守住。**連帶解釋** `SPEC_10` §4.1.2（§0.1「最大的一處對不上」）為何無法被遵守：**它用一條規則回答一個需要兩條的問題**，而它寫於這條界線存在之前 | commit §1.1.8 ⑧⑨、**§1.2.2** | **不堵弧 A**；但 D3（本機面／服務面兩套機制）與 Q-035 的 Q2a（寫入層）都在它底下 |

---

## 3. 舊開工順序（歷史快照；不再排程）

> 以下保留 2026-08-09 前「為何 W8′ 必須早於弧 A」的依賴推導，但其中多項狀態
> 已被 v0.13.0–v0.19.0 改寫。現行排序只看 [`../WORK_QUEUE.md`](../WORK_QUEUE.md)。

原則：**先消假敘述與損壞路徑 → 再動已裁定的機械遷移 → 再弧 A（○/日誌）→ 控制面與服務面掛在 A 之後。**  
CLI 主題（動詞形狀、命名空間細節）可後置。

### 第 0 階 — 零裁定、現在就能做

| 序 | 工作 | 為何現在 | 對應 |
| :-- | :-- | :-- | :-- |
| ~~**W0**~~ | **✅ 已完成 2026-08-06/07，切 v0.11.0**（`atomic_write` = 同目錄 temp + `fsync` + `rename`；REAL_01 §4.1.1 三條 MUST 新設）。**掛帳**：`peers.rs:452` 壓實為手工 temp+rename **無 `fsync`** 且 `.ok()?` 吞錯；`oodp.rs:639` 歸屬聲明仍裸 `fs::write` | | |
| **W1** | **紅測釘死** 並行 evolve 遺失（40 行程） | 防止回歸；記錄「修復前」行為 | control_plane §1.1 |
| | ↳ **注意**：v0.11.0 **刻意只修損壞、不修遺失**。此紅的修復方屬 **W12**（CAS+重試），不屬 W0 | | |
| **W0′** | **✅ 工單已開**：`nlang-tools/docs/verdict_must_gate_handover.md`（*一個不改變任何行為的偵測，不是偵測*）。①`peers::compact`、②`AffiliationClaim::write_file`、③`DiscoveryConfig::write` 三處改走唯一實作 `storage::atomic_write`（含**可預測暫存名**這個獨立缺陷）；④`mark` 讀路徑**必須重算位址**；⑤走訪不完整時 `run_gc` **不得清除任何物件** | **不需新裁定——是 REAL_03 §6.6 三條 MUST 的不合規**（重算義務／消費端不得丟棄裁決／裁決必須為真）⟹ 收尾歸 **ENGINE_SYNC**，不進規格正文。**危害鏈不需要攻擊者**：崩機 → 撕裂 → `#object_undecodable` → 走訪把它當葉子 → 只經它可達的後代被掃掉 | 工單 §1–§7；探針待寫 |
| ~~**W2**~~ | **✅ 已完成**(`nlang-spec local c491484`)——`commit.md` §1.1.6 與 §2.1.2 兩處更正都已落 | | |

### 第 1 階 — 純規格、引擎已行為一致（「讓規格重新變真」）

| 序 | 工作 | 為何 | 對應 |
| :-- | :-- | :-- | :-- |
| **W3** | **半完成**：**B1 已落 v0.11.0-draft.1**（SPEC_10 §4.1.2 拆成「觀測必須回報」／「固化由操作者決定」，粒度改 ○，射程限縮到自動收斂）。**B2 未落**——〔量 2026-08-07〕`spec/zh_TW` 全樹 `published` 零命中，local／published 之分仍只在原料裡，落腳處未定 | B2 的後半（`refine` 的「可執行/通過檢查」）本就依賴 O14 | commit §2.1.3；D34 |
| ~~**W3′-a**~~ | **✅ 已完成並切 v0.12.0／v0.12.0-draft.1**（SPEC_10 §2.2.1 新設五條 MUST；引擎 `evolve` 改傳 `BottomDetail`，兩處丟棄點補回。**零代修**） | 見 D37–D39 | `nlang-tools 0328319`；`nlang-spec 0c737c6` |
| **W3′-b** | **提交時報告 ⊥ 座標** — 2026-08-09 曾因 `~%Config` 可抵達 commit 衝突而重開；**v0.15.0 已使視界參數不進歷史，該活案例失效，現重新阻塞於 W12** | **〔量 2026-08-08〕§4.1.2 的 MUST 不是在被違反，是在等 W12**：合併型 ⊥ 在 evolve 邊界就被攔（`staged` 從未寫出 ⟹ 「Nothing to commit」）；明寫的 `_|_` 提交成功但**射程 MUST NOT 明文排除**；並發型需要 commit 重讀 HEAD 而它不重讀（D32），晚到者只會靜默覆蓋、不產生 ⊥。**驗收方先前寫的「現況為不合規」係誤判，此處更正** | commit §2.1.3；D32；`WORK_QUEUE` Q-017 |
| ~~**W4**~~ | **✅ 已完成（純規格，2026-08-09，未切版）**：TAG_REGISTRY **§0 三軸六載體**（規範性）＋ §1 各列「軸·載體」欄（62 列）＋ **§2 逐載體索引** ＋ §2.7 兩則命名裁定；`#incomplete` 移入 §1.2；SPEC_08 §3.2.1 與 REAL_02 §3.2.1 改為引用 §0；SPEC_04 §6 補策略相依 | D7 的「不需任何裁定」**已被量測部分推翻**——見下方 W4′ | observation D7 |
| ~~**W4′**~~ | **✅ 已完成並驗收(2026-08-09,尚未切版)**:`#routing_budget_exceeded` 與 `#max_depth_exceeded` 已鑄;**零代修**;**破壞性 #9**(只動深度型 `#blur` 的 CAID)。規格收尾含 **SPEC_08 §3.2.2 第 3 款改寫**(依實際耗盡者)＋語料六向量 | 見 ENGINE_SYNC 2026-08-09 | TAG_REGISTRY §2.7 |
| ~~**W4″**~~ | **✅ 已完成並驗收(2026-08-09,尚未切版)**:`timeout`／`max_branches`／`max_unification_depth` 在演化期生效;`~%Config` 不進歷史且提交時明說(O37)。**零代修**。規格收尾＝**SPEC_09 §6.0.1／§6.0.2 新設** | **驗收方偵察有一處太寬已更正**:`max_unification_depth` 在 unify 路徑一直是生效的,反證就在 W4′ 自己的探針裡 | O37／O38 |
| ~~**W4‴**~~ | **✅ 已完成並驗收(2026-08-09,尚未切版)**:硬界與政策界分開(`#stack_overflow` 新設)、空提交修好、O41 落地。**破壞性 #10**——創世 `~%Config` 改值移動每一個根 CAID(系統軸進 CAID)。**⚠ 交付方改了三個釘,根因在驗收方的工單**(宣告非破壞性、把釘釘在不可能滿足的值上)。原文:**⚠ 射程須加 (d):v0.15.0 已出貨一個當機路徑**——`max_unification_depth` 調大到約 490 以上,`oo evolve` 深運算式 **SIGABRT**(無 ⊥、無訊息);**由 W4″ 引入**(v0.7.0 同設定退出碼 0)。見 ENGINE_SYNC。原射程 **(a) ＋ O41**(2026-08-09 用戶裁定);(b′) 待 O40、(c) 已由 O41 吸收。原文:**W4″ 留下的三筆**:(a) **暫存只剩 `~%Config` 時會鑄空提交**（本交付引入;工單 R4 未涵蓋「只有 Config」的情形——缺口在驗收方）/ ~~(b)~~ **已撤回——旋鈕跨行程是生效的**（新行程 `oo evolve` 實測會 blur）;**取而代之的 (b′)：`oo run`／`oo eval` 看不見這個倉**（連已提交的 `y: 5` 都讀回 `_`），而 **REAL_01 §1.1 記載了一個不存在的 `--load` 旗標**——待裁/ (c) **`timeout` 只在明設時生效**,創世 `timeout: 1000` 未武裝（實測 2144 ms 跑完） | (a) 是新的、(b)(c) 是舊的;**三者都使「旋鈕是可信的」這句話還不成立** | W4″ 驗收 §11.5;SPEC_09 §6.0.2 自陳缺口 |
| **W5** | 可選：`Approximate` 缺陷標註或廢止提案 | 真缺陷、範圍小 | O4 |

### 第 2 階 — 已裁定的機械弧（有遷移、有安全清單）

| 序 | 工作 | 為何 | 對應 |
| :-- | :-- | :-- | :-- |
| **W6** | **HOME `~/.nlang/` 改名** + `fs_guard` / `OO_NODE_HOME` / 遷移 | 已裁 D19；延遲會讓「同名碰巧安全」繼續遮 §2.2 | O22、dot_oo §2.2 |
| **W7** | **`~%Diff` → `~%Engine` 空殼搬家** | 已裁 D5；零遷移成本 | commit 弧 C |
| **W8** | （可選）`nodes/` 鑄造時機收緊——純本地勿鑄 | 減只增不減；與 O21 相關 | dot_oo §2.3 |
| **W8′** | **`.oo/` 耐久編碼改用 n/ 自身**（先偵察量代價 → 裁 O31 → 遷移）。含 `gc.rs` 走訪改走 n/ 值 | **必須早於弧 A**：W10/W11 每新增一種耐久檔，這筆債就貴一次。且它有**現成的迴歸閘**——走訪結果必須逐項一模一樣，這是 combo 化沒有的。用戶判斷：可能比 combo 化更早能吃自己的狗糧 | O31；disc 028 §6 |

### 第 3 階 — 弧 A（○ 層 + 日誌；控制面併發的真正底座）

| 序 | 工作 | 為何 | 對應 |
| :-- | :-- | :-- | :-- |
| **W9** | 裁定 **O13**（evolve 注入/固化拆分）→ 鎖定日誌詞彙 | A3 依賴 A4 | commit A4 |
| **W10** | **單一可替換 savepoint 物件**（hash 指標 + 欄位含 pin/effect 意圖） | 多檔 CAS 不可能〔**此處 `CAS` ＝ compare-and-swap，不是內容定址**；2026-08-29 補註，原字未動〕；控制面 O24/O28 的前提 | control_plane §1.4.3；A1 |
| **W11** | **`wal/` + `audit.log`（logical log）** + parent 不再設定 | 已裁名字 D17、策略 D18 | A2–A3；dot_oo |
| **W12** | meet 路徑 **CAS+重試**；`pin` 路徑 **序列化** | 兌現 SPEC_10 §4.1；CvRDT 紅利 | control_plane §1.4 |
| **W13** | 回頭看 **O16 pin 傳播** 是否還在 | 文件已警告勿先寫規則 | commit §4.4 |

### 第 4 階 — 身分索引與控制面產品化

| 序 | 工作 | 為何 | 對應 |
| :-- | :-- | :-- | :-- |
| **W14** | 裁定 **O18** 路徑=索引 + 索引形狀 | D22 介面已定，缺機制 | dot_oo |
| **W15** | **O19** 換機繼承 + `oo node adopt` 實作 | 防偽有、遷移無 | dot_oo §1.6.1 |
| **W16** | 裁定 **O24**（若 W12 後仍須產品層「單寫者 daemon」再說） | 可能已由 W12 消化 | control_plane |
| **W17** | **O25–O26** 能力出示 + 連線身分過六問 | 服務面才需要 §7.1 | control_plane |
| **W18** | **O27** 端點表 = CLI 投影重畫 | 最後畫表 | control_plane §2.1 |

### 第 5 階 — 全球圖與其餘（可與 3–4 交錯，但不堵 W0–W3）

| 序 | 工作 | 對應 |
| :-- | :-- | :-- |
| **W19** | B3/B4 全球索引與 DECLARED 信任根 | O10–O11 |
| **W20** | 工作區三類怎麼分、format 範圍 | O20、O23 |
| **W21** | 觀測軸深改（Blur 型別、TopCaused、cause 統一） | O1–O5 |
| **W22** | CLI 主題（動詞全集、help 形狀等）。**設計稿已落 `meta/oo/cli_surface.md`（2026-08-09）**——`--universe` 軸、`observe`／`eval`／`commit` 三動詞、`load` 併入 `observe --from`。**依賴 W6（名字：兩個 `.oo` 不拆則旗標有歧義）與 W10（savepoint：`--dry-run` 沒有東西可以「不建」）** | 用戶：不急；但 **W10 定形時應以本稿為第一份需求** |

---

## 4. 舊依賴圖（歷史快照）

```text
W0 temp+rename ✅ v0.11.0 ───────────────────┐
W0′ 殘帳 fsync/atomic + GC 讀 integrity       │
W1 紅測 並行 evolve（修復方在 W12）           │  止血 / 真敘述
W2 文件更正 §1.1.6 / §2.1.2 ✅                │
W3 B1 ✅ / B2 未落 ── W4 ✅ ── W4′ ✅ ── W4″ 旋鈕不生效 │
W3′-a 矛盾在哪 ✅ v0.12.0                         │
W3′-b ⊥ 座標（曾因 W4″ 重開；v0.15.0 後再等 W12）│
W6 ~/.nlang/ + fs_guard                      │  已裁定機械
W7 ~%Engine 空殼                             │
W8′ .oo/ 改用 n/ 自身  ◀── 必須早於弧 A       │
                    ▼
            W9 evolve 拆分 → W10 單一 ○ 物件 → W11 wal/audit
                    ▼
            W12 CAS+重試 (meet) / 序列化 (pin)
                    ▼
     W14–15 身分索引    W16–18 控制面產品化    W19+ 全球圖
```

**關鍵洞察（control_plane §1.4.3）：**  
「控制面能不能並行寫」**很可能不是前置裁定，而是弧 A 的副產品**——  
故 **W0 止血與 W3 規格可先於「單寫者 vs OCC」的產品裁定**。

---

## 5. 明確不要現在做的

| 不要 | 為什麼 |
| :-- | :-- |
| 先畫完整 JSON-RPC 端點表 / 實作 REAL_01 舊六端點 | D2、O27；投影優先於考古 |
| 本機面引入 §7.1 長期令牌「湊形式」 | D3、REAL_01 §7.0.2 |
| 把 mTLS「觀測身份」當承重 | O26 未過六問 |
| 先寫 pin 傳播規則 | O16；可能隨弧 A 消失 |
| 為 n/ 工作區引 reftable「因為 git 有、為了規模」 | 規模動機不適用；可借的是原子多指標 + 日誌同構 |
| CLI 大整（用戶：不急） | W22 |

---

## 6. 舊的下一份工單建議（W8′-a 已於 v0.13.0 完成）

> 本節留存當時如何由 M1–M9 拆出 W8′-a/b/c；**不得**再據此開 W8′-a。
> 現行下一弧為 `WORK_QUEUE` Q-001（W8′-b 的裁定／偵察）。

~~原建議（W0+W1）已於 2026-08-06/07 切 v0.11.0。~~
~~W3′-a *A conflict is not where you were typing*~~ 已於 2026-08-08 切 v0.12.0。

**當時建議：W8′ 的第一弧 ── 但它不是「換編碼」。（W8′-a 已完成）**

偵察（§2.3.1）把 W8′ 拆開後，**換編碼是最後一步，而且是最便宜的一步**。
擋在前面的是三件按此順序的事：

| 弧 | 內容 | 為何在這個位置 | 破壞性 |
| :-- | :-- | :-- | :-- |
| **W8′-a** | **`to_nlang` 對 `Value` 成為全函數**——Thunk／Code／Ref 三個變體不得再走 `{:?}` | M4。**與 W3′-a 同一缺陷類**，且是換編碼的前提：印不出來就存不進去。**非破壞性、不動 CAID、有現成迴歸閘**（既有 fmt v2 凍結 + 往返） | 否 |
| **W8′-b** | **裁 O35，並據以決定「存的是什麼」**——若耐久形不可求值，則**存入前強制** | M3／M6。這件事**改變身分**，且它才是 M1 的成因 | **是**（CAID 全面失效） |
| **W8′-c** | 真正換編碼 JSON → n/ ＋ `.oo/format` 升版 ＋ 遷移 | M5。前兩件做完後這是機械的 | 格式版本，非 CAID |

**當時建議只開 W8′-a；此建議已於 v0.13.0 執行完畢。** 當時理由：

1. **它是唯一今天就能做完的**——b 需要用戶裁 O35，c 依賴 b。
2. **它有一個 W8′ 之外的獨立正當性**：M4 漏的是 `span` 與 `RwLock { poisoned: false }`，
   而 SPEC_10 §2.2.1 上週才立下「不得洩漏實作表示」的 MUST。同一句話在印表機上
   還沒有對應的規範面——**這是規格缺口，不是只有清潔問題。**
3. **它把 M5 那個數字從「不能當結論」變成「可以當結論」**——今天的 22,269 B
   裡有一段是 Rust Debug，補完之後才知道 n/ 形到底多大。

**不要在這一弧做：** 強制（改身分）、換編碼、動 `system` 進不進 CAID
（M2 已量完，歸 W8′-b 一起裁）。

**M2 已完成（見 §2.3.1「M2 跨版本重算」）**：機制屬實且已隔離（消融
`~%Effect` 單獨就移動根 CAID，控制不動），但**九個 minor 之間沒有在累積**。
順帶量到 M8（v0.2.0 物件今天讀不出來，有控制）與 **M9（唯讀路徑上蓋假的
`format=1`）**；M9 開 **O36**。

---

## 7. 素材

* `meta/oo/observation_result.md`（2026-08-02…）  
* `meta/oo/commit.md`（2026-08-02…）  
* `meta/oo/dot_oo.md`（2026-08-04…）  
* `meta/oo/control_plane.md`（2026-08-05…）  
* `docs/discussion/028_content_addressing_two_uses_and_unison.md`（超專案，2026-08-07）
  ——**不是原料而是外部比較**；下沉三筆 → D36、O31、O32（＋O33、W8′、W3′ 升序）  
* 引擎／規格交叉見各篇 §素材出處  
